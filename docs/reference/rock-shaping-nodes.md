# 岩らしい形と色のためのノード設計メモ

作成日時: 2026-09-22 00:30
更新日時: 2026-09-22 23:10

## 目的

参考画像（`docs/references/thumbnail_bea6868d-…​.webp`。Git 対象外の手元資料）のような岩塊を、現在のノードを土台にして作るために、足りない処理と概念を整理する。ここに書く内容は候補であり、実装済みかどうかは各項の「状態」に書く。実装順と現在地は [plan](../plan/plan.md) と [progress](../plan/progress.md) を正とする。

## 参考画像の分解

| 層 | 画像で見えるもの | 現状 |
| --- | --- | --- |
| Macro（全体形） | 非対称な塊。数個の大きなブロックの集合 | Random Boxes / Voronoi で近いものを作れる |
| Meso-大（割れ目） | ブロック間の深く暗い溝、斜めに走る割れ | 無い。ピースを結合すると接触面が消えるか、隙間がそのまま残る |
| Meso-小（面の質） | 平面的に欠けた小面の連続、鋭い稜線、段状の剥離 | 無い。格子由来のなまった面だけ |
| Micro と色 | 細かい凹凸、溝に溜まる橙の錆、凸部の灰色の風化、面の向きで変わる色 | 材質はあるが、形状に連動するマスクが無い |

効果が大きいのは、SDF 上で形を「面で削る」操作と、形状から材質マスクを作る仕組みの2つ。

## ノードの候補

効果が大きいと見込む順に並べる。1〜5 は格子点ごとに独立した計算で、`Volume.cpp` の `FillSlices` による並列化にそのまま乗る。

### 1. Volume Boolean

状態: **実装済み**（2026-09-22）。仕様は [Volume Boolean](volume-boolean.md)。

SDF どうしの和・交差・差。なめらかさの係数で、つなぎ目の角を残すか丸めるかを選ぶ。2・3 の「面で削る」「割れ目を彫る」の土台になる。To Volume が Mesh 入力に対応しているので、`Voronoi Fracture → Piece Filter / Transform → Pieces to Mesh → To Volume` を B 側へ入れれば、分割結果で本体を彫れる。

### 2. Plane Cuts（平面群によるファセット化）

状態: **実装済み**（2026-09-22）。仕様は [Plane Cuts](plane-cuts.md)。参考画像の印象の中核。

画像の岩は丸い塊ではなく、多数の平面で切り落とされた形をしている。SDF に対して `d = max(d, dot(n, p) - h)` を N 枚分繰り返す。

- 法線は等方か、主方向1〜3系統（向きを回した直交座標系の軸 ± ばらつき）。主方向は節理に沿って割れた方向性を出す。削除した Joint Set の考え方を、メッシュ切断ではなく SDF 上の安価な操作として戻した形で、[goals](../plan/goals.md) の「主破砕は方向性を持つ Joint Set に基づく」と整合する。
- 「全体」を少数（Macro の面取り）と、「局所」を多数（Meso の欠け）の2段で使う。
- 実装して分かったこと: 形全体を切る半空間だけでは、枚数を増やすほど凸包へ近づいて凹凸が消える。Meso の欠けには範囲を限った切り落としが要る。範囲を球で区切ると球の壁が丸いくぼみとして残るので、稜線と角の欠けだけを選別している。平らな面の中央に変化を付けるのは 4 の役割になる。

### 3. Volume Crack（割れ目を彫る）

状態: **実装済み**（2026-09-22）。仕様は [Volume Crack](volume-crack.md)。

Voronoi の境界面からの距離を使って溝を彫る。`d = max(d, 半幅 − 境界面までの距離)` の形。

- Scatter Points の点を SDF 側へ渡す方式にした。境界面までの距離は F2 − F1 の近似ではなく、垂直二等分面までの距離の最小（セルが凸なので厳密）を使う。
- 幅は一様にしない。割れ目ごとの倍率（一部は閉じる）と、割れ目に沿ったノイズで変える。断面は V 字で、指定の深さで閉じる。
- 割れ目は指定の深さまで彫られ、見えない奥の壁までメッシュになる。後ろに [Volume Close](volume-close.md)（2026-09-22 追加）を置くと、幅が設定値を下回る深さから下が埋まる。
- 実装して分かったこと: 幅がセル間隔を下回る部分は格子で潰れる。画像にある髪の毛ほどの線は、形状ではなく 7 の焼き込みで表す領域になる。割れ目は平面の組み合わせで直線的に見えるので、崩すには 4 が要る。

### 4. Volume Displace / Noise

状態: **実装済み**（2026-09-22、ノード名は Volume Noise）。段化（Terrace）を除く。仕様は [Volume Noise](volume-noise.md)。

- Domain warp（歪み）: サンプル位置を低周波ノイズでずらす。Voronoi の直線的すぎる境界や、平面カットの機械的な印象が崩れる。
- ノイズで削る: なめらか（値ノイズ）、セル状（Worley F1）、小面（特徴点ごとのランダムな平面）。平面的な小面の連続には、Worley の距離そのものより、セルごとの平面のほうが近かった。
- 実装して分かったこと: 外周を空に保つため、ノイズは削る方向に限った。盛り上げる方向が要るなら格子を広げる必要がある。セル間隔より細かい凹凸は潰れるので、細かさの目安は解像度 ÷ 4 まで。
- Terrace（段化）: 層状の剥離。**実装済み**（2026-09-22、独立したノード [Volume Terrace](volume-terrace.md)）。層の法線に射影した位置の端数で、へこむ層と残す層を交互に作る。層ごとのばらつきと境のゆらぎを持つ。

### 5. Volume Smooth / Sharpen

状態: **実装済み**（2026-09-22、[Volume Smooth](volume-smooth.md)）。

Gaussian や平均曲率流で角をなまらせる量を、マスクで変える。画像では上面が風化で丸く、側面の割れ口は鋭い。全体を一様に処理するとこの差が消える。

- 角だけを削る版として [Volume Edge Wear](volume-edge-wear.md) を追加した（2026-09-22）。ぼかしとの差を稜線の強さにし、面を保ったまま稜線と角だけを摩耗させる。ばらつきで欠けた角になる。
- 実装して分かったこと: 分離ガウスぼかしで十分に丸くなる。量の変化はマスクや Field 型ではなく、入力の勾配から求めた法線の上向き度（「上向きに集中」）で持たせた。ぼかしは格子の外を距離で外挿しないと、余白が2セルしかないため面が外へ押し出される。シャープはアンシャープマスク。

### 6. 形状からマスクを作る

状態: **一部実装済み**（2026-09-22、Shape Mask。オクルージョン / 上向き度 / 高さ）。仕様は [Shape Mask](shape-mask.md)。色の決め手。

Material Mask は定数と画像だけを持つ。形状由来のソースは Shape Mask ノードにまとめ、種類で選ぶ。UV付きの Mesh を入力に取り、そのUVに対応するマスク画像を出す。選択するとマスクを貼った状態で見られる。残りのソースは種類として足す。

- Cavity / Curvature: 溝に錆の橙、凸稜に明るい欠け色。
- AO: 実装済み（種類「オクルージョン」）。Material Bake の GPU 形状AOとは別に、評価スレッドの CPU で求める。
- Normal 方向（上向き度）: 実装済み（種類「上向き度」）。上面の埃・風化・苔。
- Height / ワールド Y: 実装済み（種類「高さ」。反転で接地側の汚れ）。
- Piece / Face origin: 「切断面」と「元の外面」で材質を分ける。`Piece::faceOrigins` は既に保持している。割れ口は新鮮な色、外面は風化色という対比は、画像の説得力の大きな要素。

Apply Material は最大8段の合成を持つので、これらがマスク入力へつながれば色の層は組める。

マスクどうしの合成: **実装済み**（2026-09-22、[Mask Combine](mask-combine.md)）。乗算 / 最大 / 最小 / 差 / 混合で、「溝のうち上面を除く」のような条件を1つのマスクにする。対象は UV 空間の画像（Shape Mask / Mask Combine）に限る。

### 7. ディテールの焼き込み（High → Low）

状態: 未着手。

画像の細部はジオメトリではなく法線と Height に入っている。密な格子は各軸192点が上限なので、Micro を形状で出すのは無理がある。

- Height から法線を合成する。Triplanar の Height は現在、表示専用。
- 高解像度の SDF から、簡略化したメッシュへ法線をベイクする。簡略化は [Decimate](decimate.md) として実装済み（2026-09-22）。ベイクが入ると、ゲーム用アセットとして完結する。

## 概念として足したいもの

- **Field（スカラー場）型**: ノイズ、距離、曲率、マスクを Volume にも Material にも渡せる共通の型。6 のマスクと、4 のノイズによる変調（割れ幅、平滑量）が同じ仕組みで書ける。Plane Cuts と Volume Crack は Field 型なしの単体ノードとして実装した（Crack の幅のゆらぎはノード内のノイズ）。6 のマスクに着手する段階で、入れるかどうかを判断する。
- **スケールの階層を明示する**: 各ノードの設定をセル間隔の倍数でも指定できるようにして、解像度を変えても見た目を保つ。
- **解像度の天井**: 192³ の密格子では、4 のノイズと 3 の細い割れ目がエイリアスする。[plan](../plan/plan.md) にある OpenVDB / Sparse 導入の条件（解像度100超、レベルセットのフィルタ）に、2〜5 を入れた時点で当たる見込み。まず密格子のまま 1 → 2 → 3 を試し、解像度不足が見えた段階で移行を判断する。

## 想定するグラフ

```text
Random Boxes → To Volume ─┐
                          ├ Volume Boolean（和。角を残す）
Base Shape → To Volume ───┘
   → Plane Cuts（大・少数・主方向2系統）
   → Volume Crack（Scatter Points。幅をノイズで変える）
   → Plane Cuts（小・多数・表面近傍のみ）
   → Volume Noise（warp + 小面）
   → Volume Terrace（層状の段。必要なら）
   → Volume Smooth（上向きの面だけ強く）
   → Volume to Mesh（Dual Contouring。稜線が残る）
   → Remesh（三角形を一様に揃える。2026-09-22 追加）または Decimate（UV 展開を速くし、ゲーム用の面数へ減らす）
   → UV Unwrap ─→ Shape Mask ×n（オクルージョン / 上向き度 / …。UV付きの Mesh が入力）
                 → Mask Combine（差・乗算などで種類を組み合わせる）
   → Apply Material ×3〜4（Shape Mask / Mask Combine を Mask へ）
   → Material Bake
```

着手順は 1 → 2 → 3 → 4。ここまで実装済み。形の層（Macro / Meso）は揃い、6 は Shape Mask（オクルージョン / 上向き度 / 高さ）から着手した。残りは 6 の曲率・切断面、Micro（7）、仕上げの 5。
