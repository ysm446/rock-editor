# Remesh

作成日時: 2026-09-22 22:10
更新日時: 2026-09-22 23:50

## 位置付け

Mesh の三角形を、一様な大きさの正三角形に近い形へ作り直すノード（等方リメッシュ）。Volume to Mesh の三角形は不揃いで（Marching Tetrahedra は細長い面が多く、Dual Contouring も大きさがばらつく）、Subdivide で細分化しても不揃いのまま密になる。2026-09-22 のユーザー依頼「Subdivide の替わりに Remesh のようなノード」への対応。用途は Displace のために密度を稼ぐことなので、**UV Unwrap の後に置ける**ように UV を引き継ぐ（同日の追加依頼「UV Unwrap の前に置くと展開が重くなる」）。サンプルは `examples/remesh/`。

```text
Volume to Mesh → Decimate → UV Unwrap → Remesh → Apply Material → Displace → Mesh Output
```

UV Unwrap の前に置くこともできる（UV なしのリメッシュ。稜線のまわりも自由に動ける）。Decimate は形の細部を残しつつ減らすのに対し、Remesh は辺の長さより細かい凹凸を均す。右クリックメニューの「メッシュ」にある。入力・出力とも Mesh 型。

## 設定

| 項目 | 内容 |
| --- | --- |
| 辺の長さ | 目標の辺の長さ。形の最長辺に対する比。0.001〜0.2、既定 0.02（対数スライダー） |
| 繰り返し | 分割 → 縮約 → 反転 → 平滑化を繰り返す回数。1〜20、既定 5 |
| 特徴辺の角度 | これより大きく折れた辺を稜線として保つ。0〜180 度、既定 40。180 で稜線を保たず、角も丸くなる |

三角形数は概ね `表面積 ÷ (√3/4 × 辺の長さ²)`。400万面を超える設定は診断する。保存名は `remesh`。保存・読込と Undo / Redo に対応。

## 処理

Botsch & Kobbelt の増分リメッシュ。目標の辺の長さを L とする。

0. **前処理**: L × 2 より長い辺を持つ面を、Subdivide と同じ 1→4 分割（隣は共有辺に合わせて 2〜4 分割）で先に細かくする（最大16回）。辺を割るたびに中点から向かいの頂点へ辺を張る増分の分割だけでは、大きな三角形が扇状の細長い面に割れて面数が爆発する（実装時に 12 面の直方体で当時の上限 100 万面を超えた）。1→4 分割なら辺が毎回半分になり、形も保たれる。
1. **特徴辺の決定**: 折れ角が「特徴辺の角度」より大きく、両側の面の形が悪くない（面積の2倍 ÷ 最長辺² ≥ 0.1）辺を候補にする。候補をつないだ鎖ごとに長さを合計し、L × 3 未満の鎖は格子の段差とみなして捨てる。残った鎖が稜線になる。特徴辺は最初に決めて、分割・縮約で引き継ぐ（分割で中点も特徴に、縮約で消える頂点の特徴辺は残す頂点へ付け替える）。毎回の折れ角で判定すると、格子から作ったメッシュでは稜線でない所まで特徴になり、頂点が動けずに細長い面が残る（実装時に Marching Tetrahedra の直方体で形の悪い面が 4% 残った。静的にして 0.4%）。
2. 各繰り返しで:
   - **分割**: L × 4/3 より長い辺を中点で割る（辺の一覧を1回だけ走査）。
   - **縮約**: L × 4/5 より短い辺をまとめる。閉じた多様体のまま（リンク条件）、面が裏返らず、新しい辺が L × 4/3 を超えず、形の悪い面を新しく作らない（もとから形の悪い場所では悪化しない限り許す）ときだけ行う。特徴辺は両端が角（特徴辺が3本以上）でなければ中点へ、片方が角ならそこへまとめる。特徴でない辺は、片方が稜線上ならそこへまとめ、両方が稜線上なら稜線どうしを混ぜないので行わない。
   - **反転**: 頂点の次数が 6 に近づくとき、または細長い面（形の良さ 0.2 未満）が2倍以上良くなるときに辺を反転する。特徴辺は反転しない。反転先の頂点どうしが既につながっている場合と、面が裏返る場合は行わない。
   - **平滑化と投影**: 各頂点を隣接頂点の重心へ、頂点法線に垂直な成分だけ動かし（接線方向の平滑化）、元の表面の最近点へ投影する。稜線上の頂点（特徴辺2本）は稜線に沿ってだけ動かし、角（3本以上）と稜線の端（1本）は動かさない。動かして面が裏返る頂点は元に戻す。
3. 生きている頂点と面を詰めて出力し、閉じていることを確かめる。

### UV の引き継ぎ（UV付きの入力）

Decimate と同じ方針で、UV の島の境界（継ぎ目）を固定し、内部の UV を補間して引き継ぐ。

- **継ぎ目**: 辺を挟む2面で島が違うか、共有する頂点のコーナー UV が違う辺。特徴辺と同じ集合に入れる（反転しない）。継ぎ目の頂点は縮約せず、平滑化でも動かさない。分割はできる（中点の UV は面ごとに補間するので、両側の島で別々に補間される）。
- **内部の頂点**: 縮約では辺上の比率で UV を補間する。平滑化では、投影で当たった元の三角形が同じ島なら重心座標で UV を読む。別の島（継ぎ目の向こう）なら動かさない。
- **裏返りの検査**: 縮約・反転・平滑化のいずれでも、UV 上で面が裏返る（面積の符号が変わる）か潰れる操作は行わない。
- 出力の UV のアトラス寸法は入力と同じなので、Shape Mask の画像や Material Bake の結果もそのまま使える。
- 継ぎ目のまわりは頂点が動けないので、三角形の揃いが悪くなる（UV 展開した立方体では、辺の長さが目標の ±50% に入る割合が 7 割程度。UV なしの球では ±40% に 9 割以上）。

最近点の検索は、入力の三角形を一様格子（セルは L × 2 と最長辺 ÷ 128 の大きいほう）に登録し、近いセルから順に探す。

## 診断

| 状況 | 表示 |
| --- | --- |
| 入力が未接続 | Mesh出力を接続してください |
| 辺の長さ・繰り返し・角度が範囲外 | それぞれの範囲を示す |
| UV が不正な入力（コーナー UV の数が面数と合わない、など） | 入力のUVが不正です |
| 閉じていない入力、多様体でない入力 | 入力は閉じた向き付きのメッシュにしてください |
| 400万面を超える設定 | 出力が400万面を超えます。辺の長さを大きくしてください |

## 制限

- UV 付きの入力では、継ぎ目の頂点が動かないので継ぎ目のまわりの揃いが悪い。島が細かいメッシュでは効果が薄れる。
- 辺の長さより細かい凹凸は均される。細部を残したまま面数を減らすのは Decimate の役割。
- 特徴辺は L × 3 以上の鎖だけを保つ。短い稜線や、格子の段差程度の折れは丸くなる。
- 頂点数の異なる複数のメッシュ（Merge の後）にはメッシュごとに掛かる。
- マスクによる場所ごとの辺の長さ（適応リメッシュ）は無い。

## 検証

`tests/RemeshTests.cpp`（44項目）。半径 1 m の球（12,288 面）を辺 0.1 m で作り直し、閉じた1つの塊、三角形数が見込みどおり（2,000〜4,000）、V = F/2 + 2、体積と半径の誤差（1 cm 未満）、辺の長さの9割以上が ±40% 以内、形の悪い面が 2% 未満、次数 5〜7 が 8 割以上、UV なし、再現性、辺の長さを半分にすると面数が約4倍、粗い設定、繰り返しの効果。直方体（2×1×1）で閉じたまま体積と外接箱を保ち、頂点が元の面上にあり、辺が揃うこと、特徴なしで角が丸くなり体積が減ること。Marching Tetrahedra の直方体（形の悪い面が 1 割以上）を揃えて形の悪い面が 2% 未満になり体積が保たれること。UV 展開した立方体で、閉じたまま細かくなること、UV と島とアトラスの寸法を引き継ぐこと、体積を保つこと、島ごとの UV の面積を保ち UV が裏返った面が無いこと、継ぎ目の頂点が残ること、継ぎ目以外の辺が揃うこと。不正設定4種、不正な UV・閉じていない・400万面超の診断、取消、進捗。グラフでは型と既定、未接続の診断、評価結果の由来、キャッシュの再利用と設定変更、Remesh の後の UV Unwrap、UV Unwrap の後に置いても UV 付きの細かいメッシュを得ること。
