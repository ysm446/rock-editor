# goals — 目的と完成形

作成日時: 2026-09-20 20:05
更新日時: 2026-09-21 21:20

## 目的

Windows 11 / x64 向けのプロシージャル岩生成エディタを作る。母岩に節理を定義し、亀裂が進む範囲、まだつながる部分、完全に分離した岩塊を編集して、ゲーム等で使える岩アセットを生成する。原文は [v2 仕様](../rock_generator_spec_v2.md)。

## 完成時の流れ

```text
母岩 → 節理 → 亀裂 → 部分破断（Rock Bridge が残る）
     → 必要に応じた完全破断 → 岩塊の選択・抽出・移動
     → 二次破砕 → 欠け・溝・浸食 → メッシュ整理
     → Triplanar / UV → 表面ディテール → 書き出し
```

`Crack` は有限亀裂と未破断部、`Fracture` は完全分離の判定と Chunk 化を扱う。完全に分離してもその場に留まる Locked 状態を持つ。MVP では物理計算せずフラグで保持する。

2026-09-21 のユーザー判断により、Joint Set / Crack / Fracture ノードと、その切断・分割・Chunk 操作の実装を削除した。当面はボリューム表現の研究を優先し、節理・亀裂・破断は必要になった時点でボリューム上の操作として設計し直す。以下の流れと価値は元の構想として残す。旧実装は Git 履歴（`603bde6` 以前）で参照できる。

## 外観を優先する判断基準

2026-09-21 の追加依頼により、BoxのVoronoi分割からピースを削除・抽出する工程を新規設計する。今回の切断はメッシュ方式を採用し、旧破断ノードは復活させない。Base Rockの表示名は用途に合わせてBase Shapeへ変更する。初版はBoxを基準とする凸立体、一般のSDF由来メッシュへの対応は後続とする。[新しい分割・選別の設計](../reference/voronoi-pieces.md) を当面の方針とし、下記の旧MVP順序よりこの追加依頼を優先する。Voronoiを二次破砕だけに限定する旧記述も今回の試作には適用しない。

2026-09-21 のユーザー指示により、目的は「もっともらしい岩を作ること」と再確認した。亀裂や溝の機能を増やすこと自体を進捗の目的にしない。閉包・体積・テストの合格は必要な品質条件であり、岩らしい外観の達成とは別である。

まずユーザー指定の「ランダムに重ねた直方体の塊 → ボリューム変換」から始め、独立した Volume to Mesh ノードでメッシュへ戻す。複雑な岩を一度に狙わず、単純な塊を見ながら形を調整する。参考写真の非対称な主塊、広い面、欠けは、その後の外観目標とする。正面・斜め・側面から実アプリで比較し、基本図形に加工を加えただけに見える段階では外観目標を達成したとしない。

## 重視する価値

- 主破砕は方向性を持つ Joint Set に基づく。Voronoi は二次破砕に使う。
- 深さ、persistence、Rock Bridge を目視し、途中結果を確かめながら編集できる。
- 非破壊ノードグラフと seed により、上流を編集でき、保存・再読み込みで結果を再現できる。
- Macro は実ジオメトリ、Meso はメッシュや将来の SDF、Micro は主に Height / Normal / Roughness / AO で作る。
- ジオメトリ・描画・UI・評価を分離し、CPU とメッシュによる検証を先に進める。

## 到達点

最初のプロトタイプは Box に有限亀裂を作り、未破断部を残し、明示的な完全分割後に2つの Chunk を個別選択・移動・回転できること。seed と設定を保存して同じ結果を再現する。

MVP は4種類の母岩、複数 Joint Set、岩塊の抽出・編集、Chip、Triplanar による表面表示、OBJ 書き出しまで。順序と境界は [実装計画](plan.md)、合否は [検証計画](../reference/validation.md) に置く。

## 対象外と将来

FEM / DEM、原子・地質シミュレーション、リアルタイム物理破壊、水・雪のシミュレーション、GPU voxel solver は MVP 対象外。
ボリュームは直方体の和集合を CPU の密なスカラーグリッドにする範囲を今回の対象へ前倒しした。汎用メッシュのボリューム化・Sparse SDF、Secondary Voronoi、再帰破砕、物理的な Locked 判定、自動 UV、LOD、glTF / FBX 書き出し、地形統合は後続段階。

C++20 / DirectX 12 / Dear ImGui / CMake の既存基盤を利用する。ノード編集、PBR 素材、モデル配置、アセット管理、保存、Undo は引き継ぐ。現在の状態は [進捗](progress.md) を参照する。

## 外観の参考

ユーザー提示の `DSC02801.JPG` を参考に、大きな角張った岩塊、方向性のある深い割れ目、欠け、灰色と茶色の色むらを目指す。[外観目標](../reference/visual-target.md) に機能との対応と不足を記録する。写真と同等の外観は、MVP の機能完了だけで達成とせず、形状と材質を合わせて別途評価する。
