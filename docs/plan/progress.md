# progress — 進捗と注意点

作成日時: 2026-09-20 20:05
更新日時: 2026-09-22 01:58

## 現在地

### 2026-09-22 Volume Crack

- [設計メモ](../reference/rock-shaping-nodes.md) の3番目として **Volume Crack** を追加。入力は Volume と Points（Scatter Points の出力）。点が作る Voronoi の境界面までの厳密な距離（垂直二等分面までの距離の最小）で溝を彫る。断面は表面で最も広く、指定の深さで閉じる V 字。割れ目ごとの幅のばらつき（点の対から決める固定の倍率。一部は閉じる）と、割れ目に沿ったゆらぎ（3D の値ノイズ。途切れる）を持つ。幅と深さは形の最長辺に対する比。格子は入力のまま。保存名 `volumeCrack`、右クリックメニュー、設定欄、保存/読込、Undo/Redo（既存スナップショット）、ボリューム枝のキャッシュ（Volume 側と Points 側の両方の上流をキーに含む）に接続。仕様は [Volume Crack](../reference/volume-crack.md)、サンプルは `examples/volume-crack/`。
- Plane Cuts で分かった「重なった立体のボリュームは内部に残る面の近くでも距離が小さい」問題は、ここでは内部の空洞として現れる。彫ったあとで格子の外周から外部をたどり、届かない彫り跡を入力の値へ戻す。入力にもとからある空洞は残す。浮いた塊は除かない（深さ 1 で形を分けるのは意図した使い方）。
- Scatter Points は凸な Mesh を必要とするので、凸でない形に割れ目を入れるときは、形を囲む Base Shape の Box に点を散らす。点は形の外にあってもよい。この使い方をサンプルと仕様に書いた。
- 単体テスト42項目を追加（`tests/VolumeCrackTests.cpp`）。溝の位置と深さ、V 字、対称、格子の不変、離れた点の完全一致、深さ 1 での分割、幅 0 の恒等、再現性、ばらつき・ゆらぎ、形の外の点、内部の空洞を作らないこと、不正な設定と点の拒否、グラフの型制約・診断・キャッシュを確認。Release/Debug ビルドと全2,576項目が成功。
- Release 実アプリでサンプル（面取り → 割れ目 → 稜線の欠け）を開き、割れ目が複数の面をまたいで連続し、太さに差があり、途中で消えるものがあることを確認。設定欄と、`--save-project` による保存で設定と8本の接続が残ることを確認。検証は `build/statusbar/rock_editor.exe`。実マウスでのメニュー操作と Undo/Redo の手動確認は未実施。
- 制限：セル間隔より細い割れ目は格子で潰れる（幅 0.03 は解像度96で約3セル）。割れ目は平面の組み合わせで直線的。方向性を付ける設定は無い。512点・各軸192点では重い。

### 2026-09-22 Plane Cuts

- [設計メモ](../reference/rock-shaping-nodes.md) の2番目として **Plane Cuts** を追加。平面の群で Volume を切り落とす。枚数・Seed・適用範囲（全体 / 局所）・半径・深さの最小と最大・法線の分布（等方 / 主方向1〜3系統、向き、ばらつき）・なめらかさを持つ。長さの設定は形に対する比で持ち、格子は入力のまま。保存名 `planeCuts`、右クリックメニュー、設定欄、保存/読込、Undo/Redo（既存スナップショット）、ボリューム枝のキャッシュに接続。仕様は [Plane Cuts](../reference/plane-cuts.md)、サンプルは `examples/plane-cuts/`。
- **実画面を見て設計を直した。** (1) 全体に効く半空間だけでは、枚数を増やすと凸包へ近づき、直方体の塊の凹凸が消えた。表面の点まわりの球の中だけを切る「局所」を追加した。(2) 局所の中心が形の内側に選ばれ、空洞と破片ができた。重なった立体のボリュームは内部に残る面の近くでも距離が小さいためで、表面の点を「隣に外部の点を持つ内部点」で選ぶよう直した。(3) 球の壁が丸いくぼみとして残った。切断面を斜めに持ち上げる案は上方の形まで削るため取りやめ、中心を凸な点に限り、法線を表面の外向きへ寄せ、切り取る材料が球の縁に掛かる割合で欠けを選別する方式にした（稜線と角は通し、平らな面のくぼみは除く）。浮いた小片は最大の塊だけを残して除く。
- 単体テスト58項目を追加（`tests/PlaneCutsTests.cpp`）。球冠の体積、格子の不変、形を広げないこと、影響のない点の完全一致、再現性、枚数追加時の既存平面の不変、等方の偏りのなさ、主方向の円錐と系統、不正設定の拒否、局所の中心・法線・深さ、十字形での全体と局所の差、浮いた小片の除去、グラフの型制約とキャッシュを確認。Release/Debug ビルドと全2,534項目が成功。
- Release 実アプリでサンプルを開き、切り落とす前・全体5枚・局所256枚指定の3段の形、設定欄、`--save-project` による保存で2ノードの全設定と5本の接続が残ることを確認。検証は `build/statusbar/rock_editor.exe`。実マウスでのメニュー操作と Undo/Redo の手動確認は未実施。
- 制限：局所で実際に使われた枚数は表示していない。局所の欠けは稜線と角に集まり、平らな面の中央は欠けない（Volume Displace / Noise の役割）。距離値は符号が正しい近似。

### 2026-09-22 設計メモと Volume Boolean

- ユーザー提示の参考画像（`docs/references/thumbnail_bea6868d-…​.webp`、Git 対象外）のような岩を作るための処理を整理し、[岩らしい形と色のためのノード設計メモ](../reference/rock-shaping-nodes.md) に残した。候補は Volume Boolean、Plane Cuts、Volume Crack、Volume Displace / Noise、Volume Smooth、形状からのマスク、ディテールの焼き込み。概念として Field（スカラー場）型を挙げた。着手順は Boolean → Plane Cuts → Crack。
- その1番目として **Volume Boolean** を追加。A / B の Volume 入力から和・交差・差を取り、なめらかさ（0〜10 m）でつなぎ目を丸める。結果は A の格子を引き継ぎ、A の値は補間せずに使う。和は B を含む範囲まで格子を延ばし、交差は重なる範囲へ狭める。Z スライス単位の並列処理とボリューム枝のキャッシュに対応。保存名 `volumeBoolean`、右クリックメニュー、設定欄、保存/読込、Undo/Redo（既存スナップショット）に接続。仕様と制限は [Volume Boolean](../reference/volume-boolean.md)、サンプルは `examples/volume-boolean/`。
- 単体テスト36項目を追加（`tests/VolumeBooleanTests.cpp`）。体積 8.5 / 0.5 / 7.5 m³、格子の広がり方、A の値の完全一致、再現性、なめらかさ、離れた B、上限・不正設定の診断、グラフの型制約・未接続の診断・キャッシュを確認。Release/Debug ビルドと全2,476項目が成功。
- Release 実アプリでサンプルを開き、差・なめらかな和・交差の3つの形、ノード選択時の設定欄と説明文を確認。`--save-project` による保存で `volumeBoolean` の設定と7本の接続が残ることを確認。通常の実行ファイルは起動中だったため、検証は `build/statusbar/rock_editor.exe`。実マウスでのメニュー操作と Undo/Redo の手動確認は未実施。
- 制限：結果の距離値は符号が正しい近似で、後続のオフセット・平滑化には距離の再計算が要る。細部は A のセル間隔まで。なめらかさ 0 のつなぎ目は Dual Contouring でもセル程度にギザつくことがある。

### 2026-09-22 更新中の表示をステータスバーへ移動

- ユーザー指定により、非同期評価中の「更新中…前回の結果を表示しています」をビューポート左上からステータスバー左端へ移した。「マテリアルを評価中…」と同じ並びで、評価が終われば消える。ピース設定欄の案内文は残す。
- Releaseビルド成功。`examples/material-layers` の起動直後のフレームでステータスバーに表示され、ビューポート上に出ないことを実画面で確認。起動中の通常Releaseはロックされていたため、検証用は `build/statusbar/rock_editor.exe`。通常の実行ファイルへの反映はアプリ終了後の再ビルドが必要。

### 2026-09-22 全体レビューによる修正と最適化

- **形状コアの高速化（出力はビット単位で不変）**：To Volumeの一般メッシュ経路を(Y,Z)の行単位で並列化。BVHは子の順序付けに使った箱距離を再利用し、隣の格子点の最近距離から探索の上限を決める。Random Boxesのボリューム化とVolume TransformはZスライス単位、Voronoi Fractureは片単位で並列化した。診断・三角形数の上限・体積の合計はID順にまとめ、直列処理と同じ結果・同じエラー文を返す。`InspectMesh` の辺照合は要素ごとに確保しない開番地法の表へ変更。Marching Tetrahedraの交点表は事前確保する。
- 旧コードと新コードを同じ入力で比べる検証用プログラムで、To Volume（楕円体＋ノイズ12,288三角形・解像度96、Dual Contouringの53,892三角形の再ボリューム化、原点から500 m離れた入力）、Random Boxes 32個・解像度96、Volume Transform、Marching Tetrahedra / Dual Contouring、Voronoi 24/232/512点×2 Seed（方向付き伸長を含む）の全出力のハッシュが一致。Release実測は To Volume 5,774→187 ms、再ボリューム化 10,472→359 ms、Random Boxes 90→9 ms、Volume Transform 64→9 ms、Voronoi 512点 281→19 ms。単体テストのVolumeドラッグ相当は19.0→9.8 ms/更新、Releaseの全テストは13.5→約4.5秒。
- **キャッシュ**：Apply Materialを通るとボリューム枝のキーが作れず、下流のTo Volumeを毎回作り直していた。材質・マスクは接続だけをキーへ含め、形状のキーを下流へ通す。
- **Scatter → Voronoi**：Scatterは倍精度の点で、Voronoiはfloatへ丸めた保存値で内外を調べていた。Scatterでも保存値をVoronoiと同じ許容差で確かめる。従来の判定は残したので、既存Seedの結果は変わらない（上の一致検証に含む）。
- **Volume Transform**：移動量に対してセルが小さく、floatの格子座標が隣どうしで潰れる設定を診断する（To Volumeと同じ考え方）。
- **アプリ**：評価タスクの例外を生成エラーとして表示。終了時にタスクを止めて待つ。選択ノードだけが変わったときは実行中の評価を止めず、完了後のキャッシュで新しい選択を評価する。ベイク状態の `HintText` へ書式文字列として渡していた不具合を修正。テクスチャ削除で不透明度の参照を外し、使用箇所の一覧・件数へ不透明度とMaterial Maskを追加。文書の切り替えでグラフのクリップボードを捨てる。AO結果とベイク画像の寸法を照合。設定ファイルは一時ファイル経由で保存。`projectVersion` のないシーンを版4として拒否していた既定値を1へ修正。
- **描画**：Apply Material使用時に捨てられていた基本材質の読み取り（Triplanarなら9回の異方性サンプル）を省略。マスク抜きの `discard` がある場合は従来どおり読む。`examples/material-layers` の実画面を変更前後のシェーダで撮り、ビューポートが画素単位で一致。生成メッシュの頂点配列の事前確保、UV面積0の面のNaN接線の回避、`UploadMeshScene` のシーン全体の複製1回分を削減。StructuredBufferをCOMMONで作成し、DebugのD3D12検証の警告を解消。
- **ビルド**：`/MP` を追加。`rock_sources` の `RockMesh.cpp` の二重登録を削除。
- Release/Debugビルドが警告の追加なしで成功し、単体テスト2,440項目が両構成で成功（Apply Material下流のキャッシュ4件、遠方のScatter→Voronoi 1件を追加）。`--test-gpu-ao` の実GPU比較も成功。Release実アプリで `examples/material-layers` と `examples/voronoi-pieces` の読み込み・表示を確認。実マウスでの操作確認、Debug実画面でのD3D12警告の消滅確認は未実施。
- レビューで見つけたが今回は直していない項目は「未完了」の「レビューの持ち越し」に記録。

### 形状AOのGPU化・進捗表示

- 形状AOのレイ判定をDX12 Computeへ移行。空間/UV BVHはCPUで構築し、GPUは128×64画素のタイルをフレーム間に分割して処理する。完了タイル数による進捗表示とキャンセル、入力変更時の破棄、適用前の指紋照合を追加。専用DXR機能は不要。
- 実GPUとCPU参照実装の比較で、平面・遮蔽・距離・強度・8/32/128サンプルの最大差は8bit値で1。タイル端・進捗単調増加・部分実行の破棄/再利用を開発用 `--test-gpu-ao` で検証。512のSDFメッシュのAOベイク画像は従来CPU版と画素単位で一致。Release実アプリで2048/128サンプルの完了・進捗画面、キャンセル、保存再読込を確認。Debugの2048実画面検証は長時間化したため中止し、実GPU比較テストとビルド・CTestで検証した。

- Release/DebugビルドとCTest成功（約15秒/131秒）。実GPU参照比較も両構成で成功。DebugのD3D12検証でエラーなし（共通StructuredBuffer生成によるCOPY_DEST初期状態の警告は残る）。

### Apply Material・マスク合成・形状AO

- Apply Material（Mesh / Material / Mask → Mesh）とMaterial Mask（定数・画像R、反転、UV/Triplanar）を追加。マスク省略は全面置換、接続時は最大8段の素材合成。Surfaceごとの投影を保持し、UV UnwrapとMaterial Bakeへ引き継ぐ。旧Output/BakeのMaterial入力は全面置換として互換維持。
- Material Bakeに距離・強度・サンプル数を持つ形状AOを追加。UV画素ごとのBVHレイ判定で素材AOへ乗算し、BaseColorは保持する。マスク画像・各段の投影・AO設定をベイク指紋へ含める。保存・再読込・Undoに対応。
- Release/Debugビルドと全CTest成功（約13秒/107秒）。マスク型、全面置換、素材順序、UV/Bake継承、8段上限、無効Surface、Undo/Redo、AOの遮蔽・距離・強度・再現性を検証。実アプリで黒/白/50%と画像マスクのプレビュー・ベイクを確認。黒/白/50%のBaseColor中央値はそれぞれ[202,38,38]/[38,38,202]/[150,38,150]。AOは素材の上限値を維持し、遮蔽で暗くなることを確認。保存再読込でマスク・AO・指紋と接続を維持し、AO距離変更で再ベイク待ちになることを画面確認。配布サンプルを別ルートにコピーして読み込み・ベイク成功。
- 詳細と制限は[素材の適用と形状AO](../reference/material-application.md)。サンプルは `examples/material-layers/`。

### 2026-09-21 Dual Contouringの共有面接続修正

- sample.rockscene相当（Box 2×3×2、232点・Seed 1、内部79片、解像度64/96）で、共有面の2本の輪郭が同一の辺になり4面が接続する問題を再現。輪郭ごとの面上中間点を共有して別の接続を保つよう修正した。
- 別方式へ切り替えず、Dual ContouringのQEFを維持。再現条件とセルの全255非空符号配置を回帰テストへ追加。Release/Debugのビルドと全CTest（約13秒/111秒）、実際のdata/Scenes/sample.rocksceneを設定変更せずに開いたSDFプレビュー表示を確認。通常のRelease実行ファイルへScatter上限拡張も含めて反映済み。

### 2026-09-21 Scatter Points上限拡張

- 点数の上限を128から512へ拡張。UIとScatter/Voronoiの検証に共通定数を使用し、既定24点と既存Seedの生成規則は維持する。点数を増やすほど分割処理の負荷は増える。
- 最大点数での分割・各片の閉包・体積保存と、上限超過の拒否を回帰テストへ追加。

- Releaseの全CTest成功。起動中アプリによる通常exeのロックのため、アプリ本体は `build/scatter-preview/rock_editor.exe` へ別出力してビルド成功。通常のexeへの反映はアプリ終了後に再ビルドが必要。

### 2026-09-21 Mesh入力のTo Volume

- Random BoxesをMesh出力、To VolumeをMesh入力へ統一。Base ShapeやPieces to Meshから閉じたメッシュをボリューム化できる。直方体の高速経路を維持し、一般メッシュのBVH距離計算、凹形状・穴・空洞・重複・分離ピース、非同期評価と上流キャッシュ無効化に対応。旧シーンの接続は維持する。
- 開いたメッシュ、逆向き外殻、無効設定を診断。自己交差修復は対象外。重複面を含む内部距離は近似となる。一般メッシュは25万三角形・100万頂点まで。
- 解析Boxとの全格子点比較、凹形状、トーラス、内殻、重複・接触・分離した立体、UV接続、Voronoiセルの再ボリューム化とキャッシュの回帰テストを追加。

- Release/Debugビルドと全CTestが成功。実アプリでBase ShapeとPieces to Meshからの変換表示を確認。旧Random Boxesシーンを読み込み・再保存し、接続IDが維持されることも確認した。

### 2026-09-21 Voronoi分割とピース操作（V1〜V4初版）

- [設計書](../reference/voronoi-pieces.md)を追加。Scatter Points / Voronoi Fracture / Piece Select / Piece Filter / Piece Transform / Pieces to Meshの6ノード、ピースID・選択の有効性、方向性のある分割、Box面を指定した外周除去、保存/Undo、キャッシュと非同期評価を定義した。
- 初版はBoxを基準とする凸立体で、分割→外側除去→一部抽出→個別操作→UV/ベイクを完成させる。凹形状や空洞を含む一般メッシュの対応は次段階。6ノードを実装済み。サンプルは `examples/voronoi-pieces/`。
- Base Rockの表示名をBase Shapeへ変更。ノードタイトル、追加メニュー、エラー表示を更新し、保存名 `baseRock` と設定は維持した。Releaseビルド成功。旧保存名のシーンを読み、画面にBase Shapeが表示され、再保存でも `baseRock` が維持されることを確認。

- `geometry/Pieces` は正規化した倍精度座標の凸クリップを使用。同一辺の交点共有と境界閉路によるキャップ生成を行い、floatメッシュの閉包・正体積・体積保存を確認してから公開する。2〜128点、伸長比16以下、入力2万/出力25万三角形まで。
- Points / Pieces / Selectionを独立型として追加。Piece Selectは手動ID、Box外面、重心範囲、体積、ランダムに対応。Keep/Deleteを接続済みで追加できる。Piece Transformは全体/各片の重心、IDごとの変換、W/E/Rギズモを持つ。
- 分割ノードID・生成世代・点IDで片を識別。上流変更で手動選択/個別変換が古くなればリセットを案内する。Selectionは枝と配置にも結び付け、別の入力への誤適用を防ぐ。JSONの64bit世代は文字列で保存。コピーした分割ノードへの内部参照も更新する。
- Scatter/Voronoiを入力内容でキャッシュし、Filterは参照、Transformは行列を更新。CPU評価はグラフのスナップショットを非同期処理し、取消と世代照合で古い結果を棄却する。GPUへの転送はメインスレッド。更新待ちは前回表示と明記し、選択とベイクを止める。
- Release画面で元Box、32分割、外面の除去、一部抽出、UVチェッカー、PNG4枚のベイク、保存再読込を確認。CLI入力注入で移動ギズモとEsc取消、手動クリック選択、Shiftの追加/解除、空白の解除を検証。検証用実行ファイルと画面は `build/uv-bake/`、`build/uv-bake/validation/voronoi-*.png`。
- 自動試験で複数Seed、方向付き伸長、最大128片、薄いBox、最小/最大寸法、辺/頂点上の切断、断面の面積と向き、最近点条件による所属・非重複、凹形状/重複点の拒否、選択の世代不一致、キャッシュ再利用、UV接続、個別XYZ変換、JSON往復を確認。

- 最終コードでDebug/Releaseビルドが警告なしで成功。CTestは両構成とも全件成功（Debug約40秒、Release約8秒）。通常の `build/bin/Release/rock_editor.exe` にも反映済み。サンプル一式を独立した検証ルートへコピーして、読み込みとベイクを確認した。

**初版の制約**：凹形状・空洞・自己交差の修復は未対応。Outerの6方向は軸に沿ったBox面を対象とする。Pieces to Meshは接触面を残す単純結合。ギズモの倍率は均等、XYZ別倍率は数値欄。実マウスによる全機能の通し操作は未検証。

### 2026-09-21 Joint Set / Crack / Fracture ノードを削除

- ボリューム研究の方針に合わせ、ユーザー判断で3ノードを削除。グラフ定義・評価器・保存/読込・プロパティ UI・右クリックメニュー、`src/crack/`・`src/fracture/`・亀裂/Bridge ガイド描画、Fracture の Chunk 選択・色分け・ギズモ、関連テスト6本を取り除いた。
- 列挙値 35～37 は再利用しない。旧シーンのこれらのノードは未知の種類として捨て、つながっていたリンクも読み込み時に除く。Base Rock は残す。
- Debug ビルド（アプリ・テスト）が警告なしで成功し、単体テストはすべて成功。実画面での確認はまだ行っていない。

### 2026-09-21 UV解像度を2のべき乗へ統一

- UV Unwrapの解像度を128 / 256 / 512 / 1024 / 2048 / 4096の選択式に変更。既定値は1024。
- 旧シーンの任意解像度は次の2のべき乗へ切り上げて読み込む。生成処理でも非対応値を拒否し、解像度変更後は既存の指紋比較で再ベイクを案内する。
- Releaseビルドと単体テストが成功。実画面の選択欄と、旧シーンの600が1024へ変換されて保存されることを確認。

### 2026-09-21 英数字付きUVチェッカーと通常の照明

- 参考画像に合わせた16×16マスの2048×2048 PNGを `assets/textures/uv_checker.png` に追加。続く指定でHoudini形式の配置へ変更し、明るいマスに行名A〜P、暗いマスに列番号1〜16を表示する。再生成スクリプトは `tools/generate_uv_checker.py`。CMakeで実行ファイル横へ同梱する。
- チェッカーを照明付きの拡散色として表示し、天球・影・露出・トーンマッピング・被写界深度の処理を共通化。シーンの材質やベイクへ混ぜず、チェック終了後は元の表示設定へ戻る。
- Debug/Releaseビルド、Python構文確認、Release実画面で文字と陰影を確認。同じカメラの通常表示と背景領域が画素単位で一致し、保存したグラフ・材質・プレビュー設定も一致。ISO 100→400でチェッカーにも露出が反映された。実行ファイルは `build/uv-bake/rock_editor.exe`。

### 2026-09-21 自動UV展開・UVビュー・材質ベイク

- 前段の生成メッシュ材質・Triplanar対応を `6b4ac2e` にコミット。その後、ユーザー指定の `UV Unwrap → Material Bake` を追加。xatlasで生成メッシュを1枚のUVへ展開し、選択時のチェッカーと、ビューポート横の「UVビュー」タブで確認できる。
- ベイクはSurface材質をUVへ手動転写し、色・DirectX法線・Roughness/Metallic/AO・Heightの4枚のPNGを保存して適用する。入力変更時は元材質へ戻し再ベイクを案内する。保存/読込・Undo/Redoへ接続した。
- SDF由来の微小面を展開できるよう座標を正規化し、面積のあるUV重なりを検査・修復する。両メッシュ変換方式のUV、形状不変、決定性、キャッシュ、材質優先順、Undo/Redo、余白のテストが成功。
- Debug/Releaseビルド、Release単体テスト、Debug CTest（1/1、48.34秒）が成功。実画面のベイクボタン、チェッカー選択/解除、UVタブ、512/1024出力、形状・投影・余白変更時の再ベイク待ちを確認。保存→再読み込みで指紋とビューポート画像が一致した。
- サンプルは `examples/uv-bake/`、仕様・制約は [UVとベイク](../reference/uv-bake.md)。実行ファイルは `build/uv-bake/rock_editor.exe`。起動中の通常Release版は置き換えていない。初版は同期処理・8bit PNGで、UVの手動編集・High/Low転写・メッシュ書き出しは含まない。

### 2026-09-21 生成メッシュへのSurface材質とTriplanar

- Mesh OutputにMaterial入力を追加。Surfaceの材質を生成メッシュへ渡し、UV／Triplanarを選べる。Triplanarはワールド座標基準で、サイズ・位置・回転・混合の鋭さを調整。Surface単位で保存・Undo/Redoする。
- Base Color／Normal／Roughness／Metallic／AOを接続。法線の投影方向を戻して勾配を合成する。Heightはチャンネル表示のみ。UV展開・ベイク・Height変位・ローカル座標追従は未対応。モデル材質は既存のモデル側設定を使う。
- Debug/Releaseビルド、Release単体テスト、Debug CTest（1/1、52.99秒）が成功。実画面で材質表示、背面の投影、UI切り替え、旧シーン互換、保存往復を確認。サンプルの別フォルダへのコピーも読み込み・表示できた。詳細は [仕様と検証](../reference/triplanar.md)。サンプルは `examples/triplanar/`。
- 実行ファイルは `build/triplanar/rock_editor.exe`。起動中のアプリは置き換えていない。

### 2026-09-21 SDFプレビュー方式の共通設定

- 「プレビュー設定」に共通の変換方式を追加。Volume の直接表示に適用し、明示的な Volume to Mesh の出力方式とは独立させた。アプリ設定に保存し、シーンや Undo 履歴は変更しない。
- 表示方式を外皮キャッシュの判定に含め、内部SDFは再利用する。To Volume / Volume Transform / Mesh Output への直接接続、方式の往復、明示的な変換結果への非干渉を回帰テストで確認。
- Debug/Releaseビルド、Release単体テスト、Debug CTest（1/1）が成功。隔離したアプリ設定でDual Contouring表示と、画面の既定値ボタンによるMarching Tetrahedraへの即時切り替え・設定保存を確認。表示三角形数は72,161から222,561へ変化した。
- 実行ファイルは `build/sdf-preview/rock_editor.exe`。検証画像は同フォルダの `validation/`。起動中の通常Releaseは置き換えていない。

**2026-09-21 のユーザー指定により、しばらくこのリポジトリを「岩からボリュームを研究する場所」として使う。** 見た目の完成を急がず、ボリューム（SDF の格子）上の操作を増やして試す。現状の表現は密な配列に持つ SDF で、OpenVDB は使っていない。

`Random Boxes → To Volume → Volume Transform → Volume to Mesh → Mesh Output` が通る。重なる直方体の塊を Seed で作り、実体のある3次元グリッドへ変換し、格子のまま動かして外皮を表示できる。

**参考写真のような外観は未達成。** これは形を作る出発点であり、亀裂や溝の機能数を外観の進捗とは扱わない。溝の拡張は保留。次の調整はこの塊の実画面を見て判断する。

| 区分 | 現在の状態 |
| --- | --- |
| アプリ基盤 | DX12 / ImGui、モデル表示、グラフ編集、素材・アセット・保存基盤あり |
| 岩生成 | Base Shape（Box / RoundedBox / Sphere / Ellipsoid、丸み、分割数、弱いノイズ、Seed）、Random Boxes、Scatter Points / Voronoi Fracture とピースの選別・個別変換に対応。Joint Set / Crack / Fracture は2026-09-21に削除済み |
| メッシュ接続 | RockEvaluator → RockMesh → SyncMeshGraph で表示。Merge と途中プレビューに対応。Volume to Mesh は Marching Tetrahedra / Dual Contouring を選択可能 |
| 評価・保存 | Revision ごとの再評価、寸法と seed の保存/復元、Undo/Redo に対応。ボリューム系の枝キャッシュに対応。他の枝のキャッシュは P6 |
| ボリューム | 密な配列の SDF。Volume Transform で移動・回転・拡大。格子の再サンプルで実装。ビューポートのギズモで操作できる。Volume Boolean で2つのボリュームの和・交差・差を取れる。Plane Cuts で平面の群による面取りと欠けを作れる。Volume Crack で点の群の境界に沿う割れ目を彫れる |
| 材質 | Apply Material / Material Mask、Triplanar、UV Unwrap、Material Bake（GPU形状AO）に対応 |
| 次の作業 | [設計メモ](../reference/rock-shaping-nodes.md) の順に、Volume Displace / Noise（直線的な割れ目と平らな面を崩す）、Volume Smooth、形状からの材質マスクが候補 |

## 完了

### 2026-09-21 Volume to Mesh に Dual Contouring を追加

- 「変換方式」で Marching Tetrahedra（既定・従来方式）と Dual Contouring を選択する。設定・保存・読込・Undo/Redoに接続。方式をキャッシュキーへ含め、切り替え時も上流SDFを再利用する。設定のない旧シーンは従来方式。
- Dual Contouring は入力SDFの格子から交点・法線を推定し、セル内に制約したQEFで頂点を求める。セル内で分離した表面には別頂点を配置し、曖昧な面の接続を両セルで揃える。最終的な閉包・向き・体積の検証は共通処理。詳細と制約は [メッシュ変換方式](../reference/volume-meshing.md)。
- Debug/Releaseビルド成功、Debug CTest（1/1、38.16秒）、Release単体テスト全項目成功。複数Seed・最大32箱/解像度96・内部空洞・斜めBox・球・球殻・決定性・不正入力・方式変更・キャッシュ・Undo/Redoを検証。
- Release実画面で両方式と設定欄を表示し、Dual Contouringの保存→再読込→再保存でグラフが一致。解像度48の同じ変換サンプルで描画統計の三角形数222,561→72,161。実マウスによるメニュー操作の通し確認は未実施。比較画像・保存ファイルは `build/dual-contouring/validation/`。
- 比較用シーンは `examples/volume-meshing/`。実行ファイルは `build/dual-contouring/rock_editor.exe`。起動中の通常Release版は置き換えていない。
- 格子で失われた細部や再サンプル誤差は残る。角が完全な直線になる保証はなく、セル程度の小さな穴・接続のトポロジーは方式間で変わる場合がある。適応格子・簡略化・一般的な自己交差検出は未対応。

### 2026-09-21 スムーズシェーディングとワイヤーフレームの削除

- 岩メッシュの表示が面法線だけだったので、「表示」メニューに**スムーズシェーディング（折れ角 40 度）**を足した。オフなら従来と同じ面法線。
- `MakeRockMeshData` に折れ角つきの頂点法線の平均を実装。頂点を使う面を CSR で持ち、その面と40度以内で向きが揃う面だけを面積で重み付けして平均する。直方体の90度の角は分かれ、面法線のまま残る。
- 平均した法線とは辺の向きが直交しなくなるため、接線をグラム・シュミットで張り直す。これを入れる前は `ValidateMeshScene` の直交条件で弾かれ、メッシュが表示されなかった。
- 表示の属性なのでシーンには保存せず、アプリ設定（`settings.json` の `display.smoothShading`）に持つ。法線は頂点に焼くので、切り替えると `SyncMeshGraph` がメッシュを作り直す。
- ワイヤーフレーム表示（分割前 / 分割後）を削除。道路エディタ由来で、テセレーションを使わない岩メッシュでは分割前後の区別に意味がない。メニュー・`DisplaySettings`・`PreviewRenderer` の分岐と `SceneMesh::showWireframe`・`PsWireframe` シェーダまで取り除いた。
- Debug ビルド、CTest（1/1、34.90秒）、単体テスト全項目が成功。`rock_editor_tests` に `src/renderer/RockMesh.cpp` を足し、法線の単位長・接線の直交・球で大半の法線が変わること・直方体の角が分かれること・描画へ渡せることを検査する。
- 実画面で、同じシーンのフラットとスムーズを描き比べて確認（`build/volume-transform-validation/shots/smooth-compare.png`）。格子由来の段差の陰影は消え、輪郭の段差は残る。
- 形は変えていないので、輪郭の階段は解像度・平滑化・抽出法の課題として残る。メニューの実マウス操作と Release の実行ファイルでの確認は未実施。

### 2026-09-21 Volume Transform の回転時の格子上限を修正

- Y軸回転で表示が消える報告を受けて調査。最大解像度96では余白追加だけでも旧上限102点を超える場合があった。保存済み `sample.rockscene` の解像度64・直方体11個の設定では、保存済みの回転にYを15度加えると107×98×104点となり、同じ上限に達する。報告時の未保存状態そのものは未取得。
- 内部格子の上限を各軸192点へ変更し、入力検証・変換・表面抽出で統一。解像度や距離値を粗くする回避は行わず、割り当て前の上限検査を維持。連続変換で上限を超えた場合は引き続き診断する。
- ノード設定欄だけでなくビューポート下部にも生成エラーを表示し、設定欄が閉じていても消えた理由が分かるようにした。
- Debug/Releaseビルド、Debug CTest（1/1、38.46秒）、Release単体テスト全項目が成功。解像度96の0/1/15/45/90/-45/180/360度・複合回転、保存済み設定とY15度追加、精度・閉包・体積・上限拒否を検証。
- 修正版Releaseは `build/volume-optimized/rock_editor.exe`。起動中の `build/bin/Release/rock_editor.exe` は旧版のまま。前回のキャッシュ最適化も修正版に含む。
- Release実画面で、保存済み設定のY回転を15度増やしたメッシュの表示と、未接続入力のエラーがビューポート下部に出ることを確認。元シーンは変更せず、検証コピーと画像を `build/rotation-fix-validation/` に保存した。

### 2026-09-21 Volume Transform の評価負荷を削減

- アプリが保持する `RockEvaluationCache` を追加。Random Boxes / To Volume / Volume Transform / Volume to Mesh の枝について、設定値と上流接続が同じなら結果を再利用する。Volume の直接プレビューも同じグリッドの外皮を再利用する。
- 上流の変更・接続変更・削除・Undoによる復元は、改版番号だけに頼らず内容で判定する。各ノードの最新結果だけを保持し、削除済み・変更済みの結果は破棄する。ボリューム以外の永続キャッシュは未対応。
- `InspectMesh` の辺照合を64bitの頂点IDペアとハッシュ表へ変更。閉包・向き・連結性・体積・不正値の検証条件を維持する。
- Debug ビルドと CTest（1/1、16.52秒）、別出力先の Release ビルドと単体テスト全項目が成功。キャッシュなしとの頂点・三角形の一致、上流SDFの再利用、回転・移動・解像度・Seed・接続変更・Undo相当の復元・上流削除・直接プレビューを確認。
- Release、解像度48、移動と回転の連続6更新でCPU評価を比較。最適化前相当23.76ms/更新、上流キャッシュのみ21.08ms、辺照合の改善後はキャッシュ込み15.38ms（約35%減）。短い実測の参考値で、UI・描画データへの変換・GPU転送を含まない。
- 起動中の通常Release版は置き換えていない。最適化版は `build/volume-optimized/rock_editor.exe`、検証ログは同フォルダの `tests-release.log`。ドラッグ中の低解像度化や遅延確定は導入しておらず、変更したボリュームの再サンプルとメッシュ生成は引き続き行う。
- 最適化版Releaseでサンプルの起動・描画を確認。画像は `build/volume-optimized/validation/preview.png`。実マウスによるドラッグの体感確認は未実施。

### 2026-09-21 Volume Transform のギズモ操作

- Volume Transform を選ぶと、Model / Transform / Chunk と同じギズモをビューポートに出す。W 移動 / E 回転 / R 倍率、Ctrl の刻み、ドラッグ中の Esc で取り消し。
- ギズモの中心は移動量の位置。ボリュームは倍率 → 回転 → 移動の順に原点まわりで動かすので、その点が回転・倍率の中心にもなる。
- 既存の `NodeTransform` / `NodeGizmoFrame` に分岐を足して実装。`NodeTransformRef` に「形を作り直す設定か」を示す `regenerate` を追加し、Chunk と同じくドラッグ中にグラフを改版して再評価する（Model / Transform は従来どおり改版しない）。
- 表示中の岩がこのノードを上流に持つときだけギズモを出す（`RockMeshUsesNode`）。ビューポートの Delete によるノード削除はモデルの系統のノードに限定した。
- Debug ビルド、CTest（1/1、20.53秒）、単体テスト全項目が成功。CLI 入力注入で移動・回転・倍率のドラッグを実行し、移動 X=1.76 / 回転 Z 方向の変化 / 倍率3.71 が設定へ入り、保存ファイルに残ることを確認。Esc 付きのドラッグでは設定が変わらないことも確認。
- ドラッグ中は毎フレーム上流から再評価するため、解像度を上げると重い。実マウスによる通し操作と Undo/Redo の手動確認、Release の実行ファイルでの確認は未実施。検証素材は `build/volume-transform-validation/shots/`。

### 2026-09-21 Volume Transform とメニューの型別整理

- Volume 入力を、倍率 → 回転 → 平行移動の順に原点まわりで動かして Volume を返す `Volume Transform` を追加。回転は右手系 Z → X → Y で Crack / Model と同じ規約。
- 格子を作り直す実装。出力の各点を逆変換して入力をトリリニア補間で読み、倍率を掛け戻して距離の単位を保つ。格子の外は外周の正値へ外に出た距離を足し、符号を外部のまま保つ。
- セル間隔を倍率に比例させ、拡大・縮小でセル数を変えない。範囲は入力格子の8隅の AABB + 2セル。回転で各軸102を超える場合は上流の解像度を下げるよう診断する。0倍率・非有限値・空の格子・内部が残らない場合も診断する。
- グラフの右クリックメニューを型別に再編。「メッシュ（ポリゴン）」「ボリューム」「モデル」「共通」「材質」の見出しで分け、変換ノードは変換後の型の側に置く（Volume to Mesh はメッシュ側、Random Boxes はボリュームの素材としてボリューム側）。
- 保存名 `volumeTransform`、プロパティ UI、保存/読込、Undo/Redo に接続。サンプルは `examples/volume-transform/`。
- Debug / Release ビルドと CTest（1/1、21.14秒）、単体テスト全項目が成功。識別変換・移動・45度回転・倍率2/0.5・連続適用の体積と外接箱、再現性、上限と不正設定の診断、型の接続制約、評価とプレビューへの伝播、Undo/Redo を確認。
- Debug の実行ファイルで、移動のみ・回転のみ・縮小のみのシーンを描画し、形を保ったまま位置・向き・大きさだけが変わることを目視確認。保存→読込→再保存でグラフが一致。検証素材は `build/volume-transform-validation/`。
- 再サンプルのため、回転では境界がセル1個ぶん程度なまる。軸ごとの倍率、平面を境に片側だけずらす操作、実マウスによる右クリックメニューの通し操作は未対応・未検証。**Release の `rock_editor.exe` は起動中のため再リンクしていない。**

### 2026-09-21 Volume to Mesh

- Volume 入力からゼロ等値面を取り出し、通常の Mesh 型を出力する独立ノードを追加。追加設定はなく、解像度は上流の To Volume で調整する。
- To Volume のグリッド生成とメッシュ抽出を分離。旧シーンの Volume 直接表示は最終プレビュー段階で外皮を抽出して維持する。
- 右クリックメニュー・プロパティ説明・保存名 `volumeToMesh`・Undo/Redo・4ノードのサンプルを接続。
- Debug / Release ビルド、CTest（1/1、19.49秒）が成功。Release で4ノードの表示・変換結果・説明欄を確認し、保存→再読込→再保存のグラフが一致。検証素材は `build/volume-to-mesh-validation/`。
- 通常の Release（`build/bin/Release/rock_editor.exe`）へ反映済み。その実行ファイルで4ノードのサンプルの表示・保存を確認。


### 2026-09-21 Random Boxes と To Volume

- 直方体集合を作る Random Boxes、内外の符号を持つ密な3次元グリッドを作る To Volume を追加。Boxes / Volume を独立したピン型にし、Mesh Output でそれぞれプレビューできる。
- Box の個数・寸法・サイズばらつき・配置の広がり・回転幅・Seed、ボリュームの解像度を編集。追加 Box は最初の Box と必ず重なる。グリッドは和集合を評価し、内部の重複面を出力しない。空洞の壁は体積の向きで外皮と区別して保持する。
- 設定の保存/読込と既存スナップショットによる Undo/Redo に接続。例は `examples/random-boxes/random-boxes.rockscene`、手順は [直方体とボリューム](../reference/box-volume.md)。
- Debug / Release ビルド、CTest（1/1、19.19秒）が成功。Seed の再現、Box の重なり、和集合の体積、閉包、最大個数/解像度、内部空洞、型の接続制約、Undo/Redo を確認。
- CLI 入力注入で設定欄を操作し、個数8→20、解像度48→65の独立した変更と再生成・保存を確認。実マウスによる手動通し操作と Debug 描画は未検証。
- Release で変換前後、別 Seed、回転0、解像度16/48、3視点を確認。保存→読込→再保存のグラフ一致を確認。素材は `build/box-volume-validation/`、実行ファイルは `build/box-volume-release/`。
- 境界は格子解像度による近似で、表示は面法線。細かな角張りが残る。極端に薄い寸法は解像度不足を診断する。任意 Mesh の変換、VDB、平滑化は未対応。岩らしい完成外観を得たという評価ではない。


### 2026-09-21 曲面の有限長部分溝（試作・保留）

- ユーザーの外観優先の指示により、この試作の追加拡張は保留。今回のコミットに試作として保存し、今後の優先作業にはしない。
- 前回の全幅部分切断を `a90f992` にコミット。今回は「Mesh 有限溝」へ拡張し、U 半幅で溝の長さを指定できるようにした。
- U 両端の最大2平面を追加し、長さの終端壁も生成。断面の平均点が全境界辺の内側なら扇形で三角形化し、該当しない断面は従来の ear clipping を使う。セル結合後の開いた辺に残る T 字接続を分割して閉じる。
- Debug / Release ビルドと CTest（1/1、10.08秒）が成功。4形状×軸方向/斜め×全幅/有限長、ノイズ付き3072三角形、決定性、体積、両端/片端/非交差の解析解、端面の向き、グラフと Undo/Redo、従来の完全分割を確認。
- Release で4形状の斜め有限溝、片端が外面へ抜ける溝、ノイズ付き軸方向の有限溝を目視確認。有限長設定の保存と、楕円体シーンの保存→読込→再保存でグラフが完全一致。
- ノイズ付き楕円体（寸法2/2.5/1.7、分割16、強度0.08、Seed17、回転12/25/8）は中間 tip 平面の数値検証で拒否。壊れたメッシュは出力せず診断する。任意設定での成功を保証する段階ではない。
- 検証素材は `build/p5-finite-validation/`（拒否例も保存）、実行ファイルは `build/p5-finite-release/`。実マウスの通し操作と Debug 描画は未検証。

**残る範囲**：不安定断面の改善、交差する複数亀裂、曲面の Bridge 計測、複数ループ/穴、一般的な自己交差修復。P5 全体は未完了。

### 2026-09-21 曲面と任意角度の全幅部分溝（P5 限定実装）

- `crack/MeshCut` と Crack の「Mesh 全幅溝」を追加。U 全幅と +V 側外面を覆うパッチから、有限深さの溝を作る。部分切断オン時だけ形状へ適用する。
- 亀裂幅の両側と終端の3平面で母岩を区分し、溝セルと内部区切り面を除き、外皮・両壁・終端を結合する。未破断部を残す閉じた1連結体と体積を検証する。
- 未加工の母岩1個・切り込み1回・4096三角形以下。ゼロ幅/深さ・非交差は入力を維持する。曲面の Bridge 断面値を矩形近似で代用せず、計測/表示は未対応と明示する。
- 旧シーンは Box 専用方式を維持。Mesh 全幅溝の設定を保存/読込と Undo/Redo へ接続。
- Debug / Release ビルドと CTest が成功（1/1、8.76秒）。4形状の軸方向/斜め切断、3072三角形のノイズ付き母岩、決定性、体積保存、残存部の完全分割、無変更条件と不正設定、Undo/Redo を確認。
- Release で4形状とノイズ付き母岩の溝の壁・終端、ゼロ幅、深すぎる設定の診断を目視確認。切断設定の保存と、保存→読込→再保存のグラフ一致を確認。
- 検証素材は `build/p5-slot-validation/`、実行ファイルは `build/p5-slot-release/`。実マウスでの通し操作と Debug 描画は未検証。

**残る範囲**：U 方向も途中で止まる有限長溝、交差する複数亀裂、曲面の Bridge 断面計測/表示、穴/複数ループの中間切断、一般的な自己交差修復。P5 全体は未完了。

### 2026-09-21 Joint Set による多片の完全分割（P5 限定実装）

- `fracture/MultiSplit` と Fracture の「Joint Set で分割」を追加。非表示でも保持する上流の節理定義を、無限平面列として適用する。旧シーン/既定は従来の単一平面モード。
- 非交差/接触で片を維持し、同一/逆法線の重複面は最初の定義を使う。各切断は閉じた1連結体と単純な断面ループ1本を条件とし、極小片・多ループ・不正閉包は全体を診断する。
- 最大32平面・128片・20万三角形。母岩比1e-7以下の片を拒否し、最終体積和を相対1e-4で確認する。
- 節理ノード ID/パッチ番号と各側の組で片の変換キーを生成。色分け、ドロップダウン/クリック選択、Locked、移動/回転、保存/Undo に対応。最終片の共有断面・面積と元の節理を保持する。
- Debug / Release ビルド、CTest が成功。直交3平面の8片/12共有断面、平行列、曲面3形状と弱いノイズ、複数 Seed、32平面の33片、128片超過、再現性、体積、Undo/Redo を確認。
- Release で曲面/Box の9片、分離配置、ばらつき、ガイド非表示、上限エラー、旧単一平面を目視確認。分離配置の保存→読込→再保存でグラフが一致。
- CLI 入力注入で9片目のクリック選択とギズモ表示、移動/回転、Esc 取消、Locked の操作抑止を確認。移動/回転は対象キー1つだけが変わり、取消/選択のみ/Locked 時は保存グラフが一致。実マウスによる通し操作と Debug 描画は未検証。
- 検証素材は `build/p5-multi-validation/`、実行ファイルは `build/p5-multi-release/`。


**残る範囲**：曲面/交差パッチの部分切断、穴/複数ループ、再帰破砕、continuity/roughness。平面集合やコピーによる節理 ID の変更でキーが変わる場合、古い片の設定は残すが適用せず、新しい片は Locked の既定値にする。自動対応付け/選択の永続化は P6。

### 2026-09-20 Joint Set の有限パッチ列（P5 後半の表示基盤）

- `crack/JointSet` と Joint Set ノードを追加。1～64本のパッチを基準法線方向に配置し、位置とローカル U/V 軸の傾きを固定ハッシュと Seed で再現する。
- 設定パネルで中心・回転・間隔・位置/角度ばらつき・オフセット・本数・Seed・半幅・深さ・Persistence・Aperture・表示を編集。保存/読込と Undo/Redo に接続。
- 複数系統の直列接続、途中プレビュー、合流時のパッチ単位の重複排除に対応。ガイド表示は母岩の実形状を変更しない。
- 向き/配置、決定性、最大本数とばらつき、有限範囲、不正設定、母岩不変、直列/合流、Undo/Redo の自動テストが成功。Debug / Release ビルドと CTest が成功。
- Release で1系統/2系統、表示切替、Seed 違い、設定省略時の既定値、不正本数の診断を目視確認。保存→読込→再保存で全設定と接続が一致。検証素材は `build/p5-joint-validation/`、実行ファイルは `build/p5-joint-release/`。実マウスによる通し操作と Debug の描画確認は未実施。

**残る範囲**：Joint Set による実切断・多片分割、continuity/roughness、曲面/交差部分亀裂。Fracture は Joint Set のパッチ列をまだ使わず、従来の独立した単一平面で分割する。

### 2026-09-20 母岩形状と弱い BaseNoise（P5 前半）

- `geometry/BaseRock` に4形状と共通設定を追加。曲面は cube surface の共有頂点を投影して生成し、Sphere は直径、Ellipsoid は各軸寸法、RoundedBox は最短半辺に対する丸みで指定する。
- 面分割数は4/8/16/32（最大12,288三角形）。ノイズなしの Box と丸み0の RoundedBox は従来の Box メッシュをそのまま使う。
- seed を使った固定ハッシュと補間によるノイズを、原点からの正の放射方向変位として加える。強度0～0.15、細かさ0.5～4。強度0では Seed によらない。
- 設定パネル、保存/読込、Undo/Redo とグラフへ接続。旧シーンは Box・ノイズ0として読み込む。不明形状/不正設定は診断する。
- 全形状・全分割数の閉包、連結性、外向き面、頂点共有数、指定寸法、球/楕円体の解析曲面と体積、最小/最大寸法、最大ノイズ・丸み、Seed の再現性と変位上限を自動検証。
- 曲面の中央/斜め平面による分割、ノイズ付き母岩の分割、体積保存、曲面への部分切断拒否、Undo/Redo を検証。Debug / Release ビルドと CTest が成功。
- Release で Sphere / Ellipsoid / RoundedBox / ノイズ / Seed 違い / 斜め分割 / 旧 Box / 不明形状の画面を確認。ノイズと分割設定の保存→読込→再保存を検証。素材は `build/p5-validation/`、実行ファイルは `build/p5-release/`。

**残る範囲**：Joint Set・多片分割・交差亀裂を含む P5 全体は未完了。P3 の部分切断は引き続きノイズなし Box 専用。曲面は三角形近似で、表示は面ごとの法線。写真の主稜線・細かな亀裂・剥離・材質はこの段階で完成とはしない。


### 2026-09-20 単一平面での完全分割と Chunk 操作（P4 限定実装）

- `fracture/PlaneSplit` と Fracture ノードを追加。共有交点で両側をクリップし、断面の境界頂点を保持した三角形化で閉包を作る。両側が閉じた1連結体、体積が正、体積和が一致することを検証する。
- 入力は未分割の岩1個。単純断面1本を対象とし、任意角度の平面、Box の頂点/辺を通る平面、P3 の切り込みを含む凹断面に対応。Crack から渡された形状を分割し、Depth を自動的に完全貫通へ変更しない。
- `(ノード ID, 負側1/正側2)` の Chunk ID、親の生成元、分割面の中心・法線・面積を保持。再帰分割・複数ループ/穴・面の重なり・極小片は診断する。
- 青/茶で色分けし、クリックまたは設定欄で片を選ぶ。Locked を解除すると数値欄と W/E の移動/回転ギズモで編集できる。Locked は現在の配置を保つフラグ。物理計算はしない。
- 片の回転は分割直後の外接箱中心が基準。大きな移動でも体積積分が安定するよう基準点を変更し、変換精度が不足するときは診断する。
- 設定・変換・Locked を JSON に保存し、生成 ID を再構築する。Undo/Redo は既存スナップショットへ統合。選択中の片番号自体は UI の一時状態で、保存しない。
- Debug / Release ビルドと CTest、160組の Box 寸法・斜め平面、最小/最大寸法、辺/頂点上、法線反転、非交差、極小片、部分亀裂からの分割、断面法線・面積・体積、Merge と Undo/Redo を検証。
- Release の CLI 入力注入で、片のクリック選択、移動ギズモ、回転ギズモ、Esc による取消、Locked 中の操作抑止を確認。実マウスでの手動通し操作とは区別する。
- Box / 部分切断 / 有限長の溝 / 斜め平面 / 非交差の画面、設定の保存・再読み込みを検証。検証素材は `build/p4-validation/`、実行ファイルは `build/p4-release/`。
- ユーザー提示の `DSC02801.JPG` を外観の参考として整理。細かな分岐亀裂・剥離・材質の調整は現 MVP の機能完成と別に検証する。

**残る制約**：外部の任意メッシュの自己交差検出・修復、穴/複数断面、再帰分割、複数片一括変換、倍率・抽出・削除、選択の保存、永続的な評価キャッシュ。分割平面自体の操作は数値欄。ドラッグ中は小さなメッシュを再評価・転送する。Debug の実画面確認と手動操作は未実施。


### 2026-09-20 Box の部分切断と Rock Bridge（P3 限定実装）

- `crack/PartialCut` に、未加工 Box 1個と軸に沿う単一パッチの実切断を追加。Crack の「部分切断 (Box)」で有効にする。旧シーンは既定で無効。
- 切り込みの境界平面で Box を厳密に分割し、側壁・終端・外面を閉じた1連結体として生成する。均等 voxel や形状の解像度近似は使わない。
- +V 側の外面から入る切り込みに限定。任意角度・内部空洞・複数切断・複数 Box・Bridge 消失・開口が両側面へ抜ける設定は診断する。幅/深さゼロ・非交差は元の Box を保つ。
- 実切込深さ、未破断部の厚さと断面積を表示。Bridge の断面は緑の透視ガイドとして表示でき、亀裂の候補ガイドとは別に切り替えられる。
- 上流からノードごとに評価し、加工前と加工後の枝を別の結果として保持。評価1回の中で共有上流を再利用する。切断設定・Bridge 表示は保存/読込と Undo/Redo に対応。
- Debug / Release ビルド、既存テストを含む CTest 成功。160組の寸法・範囲・深さ、64組の90度回転、最小/最大 Box、薄い Bridge を検証した。
- 自動検証は閉包、1連結成分、各頂点周囲の面のつながり、除去体積、全三角形が固体境界にあること、側壁・終端の存在、描画データ、非破壊な枝、Undo/Redo を確認。
- Release の画面で全幅の溝、有限長の溝、Bridge 表示、90度回転、幅ゼロ、貫通時の診断、旧P2のガイド表示を確認。保存→再読込→再保存で設定と接続が一致。
- 検証素材は `build/p3-validation/`、検証用 Release は `build/p3-release/`。既存 data/ は変更していない。

**残る制約**：任意角度・一般メッシュ・複数交差への汎用切断処理、taper、粗さ、一般メッシュの自己交差検出器は未実装。P3 の対応範囲では直交分割で面の交差を避ける。専用ギズモと実マウス操作による通し検証は引き続き未対応/未実施。


### 2026-09-20 有限亀裂パッチの表示（P2）

- `crack/CrackPatch` に中心・回転・U/V 半幅・Depth・Persistence・Aperture と座標系の生成・検証を追加。
- `Base Rock → Crack → Mesh Output` で候補矩形（青）と到達矩形（橙）を半透明で透視表示する。母岩のトポロジーは変更しない。
- 設定パネルで位置・回転・各パラメータを編集し、ガイドを非表示にできる。表示深さは `min(depth, 2 * extentV) * persistence`。これは P2 の可視化規約で、母岩表面との交差や未破断部の判定はまだ行わない。
- 設定と表示フラグの保存/読込、Undo/Redo、連続した Crack、途中プレビューに対応。入力欠落・不正パラメータは診断し、古いガイドを消す。
- Debug / Release ビルド、既存と新規の CTest が成功。パッチの回転・範囲・深さの境界、開口幅、透視用描画データ、母岩不変、表示切り替え、Undo/Redo を検証。
- Release で通常の到達範囲、Persistence 0、全範囲到達、ガイド非表示を撮影して確認。実アプリの保存→読込→再保存で全パラメータと接続が一致。
- 検証素材は `build/p2-validation/`、検証用 Release は `build/p2-release/`。既存 data/ は変更しない。
- 位置・回転の編集は数値欄。パッチ専用のビューポート選択・ギズモ、およびマウス操作による通し検証は未対応/未実施。


### 2026-09-20 Base Rock の Box（P0/P1）

- `geometry/Mesh` に共有頂点の CPU メッシュ、Box、面法線、AABB・体積・閉包・連結性の検証を追加。
- Base Rock を右クリックメニューに追加。原点中心の Box を Mesh Output / Merge / 途中プレビューから表示する。
- 寸法は各軸 0.001～1000 m。Seed は保存するが Box 形状には影響しない。寸法不正時はノード ID を含む診断を出す。
- `ProjectIo` に設定の保存/読込を追加し、既存 Undo/Redo のスナップショットに統合。
- Debug ビルド、既存と新規テストを含む CTest が成功。テストは形状・不正入力・描画変換・接続・再評価・Undo/Redo を確認。
- Release 版で 3×4×5 m、Seed 123 の Box と設定パネルを目視確認。実アプリで保存→再読み込み→再保存を行い、寸法・seed・接続の一致を確認。
- 検証資料は Git 対象外の `build/p1-validation/`（`box-ui.png`、`reloaded-ui.png`、往復保存したシーン、ログ）。既存 `data/` は変更していない。

**検証環境の補足**

- vcpkg 再実行が `C:/vcpkg/buildtrees` の書き込み権限で失敗したため、導入済み依存を利用して `cmake --preset x64 -DVCPKG_MANIFEST_INSTALL=OFF` で再構成した。リポジトリのプリセットは変更していない。
- 起動中の既存 Release アプリが通常の出力先を使用していたため、MSBuild の `OutDir` で `build/p1-release/` に検証用実行ファイルを出力した。
- Debug の GPU ベースバリデーション有効時は初回描画までの待機が長く、今回の画面確認は Release で行った。Debug の起動描画確認は未完了。


### 2026-09-20 岩をモチーフにしたアプリアイコン

- 切断面と割れ目のある岩塊を描いた透過 PNG と、16 / 24 / 32 / 48 / 64 / 128 / 256px を含む ICO を作成。
- `assets/icons/rock_editor_v2.*` に画像・生成プロンプトを保存し、`RockEditor.rc` の参照先を更新。

### 2026-09-20 rock-editor への改名と土台のクリーンアップ

road-editor（旧 terrain-graph）を岩生成エディタの土台にするための整理。

**削除したもの**（約 14,000 行）

- `src/graph/`：Road / RoadMask / RoadProfile / Path / ConnectionPrototype /
  SurfaceLayout 一式（SurfaceLayout / Editing / Evaluation / BandGeometry / PresetGraph）
- `src/io/SurfaceLayoutIo`
- `src/app/`：ApplicationPathEdit / ApplicationSurfaceLayout / ApplicationSurfacePreset /
  ApplicationBoundaryMaterial / RoadMaskUi
- `tests/`：RoadTests / SurfaceLayoutTests / ConnectionPrototypeTests
- `tools/make_connection_prototype.py`
- ノードの種類：Road / Lane Marking / Road Mask / Decal / Shoulder / Crack / Path
- 値の型：Path / RoadMask
- レイヤーマテリアル（.tglayer）と境界マテリアル（.tgboundary）の編集・アセット機能（描画用の関連型・属性には残存あり）
- 旧形式（.tgproj / .mmproj / .mmmat）の読み込み互換

**残したもの**

DX12 レンダラ（rhi / renderer）、Dear ImGui、ビューポートと軌道カメラ、
ノードグラフ（Surface / Model / Transform / Merge / Mesh Output）、
マテリアル・テクスチャ・天球・モデルのアセット基盤、
プロジェクトの保存と読み込み、アセットブラウザ、アンドゥ。

**改名**

| 対象 | 旧 | 新 |
| --- | --- | --- |
| CMake プロジェクト / ターゲット | `road_editor` | `rock_editor` |
| vcpkg マニフェスト | `road-editor` | `rock-editor` |
| リソーススクリプト | `src/app/TerrainGraph.rc` | `src/app/RockEditor.rc` |
| C++ 名前空間 | `tg::` | `rock::` |
| マクロ接頭辞 | `TG_` | `ROCK_` |
| JSON 形式識別子 | `terrain-graph.*` | `rock-editor.*` |
| 内部フォルダ | `<root>/.terrain-graph` | `<root>/.rock-editor` |
| 設定の保存先 | `%LOCALAPPDATA%/road-editor` | `%LOCALAPPDATA%/rock-editor` |
| レイアウト | `road_editor_imgui.ini` | `rock_editor_imgui.ini` |
| プロジェクトの目印 | `project.tgproj` | `project.reproj` |
| シーン | `.tgscene` | `.rockscene` |
| マテリアル | `.tgmat` | `.rockmat` |
| 天球 | `.tgsky` | `.rocksky` |
| モデル | `.tgmodel` | `.rockmodel` |

Debug ビルドとテスト（`rock_editor_tests`）が通り、アプリが起動して
ビューポート・グラフ・アセットブラウザが表示されることを確認した。

## 未完了

### レビューの持ち越し（2026-09-22）

- 材質だけの編集でも `MarkDocumentChanged` が形状の再評価・全メッシュの再構築・再転送を起こす。ベイク指紋はそのたびに参照テクスチャのファイルを読み直す。材質の変更と形状の変更を分け、テクスチャのハッシュを読み込み時に控える。
- `Mesh::Create` がメッシュ1個ごとにGPU全同期を行う（最大512片）。転送を1回のコマンドにまとめる。Piece Transformは頂点へ焼かず、描画時の行列で渡す。
- ビューポートのリサイズごとの `WaitForGpu`、形状AOのタイルごとの同期待ち、最小化中にベイクが進まない点、デバイスロスト後に通知なく止まる点。
- ベイクの途中失敗で書き出し済みPNGと読み込み済みテクスチャが残る。再ベイクのたびに材質・テクスチャ・フォルダが増える。
- ギズモのドラッグをEscで取り消すと、何も変わらないUndo段が残る。
- UV Unwrap・Volume to Mesh・Volume Transformは取消トークンを見ない。
- Marching Tetrahedraの出力（解像度96で約16万〜40万三角形）がTo Volumeの入力上限25万三角形を超え得る。
- CTestが全スイートを1件として実行する。`--test-gpu-ao` はCTest未登録。テストとアプリで同じソースを二重にコンパイルしている（共有の静的ライブラリ化が候補）。
- Releaseの実行ファイルがソースツリーの `shaders/` の絶対パスを参照する。

- （削除済み機能の残作業だった「完全分割の穴・複数ループ」「部分切断の不安定断面・複数交差」は、Joint Set / Crack / Fracture の削除により対象外。ボリューム上の操作として設計し直すときに改めて扱う）
- Chip、OBJ 書き出し。（Joint Set / Crack / Fracture は削除済み。Triplanar と枝単位キャッシュは対応済み）
- P1 の Box は無地表示。Surface 接続と岩用 UV/Triplanar は未対応。既存の Transform ノードはモデル専用のまま。
- グラフからの寸法変更・Undo/Redo は自動テスト、Chunk ギズモは CLI 入力注入で確認。実マウス操作による一連の編集操作は未検証。

## 注意点

- src/renderer/MeshData.h には roadUv、道路・境界材質関連属性が残る。岩用トポロジーとは分離し、残存部分の全面整理を岩生成の前提作業にはしない。

- **`data/` は改名の対象外**（依頼で「触らない」と決めたため）。
  中身は road-editor 時代のまま（`project.tgproj`、`.terrain-graph/`、
  `BoundaryMaterials/`、`LayerMaterials/`、道路テクスチャなど約 1 GB）。
  新しい rock-editor はこれらを認識しないので、ルートを開くと
  `project.reproj` を新たに作る。不要になったら手で整理する。
- 旧 `assets/icons/rock_editor.*` は道路の絵柄の資料として保持。アプリは `rock_editor_v2.ico` を使用する。
- `AGENTS.md` / `CLAUDE.md` はバージョンの基準を `package.json` と書いているが、
  このプロジェクトに `package.json` は無い。実際の基準は `CMakeLists.txt` の
  `project(rock_editor VERSION ...)`。
- 旧 road-editor の状態は Git の最初のコミット（`chore: 既存の road_editor /
  TerrainGraph 時点のスナップショット`）に残してある。
