# progress — 進捗と注意点

作成日時: 2026-09-20 20:05
更新日時: 2026-09-27 00:00

Shape Maskに「曲率（谷）」「曲率（山）」を追加。符号で凹凸を分け、平面は両方で黒。曲率スケールと既存の下限・上限・ガンマで調整する。[仕様](../reference/shape-mask.md)。

Deposition Maskを追加。土が溜まる候補をメッシュの形からマスクにし、既存のApply Materialへ渡す。形状の厚み生成は含めない。[仕様](../reference/deposition-mask.md)。

岩用の共有レイヤーマテリアルを追加。terrain-graphから素材データ・検証・合成・保存の仕組みを移植し、4層の一覧編集と均一／ムラの被覆に整理。道路関連は除外。Surface経由で利用する。[仕様と操作](../reference/layer-material.md)。

## 現在地

### 2026-09-27 人のシルエットによるサイズ比較

- 「表示」→「人のシルエット（サイズ比較）」でオン・オフ。「人のシルエット設定」で身長0.5〜2.5 m（既定1.7 m）とXYZ位置補正を変更する。アプリ設定として保存。
- 表示メッシュの右端から0.65 m離し、足元を底面へ合わせる。モデルのみのシーン／メッシュなしでは(3, 0, 0)を基準にする。向きはカメラへ追従するが、Y軸は直立を保つ。深度テストにより岩の後ろでは隠れるため、必要なら位置を補正する。生成・ベイク・書き出し用のメッシュには加えない。
- Releaseビルドと全CTest成功。隔離したアプリ設定・サンプルシーンで表示オン／オフ、別角度での遮蔽、身長と位置補正の反映を画像確認（build/human-scale-validation/）。実マウスでのメニュー操作は未確認。

### 2026-09-26 全ノードの処理ステータスを固定欄へ統一

- Layered Boxes等の先頭に更新中だけ入る説明を削除。UV Unwrap・Shape Mask・Noise Mask・Mask Combine・Remesh・Decimateの処理表示も共通の固定欄へ移した。
- エラーはクリップ＋ホバーで全文表示。メッシュ数とプレビュー見出しは結果の有無に依存させず、全ノードで設定欄の縦スクロールバーを常設。出力集計とベイク状況は固定1行。グラフカードの計算中表示は文字追加から既存の色印へ変更した。
- Releaseビルド・全CTest成功（約13秒）。Layered Boxesの通常時とエラー時でスライダー領域の画素が一致することを確認（`build/status-layout-validation/`）。CLI画像撮影は評価完了を待つ仕様のため、処理中と完了後の連続ドラッグ比較は未確認。

### 2026-09-26 Noise Mask

- 独立したNoise Maskを追加。3D位置の滑らかなノイズを4段階で重ね、歪みとコントラストを加えてUVマスクにする。基本UIは大きさ・コントラスト・Seed。
- 白黒プレビュー、Apply Material、Mask Combine、下流キャッシュ、反転、保存設定とUndo/Redoに対応。`examples/noise-mask/` に色ムラのサンプルを追加。
- Releaseビルド・全CTest成功（約12秒）。UVの島間の同一位置、UV配置変更、Y座標の影響、Seed再現性、入力診断、キャンセル、素材とマスク合成、キャッシュ更新、Undo/Redoを検証。実アプリの白黒プレビュー・素材適用を確認。保存→再読込→再保存でグラフと設定が一致。画像は `build/noise-mask-validation/`。実マウスでの連続ドラッグとGPUベイクの専用比較は未実施。

### 2026-09-26 Piece Selectの方式選択を一本化

- プリセットボタンを削除し、方式の変更をプルダウンに統一。7方式を日本語の動作名で表示する。方式変更は既存の設定値を保持する。
- Releaseビルド成功。表示項目のみの変更のため、新規テストは追加していない。

### 2026-09-26 上下支持と全層共通の逐次侵食

- 隣接層の実際の上下面から投影交差面積を計算し、分割結果にキャッシュ。両面を覆われた片を候補から外し、全層共通で1片ずつ欠ける。旧Peelの層別減衰は廃止し、対象層の指定は維持。
- UIの基本操作を進行・ばらつき・別の欠け方に整理。Seed・中心保護・対象層・反転と層別集計は折りたたむ。
- Releaseビルド・全CTest成功。上下の面積、重なりのない板、回転した積層、立体分割で上下面に届かない片、中間層の保護、削除順の延長、Filter後の継続、保存・Undo・分割キャッシュを検証。
- 実アプリの進行45%で形状と折りたたんだUIを画像確認（`build/piece-support-validation/selection.png`）。実マウスでの連続ドラッグは未確認。

### 2026-09-26 Piece Selectの操作中のレイアウト安定化

- 更新中だけ先頭に出ていた折り返す説明を固定1行へ変更。集計・層一覧は更新中も保持した入力から表示し、内容が消えてスクロール位置が巻き戻るのを防ぐ。手動選択の編集可否は従来どおり更新完了まで制限する。
- 全片をワイヤーフレームにして描画する面が0枚になっても、メッシュ数・プレビュー見出しを維持。エラー行と全選択の注意行の高さを確保し、Piece Select中は縦スクロールバーを常設する。
- Releaseビルド・全CTest成功（約12秒）。実アプリで進行45%と全片選択100%のスライダー位置が同じことを画像で確認（`build/piece-peel-validation/layout-partial.png` / `layout-all.png`）。実マウスによる連続ドラッグの確認は未実施。

### 2026-09-26 欠けの進行を中心にしたPeelの操作

- Peelの最上部に全幅・0〜100%の「欠けの進行」スライダーを配置。その直下へばらつき・Seedを移し、「最初に戻す」「別の欠け方を試す」を追加。ばらつき0では別パターンのボタンを無効にする。
- 進行は既存のfraction、ばらつきはpeelNoiseを使い、侵食順序・保存形式・Undoの仕組みは維持する。進行を動かしてもSeedを変更しない。中心保護・層の減衰によって100%でも片が残ることを明示。自動再生は今回含めない。
- Releaseビルドと全CTest成功（約12秒）。実アプリで上部の進行・ばらつき・Seedの配置を確認（`build/piece-peel-validation/progress-controls.png`）。実マウスによるスライダー・ボタン操作は未確認。

### 2026-09-26 隣接面積と外周からの侵食

- Voronoi分割時に共有面の相手IDと面積ベクトルを記録し、両側で対称な隣接情報をPiecesに保持。Layered Boxesでは板の上下面を入口から除いて、同じ板の側縁から侵食する。
- Piece SelectにPeelを追加。露出した候補の残る支持面積比で順序を決め、削除量・ばらつき・中心保護を調整できる。削除で新たに露出した隣が次の候補になる。既存の外側の層範囲・内側への減衰も適用する。
- Piece Filter後も除去した隣への記録を保持し、侵食を継続できる。非均等な共通変換では面積を変換する。親をまたぐ再分割の接続と個別移動後の再接触判定は未対応として診断する。上下の板の支持、残った全体の連結性保証、外部空気への接続判定は後続。[仕様](../reference/piece-erosion.md)。
- Releaseビルドと全CTest成功（約14秒）。5×5の格子で対称な40組の接続と各0.2m²、点/辺だけの接触除外、外周16片からの進行、中心保護、削除量増加の単調性、Filterを挟む継続、面積比の優先、層範囲、保存設定、Undo/Redo、分割キャッシュの再利用、キャンセルを検証。Debugは今回未実施。
- `examples/layered-pieces/progressive-peel.rockscene` を追加。実アプリで欠ける部分のワイヤーフレームと次の候補、Piece Filter後の形状を確認。保存・再読込・再保存でgraph全体が一致することも確認。検証画像は `build/piece-peel-validation/`。実マウスによる削除量・比較表示の操作は未確認。

### 2026-09-26 欠けるピースのワイヤーフレーム表示

- Piece Selectの選択片は面を隠してオレンジの線だけで表示する。残る片は従来の面と境界線を維持し、「削除後」では選択片の線も隠す。表示上の板の間隔にも追従する。手動編集中だけ面を残し、選択済みの片をクリックで解除できるようにした。
- Releaseビルド・全CTest成功（約12秒）。実アプリで側縁の選択片が線のみになる表示を確認。画像は `build/piece-select-validation/selection-wireframe.png`。手動編集切替の実マウス操作は未確認。

### 2026-09-26 Piece Select の可視化と外側の層の選択

- Piece Select 選択時に専用プレビューへ切り替え、選択片をオレンジ、未選択の候補を黄色、対象外をグレーで表示。境界線・層別の選択数と棒グラフ・削除後比較・表示だけ板を離す操作を追加。選択を外すと元のプレビューへ戻す。
- Rim に両側/上側/下側、外側からの層数、内側への減衰を追加。ワンクリックの初期設定は両側1層・選択率75%。旧設定は全層・減衰なしを維持。層の順位で判断し、覆われた部分や張り出しを幾何的に判定する露出処理と反復侵食は後続。
- Release ビルド・全CTest成功（約12秒）。上下/両側・対象層内の反転・指定層との共通範囲・内側への減衰・選択率増加の単調性・設定JSON往復・旧設定の既定値を検証。Debugは今回未実施。
- Release実アプリで色分けと境界線の表示を確認し、保存/再読込/再保存でgraph全体の一致を確認。検証画像は `build/piece-select-validation/selection-edges.png`。比較ボタン・間隔調整・プレビュー復帰の実マウス操作は未確認。
- サンプル `examples/layered-pieces/outer-layer-chipping.rockscene` と [仕様](../reference/layered-pieces.md) を更新。

### 2026-09-26 メッシュから始める薄板と欠け

- Layered Boxes を追加。Pieces は個々のメッシュ・変換に加えて層番号と親ピース情報を保持する。Scatter Points / Voronoi Fracture が Pieces 入力に対応し、各板を独立して分割する。面内配置で厚さを貫く分割を作れる。
- Piece Select に Rim と層指定を追加。元の側縁だけを確率選択して削除できる。上下面のみへ接する片を除外する。新たに露出した境界の反復侵食や接合面除去は未実装。[仕様](../reference/layered-pieces.md)。
- Release のビルドと全CTest成功（約13秒）。閉包・体積保存・層/親ID・板を削除した際の点とIDの安定性・型・キャッシュ・Undo/Redo・設定JSON往復・任意のボリューム変換を検証。今回の変更後のDebug検証は未実施。
- `examples/layered-pieces/` に単板、積層の面内分割、厚さ方向にも点を配置する比較を追加。Release実アプリで形状を確認。保存・再読込・再保存で設定、ピン、リンクを含むgraph全体が一致することも確認。画像は `build/layered-pieces-validation/`。実マウスでの編集操作は未確認。

### 2026-09-26 Parallel Planes と構造面による割れ目

- ユーザー合意の最初の段階として、母岩と独立した Parallel Planes と専用の Planes 型を追加。向き・間隔(m)・位置・間隔のばらつき・Seedを編集し、選択中は構造面を緑の矩形で表示する。[仕様](../reference/parallel-planes.md)。
- Volume Crack の第3入力に Planes を追加。Points / Planes のいずれか一方を使い、V字断面と幅の変化を共用する。複数系統は別方向の Parallel Planes と Volume Crack を直列につなぐ。元のPoints入力の順番・ID・接続を維持する。
- 面の配置は原点と面番号に基づくため、母岩の範囲を変えても位置と乱数がずれない。範囲内512面の上限と診断を設けた。幅のばらつきで閉じた面を格子上の偽の隙間にしない。
- 単体検証：1方向で6枚の板、2方向で36ブロック、間隔を広げて4枚、浅い割れ目のつながり、MT/DCの閉包、回転ガイド、再現性、境界条件、型、キャッシュ、Undo/Redo。Release / Debugのビルド・全CTest成功（Release約12秒、Debug約213秒）。
- Release実アプリで `examples/parallel-planes/` の板状・交差節理・浅い割れ目を確認。検証画像は `build/parallel-planes-validation/`。設定・ピンID・接続の保存と再読み込み後の再保存が一致すること、旧Voronoiシーンの全リンクと既存ピンIDが保持されることを確認。実マウスでのプロパティ編集・チェックボックス・Undo/Redoは未確認。
- 初版は構造と割れ方の確認用で、自然な岩の完成外観は未達。斜めの細い稜線には格子由来の段差がある。浅い割れ目でも外面に近い薄片が分離し得る。Volume Terraceは変更せず、局所剥離・面のうねり・構造に沿う風化と材質は次の段階とする。

### 2026-09-26 Volume Clip（平面での切断）

- `Volume Clip` を追加。仕様は [Volume Clip](../reference/volume-clip.md)。設定は高さ (m)・反転。保存名 `volumeClip`。名前はユーザーと相談して決めた。
- 実装: `src/geometry/Volume.cpp` の `ClipVolume`。半空間との交差 `max(d, h − y)`。切り離された塊は残す。
- 検証: `tests/VolumeClipTests.cpp`。Debug で全テスト成功。実アプリでの操作は未確認。

### 2026-09-22 Volume Edge Wear（角の摩耗）

- `Volume Edge Wear` を追加。仕様は [Volume Edge Wear](../reference/volume-edge-wear.md)。設定は半径・量・ばらつき・細かさ・上向きに集中・Seed。保存名 `volumeEdgeWear`。
- 実装: `src/geometry/Volume.cpp` の `EdgeWearVolume`。Volume Smooth のぼかしを `BlurGrid` として切り出して共用。稜線の強さ `(ぼかし − 元) / σ` を最も近い表面の点で三線形補間して読む。格子点で読むと稜線の二等分面に沿って残る差で薄い板の中心が削れる（プローブで確認し、テストに残した）。
- 検証: `tests/VolumeEdgeWearTests.cpp`（46 項目）。Release で全テスト成功。サンプル `examples/volume-edge-wear/` をヘッドレスの `--screenshot-ui` で描画し、ノードの設定欄とプレビューを確認した。
- 注意: 半径に対して薄い部分（厚さ 2σ 程度）は面もわずかに削れる。半径がセル間隔に近いと効かない。鈍い稜線（150 度以上）はほとんど削れない。

### 2026-09-22 Remesh の UV 引き継ぎ（UV Unwrap の後に置ける）

- ユーザー指摘「Remesh は Displace で持ち上げるための密度を稼ぐのが目的。UV Unwrap の前に置くと展開が重くなる」への対応。Decimate と同じ方針で UV を引き継ぐ。継ぎ目（島が違うか、コーナー UV が違う辺）を特徴辺の集合に入れて反転せず、継ぎ目の頂点は縮約も平滑化もしない。分割は面ごとに UV を補間。縮約は辺上の比率で補間。平滑化は投影で当たった元の三角形が同じ島なら重心座標で UV を読み、別の島なら動かさない。縮約・反転・平滑化で UV の裏返りと潰れを検査する。仕様は [Remesh](../reference/remesh.md) の「UV の引き継ぎ」。
- **見つけた退行**: 前のコミット（`458dff9`）で、デバッグ出力を正規表現で消したときに `SplitLongEdges` の呼び出し行まで消えていた（同じ行にデバッグ出力を書いていた）。前処理の 1→4 分割と縮約だけで球のテストは通っていたため気付かなかった。UV 付きのテストで面数が前処理のまま変わらないことから発覚し、復元した。以後、デバッグ出力は単独の行に書く。
- 単体テストは 44 項目（UV 付き 7 項目を追加。閉じたまま細かくなる、UV と島とアトラスの寸法、体積、島ごとの UV の面積と裏返りの無いこと、継ぎ目の頂点が残ること、継ぎ目以外の辺が揃うこと、不正な UV の診断。グラフでは UV Unwrap の後に置いても UV 付きの細かいメッシュを得ること）。Release / Debug で全 2,998 項目が成功。
- Release 実アプリで、`examples/shape-mask` の UV Unwrap の直後に Remesh（辺の長さ 0.01）を挟んだ検証シーンを開き、151,788 三角形の出力の上で Shape Mask（オクルージョン・上向き度）付きの3段の素材が同じ位置に貼られることを確認（`data/test/claude-session-2026-09-22/remesh-uv/`）。UV のアトラスが変わらないので Shape Mask の画像がそのまま使える。実マウスでの操作、Undo/Redo の手動確認は未実施。

### 2026-09-22 三角形数の上限を300万面 → 400万面へ

- ユーザーの問い「300万は半端では」に対し、面数は Subdivide で4倍刻みに増えるので「解像度128の Dual Contouring（約24万面）を2段割った約384万面が収まる 400万」を提案し、合意した。定数3か所・診断文・テスト・資料を 400万 に揃えた。Remesh の辺の長さの下限を 0.002 → 0.001 に下げた（直方体でも 0.002 では見積もりが約350万面で上限に掛からず、上限超過の診断をテストできないため。0.001 なら約1,400万面）。
- ユーザー依頼「ポリゴン数の上限を100万ではなく300万ぐらいまで」への対応。`kMaxDetailTriangles`（Subdivide / Displace の出力合計）、`kMaxRemeshTriangles`（Remesh の出力）、Decimate の入力上限（新設 `MaxDecimateInputTriangles`）を 300万 にし、診断文とドキュメントを合わせた。To Volume の Mesh 入力の上限（25万三角形・100万頂点）は格子化の計算量の話なので変えていない。
- Displace のテスト「予測数で拒否する」の面数を 250,001 → 750,001 に合わせた。Release / Debug で全 2,991 項目が成功。起動中のアプリが exe を掴んでいたため、`rock_editor.exe` を `rock_editor.running-<pid>.exe` へ改名してからリンクした（不要になったら消してよい）。
- Remesh の事前見積もりに掛けていた 1.2 倍の余裕を外した（直方体は面積 ÷ 辺²が最大で、余裕があると事前の診断に掛からず、300万面まで分割してから止まる）。

### 2026-09-22 Remesh（等方リメッシュ）

- ユーザー依頼「Subdivide の替わりに Remesh のようなノード」への対応。UV Unwrap の前に置く等方リメッシュ（案1）で合意し、Subdivide は残した。仕様は [Remesh](../reference/remesh.md)。
- 実装は Botsch & Kobbelt の増分リメッシュ（分割 → 縮約 → 反転 → 接線方向の平滑化 → 元の表面への投影）。投影は入力の三角形を一様格子に登録した最近点検索。縮約の安全判定（リンク条件・裏返り・形の良さ）は Decimate の考え方を流用。
- 実装して分かったこと（3回直した）: (1) 辺を割るたびに中点から向かいの頂点へも辺を張る増分の分割だけでは、12 面の直方体が扇状の細長い面に割れて 100 万面を超えた。目標の 2 倍より長い辺を持つ面を Subdivide と同じ 1→4 分割で先に細かくする前処理を入れた。(2) 縮約で「形の悪い面を作らない」を絶対にすると、Marching Tetrahedra の細長い面から始まるメッシュで何も縮約できない。もとから悪い場所では悪化しない限り許す形にした（Decimate と同じ）。(3) 特徴辺を毎回の折れ角で判定すると、格子から作ったメッシュでは段差ごとに特徴になって頂点が動けず、形の悪い面が 4% 残った。特徴辺は最初に決め（折れ角 + 両側の面の形 + 鎖の長さ ≥ 目標 × 3）、分割・縮約で引き継ぐ静的な方式にして 0.4% になった。特徴なしなら 0.01%。
- 単体テスト37項目（`tests/RemeshTests.cpp`。球の面数・体積・辺の長さの分布・次数、直方体の稜線と角の保持、Marching Tetrahedra の細長い面の解消、診断、取消、グラフ）。Release / Debug で全 2,991 項目が成功。
- サンプルは `examples/remesh/`（Volume Noise のグラフの Volume to Mesh と Mesh Output の間に Remesh、辺の長さ 0.015）。Release 実アプリで Remesh を選んで撮影し、形が保たれたまま出力が 64,448 三角形になること、`--save-project` で `remesh` の3設定と2本の接続が残ることを確認（`data/test/claude-session-2026-09-22/remesh/`）。ワイヤーフレーム表示が無いので三角形の揃い方は画面では見ていない（単体テストの辺の長さの分布で確認）。実マウスでの操作、Undo/Redo の手動確認は未実施。

### 2026-09-22 Volume Close（割れ目の奥を埋める）

- ユーザー依頼「割れた溝の内側を埋めるようなノード。溝は奥までなくてよいのに、深くまでメッシュが生成される」への対応。案として (1) 汎用のクロージングのノード、(2) Volume Crack に底の深さを足す、を示し、ユーザーが (1) を選んだ。仕様は [Volume Close](../reference/volume-close.md)。
- 実装はモルフォロジーのクロージング。`d ≥ r` を「遠い外部」とし、そこまでの距離変換（Felzenszwalb & Huttenlocher、軸ごとに並列）から「半径 − 距離」を閉じた形の符号付き距離にする。元から内部の点は入力のまま、外部の点は入力との小さいほう。入口を埋めて閉じ込められた空洞も埋める。
- 実装して分かったこと（3回直した）: (1) 格子の余白が2セルしかないので、格子の中だけでは遠い外部が見つからず、表面近くが埋まらない。外周の6方向へ「外周までのセル数 + 外周の値から r までの不足分」を候補にして外挿した。(2) 最初は埋めた点の値を「外部までの格子距離 − 半セル」で作ったが、埋めた面の等値面がセルの中間に揃い、表面が格子状に角張った（実画面でボクセルのように見えた）。距離変換に最も近い点の添字を持たせ、その点の値の超過分 (d − r) を引いて等値面までの連続な距離にした。平らな面ではこれが入力の値と一致する。(3) 表面の点をちょうど r の位置で拾うと表面が半セル外へ出たが、(2) の方式で解消。
- 単体テスト29項目（`tests/VolumeCloseTests.cpp`。上面に V 字の溝を彫った立方体で、幅より狭い奥が埋まり広い入口が残ること、埋まる境の深さ、内部を減らさないこと、体積と外接箱、溝より広い幅で立方体に戻ること、凸な形の不変、狭い入口の奥の空洞、拒否、グラフ）。Release / Debug で全 2,931 項目が成功。テストの溝は最初、箱の上まで彫っていて外側の距離が実際より小さくなり、外挿の前提が崩れて落ちた。溝を箱の中に限った。
- **遮蔽モードの追加**（同日）。ユーザー指摘「幅ではなく遮蔽具合で埋めたい。幅だと表面も閉じてしまう」への対応。表面から距離 L 以内の外部の点から、フィボナッチ格子の全方向へスフィアトレーシングでレイを飛ばし、L 以内に形へ当たった割合（遮蔽率）を求める。`(しきい値 − 遮蔽率) ÷ なだらかさ × セル間隔` と入力の小さいほうを出力にする。平らな面の近くは遮蔽率 0.5 で既定なら 2.5 セル相当の正の値になり、面のそばの入力の値を下回らないので表面は動かない（この設計にする前に「(しきい値 − 遮蔽率) × 長さ」で考えたが、平らな面のそばの値を一様に小さくして表面が外へ出るので、なだらかさは遮蔽率の幅で持ち、1セルの傾きに固定した）。既定を遮蔽にし、幅は残した。設定は距離 / しきい値 / サンプル数 / なだらかさ。
- 遮蔽のテストを書いていて分かったこと: 細い切れ込みは浅くても底の遮蔽率が 0.9 を超えて埋まる（幅の半分と深さの比で決まる。深さが幅の 0.35 倍を超えると 0.75 に達する）。「幅は埋め、遮蔽は残す」対比は、浅く広いくぼみ（幅 0.16 m、深さ 0.05 m）で確かめた。単体テストは幅 21・遮蔽 23・グラフ 8 の計52項目。Release / Debug で全 2,954 項目が成功。
- Release 実アプリで `examples/volume-close`（Volume Noise の後ろに Volume Close。初版は幅 0.015、最終は遮蔽・距離 0.2・しきい値 0.75）を撮影し、割れ目の入口は残って表面が滑らかなままなこと、幅 0.04 では割れ目が全て埋まることを確認（`data/test/claude-session-2026-09-22/volume-close/`。`after.png` は格子状に角張った初版、`after3.png` が最終）。遮蔽モード（`occlusion.png`）では、埋める前（`before.png`）と比べて表面の細かいくぼみがそのまま残り、割れ目が浅くなっていることを確認。`--save-project` で `volumeClose` の設定（モードを含む6項目）と2本の接続が残ることを確認。三角形数の減少は数値では未計測。実マウスでの操作、Undo/Redo の手動確認は未実施。

### 2026-09-22 Volume Smooth と Volume Terrace

- ユーザー依頼「Volume Smooth / Sharpen と Terrace も作成してほしい」への対応。設計メモの 5（Smooth / Sharpen）と、4 に含めていなかった Terrace（段化）を、それぞれ独立したボリュームノードにした。仕様は [Volume Smooth](../reference/volume-smooth.md)、[Volume Terrace](../reference/volume-terrace.md)。
- **Volume Smooth**: 3軸の分離ガウスぼかし（σ = 半径 × 最長辺、±3σ）。なめらかは入力とぼかしの混合、シャープはアンシャープマスク。「上向きに集中」は入力の勾配から求めた法線の +Y 成分で重みを変える。実装して分かったこと: ぼかしで格子の端の値をそのまま延長すると、外側の余白が2セルしかないため外側の距離が頭打ちになり、**面が外へ押し出されて体積が増えた**（8.0 → 8.26 m³）。端の外は距離を足して外挿する（`SampleVolume` と同じ規約）ように直し、体積が減る側（約 7.6 m³）になった。
- **Volume Terrace**: 層の法線に射影した位置を間隔で割り、端数でへこむ層と残す層を交互に作る。層番号のハッシュで深さをばらつかせ、値ノイズで境をずらす。位相は内部の外接箱の中心が基準。削る方向にだけ足すので格子は入力のまま。
- どちらも評価・キャッシュ・保存/読込・設定欄・右クリックメニューは Volume Noise と同じ形。評価器では2ノードを1つの分岐にまとめた。
- 単体テスト: Volume Smooth 36項目（`tests/VolumeSmoothTests.cpp`）、Volume Terrace 41項目（`tests/VolumeTerraceTests.cpp`）。Release で全 2,902 項目が成功。ゆらぎのテストは、最初は面の中央 1/3 だけを見ていて境の移動が範囲に入らず落ちた。面の内側全体（1/8〜7/8）を見るよう直した。
- Release 実アプリで `examples/volume-smooth-terrace`（Volume Noise の後ろに Terrace → Smooth）の各段を `--preview-node` で撮影し、傾いた層状の棚が出ること、Smooth（上向きに集中 1）で棚の上面だけが丸くなり側面が鋭いまま残ることを確認（`data/test/claude-session-2026-09-22/smooth-terrace/`）。設定欄のラベル「上向きの面に集中」が列幅で切れたので「上向きに集中」に短くした。`--save-project` で `volumeTerrace` / `volumeSmooth` の全設定と3本の接続が残ることを確認。Debug でも全 2,902 項目が成功。実マウスでの操作、Undo/Redo の手動確認は未実施。

### 2026-09-22 Mask Combine（マスクの合成）

- ユーザー合意の案（2入力、UV 画像限定、演算5種）で実装。A / B（Mask）を受けて Mask を出すノード。演算は乗算 / 最大 / 最小 / 差 / 混合。合成した値に下限 / 上限 / ガンマ / 反転を掛ける。仕様は [Mask Combine](../reference/mask-combine.md)。
- 実装は Shape Mask の経路をそのまま使う。評価結果は A のメッシュに `previewMask` を付けたもので、Apply Material / Subdivide / Displace / 描画 / ベイクは `IsImageMaskNodeKind`（Shape Mask / Mask Combine）で判定する。使う側で掛ける反転は `ImageMaskInvert` に集めた（Mask Combine は反転を画像に焼き込むので false。Shape Mask は従来どおりノードの設定から読む）。`std::get<ShapeMaskSettings>` を使う側に残さないため。
- 注意した点：入力の Shape Mask の反転は、キャッシュに残った評価結果の `previewMaskInvert` ではなくノードの設定から読む（Shape Mask のキーに反転は入っておらず、古い結果が返るため。最初はここで単体テストが落ちた）。Mask Combine のキーには入力の反転を含め、Shape Mask を再計算せずに合成だけ作り直す。
- Material Mask（定数 / 画像。面の中心で読む）は合成の対象外にし、つなぐと診断する。A と B の UV のアトラス寸法か面数が違う場合も診断する。
- 単体テスト39項目（`RunMaskCombineTests`。演算の値、反転、解像度の違い、拒否、ノード定義、診断、受け渡し、再計算、直列、Subdivide）。Release で全 2,825 項目が成功。
- Release 実アプリで `examples/mask-combine`（錆のマスクを「オクルージョン − 上向き度」にしたもの）を開き、Mask Combine（#280）を `--select-node` で選ぶと、割れ目のうち上面を除いた所だけ白いマスクが貼られること、Mesh Output で割れ目に錆・上面に埃が載ること、`--save-project` で `maskCombine` の設定と3本の接続が残ることを確認（`data/test/claude-session-2026-09-22/mask-combine/`）。実マウスでの操作、Undo/Redo の手動確認は未実施。
- 今回は起動中のアプリがなく、`rock_editor.exe` を直接リンクできた。

### 2026-09-22 Voronoi Fracture のワイヤーフレームとポイントの表示

- ユーザー依頼「ワイヤーフレームとポイントを表示するチェックボックス」と、途中の追加依頼「オンにしたらワイヤーフレームとポイントだけになるのが見やすい」への対応。どちらかをオンにすると岩の面の描画を止める（描画器に `SetMeshSceneHidden` を追加）。最初は面を残して隠れた辺を薄く透かす案で作っていたが、追加依頼で面を隠す形に変えた。
- 稜線は `geometry::PieceEdges`。最初は元の外面どうしの辺をすべて除いていたが、面を隠すと箱の輪郭まで消えるので、20 度より大きく折れた辺は出すようにした。単体テスト1項目（2分割した箱の半分の稜線が軸に沿い、長さの合計が 20 m）。Release で全件成功。
- Release 実アプリで `examples/voronoi-pieces` の Voronoi Fracture（#20）を選び、面が消えて稜線と母点だけが出ることを確認（`data/test/claude-session-2026-09-22/voronoi-wire/select-20.png`。撮影のため一時的に既定をオンにしたビルドを使い、戻してから再ビルドした）。ラベルが列幅で切れたので、列を 150 に広げ、「ワイヤーフレーム」「ポイント」に短くした。
- 続けて依頼された Piece Filter のワイヤーフレーム：Filter の出力は重い計算のキャッシュに残らないため、評価のたびにピース系ノードの直近の出力を `RockEvaluationCache::pieceOutputs` に控えるようにした（Voronoi もここから読む）。単体テスト2項目（出力が残る、失敗したノードの分は消える）。Release で全件成功。最初は面を隠して残った片の稜線を出す形で作ったが、ユーザーの意図は「除外した片をワイヤーフレームで、現存する片はそのまま描画」だったので、設定名を「除外した片」に変え、入力の片のうち出力に同じ ID で残らなかった片の稜線を、面を隠さずに重ねる形へ直した。`examples/voronoi-pieces` の Piece Filter（#40）で、残った片が面のまま、外側の除外した片が稜線で出ることを確認（`voronoi-wire/filter-40.png`。同じく一時的に既定をオンにして撮影し、戻して再ビルド）。起動中の exe は `rock_editor.running-50028.exe` と `-61680.exe` へ改名した。
- 起動中のアプリのため、今回も `rock_editor.exe` を `rock_editor.running-68428.exe` へ改名してからリンクした。チェックボックスの実クリック、他のノードへ選択を移したときに面が戻ることは未確認（コード上は毎フレーム選択を見て戻す）。

### 2026-09-22 Plane Cuts の平面の枠の表示

- ユーザー依頼「Plane Cuts の平面の矩形のラインを表示したい」への対応。選んでいる間、切り口を囲む平面上の矩形を深度付きの線で表示する（`geometry::CutFaceFrames`、`Application::AppendPlaneCutFrames`）。矩形の大きさは「実際の切り口に合わせる」案をユーザーと合意して採用。設定欄の「表示」にチェックボックス（表示用の状態でノードの設定ではない）。
- 単体テスト2項目（枠が平面上で切り口の円を囲む正方形になる、切り口の残らない平面は枠を持たない）。Release で全件成功。最初は枠の大きさを切り口と ±2 セルで比べて失敗した（格子点で拾うので各辺で約1セル大きい）。線が面の縁に埋もれない余白として計算は変えず、テストを「切り口を囲み余白は4セル未満」に直した。
- Release 実アプリで `examples/plane-cuts` の1段目（全体5枚）と2段目（局所）を `--select-node` で選び、枠が色分けされて形に隠れる部分は隠れることを確認（`data/test/claude-session-2026-09-22/plane-frames/`）。起動中のアプリが exe を掴んでいたため、`build/bin/Release/rock_editor.exe` を `rock_editor.running-67124.exe` へ改名してからリンクした（起動中のアプリはそのまま動く。不要になったら消してよい）。チェックボックスの実クリックは未確認。
- 断面の色分け（別コミット）：表示中のメッシュの面を平面ごとに振り分け（`geometry::CutFaceAssignments`）、半透明の三角形のオーバーレイとして深度付きで重ねる。材質・ピック・統計に触れない方式にした。オーバーレイの1回の描画の端点上限を 256 → 4000 に上げ、アップロードを使う分だけにした。単体テスト2項目（MT / DC で切り口の面だけを選び、面積が切り口の円に近い）。Release で全件成功（2,782項目）。
- 実画面の注意：全体の1段目（#6）では色が出ず、深度を切ると出た。浮かせる量を3セルにして確かめると、塗られた面はカメラから見て裏側にあり、隠れるのが正しかった（撮影の視点は変えられない）。局所の2段目（#9）では、表側の欠けが枠と同じ色で塗られることを確認（`plane-frames/faces-9.png`）。撮影のため一時的に既定をオンにしたビルドを使い、戻してから再ビルドした。チェックボックスの実クリックは未確認。

### 2026-09-22 Apply Material に「ハイトで合成」

- ユーザー依頼「マスクに加えて固有テクスチャのハイトマップの変位で合成するチェックボックス」への対応。Apply Material に設定（`ApplyMaterialSettings`：ハイトで合成 / なだらかさ）を追加。マスクの重みを、その素材のハイトと下地のハイトの差で寄せる（式は [素材の適用](../reference/material-application.md)）。マスク 0 / 1 は必ず 0 / 1 になるよう式を選んだ（最初の式は満たさず、テストで直した）。
- シェーダ（`EvaluateApplied`。`maskFlags` の 4、`axisZ.w` になだらかさ）と Displace の CPU ハイト（`MaterialHeight::HeightBlendWeight`）で同じ式。素材の束（`MaterialBinding`）に設定を運び、キャッシュキーとベイクの指紋（有効時のみ）に含める。保存/読込、設定欄、単体テスト4項目。Release で全件成功。
- Release 実アプリで `rock_crack.rockscene` の検証コピー（割れ目の Apply Material をオン、なだらかさ 0.15）を描き、割れ目の縁で下地の凸部が残って境目がハイトに沿うことを確認（`data/test/claude-session-2026-09-22/hblend/compare-zoom.png`）。素材が2つとも岩なので差は控えめ。実マウスでの操作は未確認。

### 2026-09-22 Shape Mask の選択時プレビューが「反転」に追従しない問題を修正

- ユーザー指摘。反転はマスク画像を作り直さない設計で Shape Mask のキャッシュキーに含めていないが、選択時プレビューの反転は評価結果（`previewMaskInvert`）に入っていたため、キャッシュが効くと古い値のままだった。`ApplyRockMaterial` で、プレビュー中のノードの現在の設定から反転を読むようにした。Apply Material / Displace / Subdivide 側はもともとノードの設定から読んでいる。
- 反転ありのシーンを開いた表示は確認したが、同じ起動中にチェックを切り替える操作（本来の再現手順）は実キー・実マウスでは未確認。

### 2026-09-22 Shape Mask にカーブ（ガンマ）

- ユーザー依頼により、下限/上限で伸ばした値に `^ ガンマ` を掛ける「カーブ（ガンマ）」を追加（0.1〜10、既定 1、対数スライダー）。画像を作る段階で掛けるので、プレビュー・ベイク・Displace・Subdivide のマスクに同じく効く。キャッシュキーと保存名 `shapeMask.gamma` を追加。旧シーンは 1 で読む。
- 単体テスト3項目（値の二乗になること、0 と 1 を保つこと、範囲外の拒否）。Release で全件成功。設定欄の表示を実画面で確認。

### 2026-09-22 ビューポートの統計表示の三角形数を修正

- ユーザー依頼で「Decimate が目標に届かない」「Subdivide の設定欄の前回入力が合わない」を調査。原因はどちらも、ビューポートの統計表示が**描画した三角形**を数えていたこと。岩はメインパスと影のカスケード4回の計5回描かれるので、統計は 5 倍（＋空の全画面三角形1枚）になっていた（4,000 面 → 20,001）。Decimate も設定欄も正しかった。
- 表示を「描画三角形（影を含む）」と「メッシュ三角形」（評価結果の合計）の2行に分けた。過去の記録のうち、この 5 倍の値で書いていた三角形数（Shape Mask のサンプル、128 解像度の比較、Decimate の「届かない」記述）を訂正した。2026-09-22 の Decimate 追加時の「目標 10,000 と 3,000 が同じ形（11,176枚）」の記録も同じ表示から読んだ可能性があるが、再検証はしていない。

### 2026-09-22 Subdivide の Mask 入力

- ユーザー依頼「ひび割れの内部まで細かく割ってしまうので、見える場所だけにかけたい」への対応。Subdivide に Mask 入力と「マスクのしきい値」を追加。面の中心のマスク値がしきい値以上の面だけを4分割し、隣の面は共有辺に合わせて2〜4分割して閉じたまま保つ（red-green）。選ばれなかった面の子は次の段でも割らない。Shape Mask（UV 画像）と Material Mask（定数 / 画像。UV / Triplanar）に対応し、反転も反映。キャッシュキーと保存/読込を更新。旧シーンの Subdivide は Mask 未接続として読める。
- 単体テスト7項目を追加（半分の面だけ割って閉包と面数、子の伝播、UV/チャートの保持、全黒で不変、しきい値、サイズ不一致の拒否、Mask ピン）。Release で全件成功。
- Release 実アプリ（`examples/shape-mask` を Decimate 2万面にした検証シーン）：入力 4,000 面、2段で一様なら 64,000 面のところ、反転したオクルージョンで 21,982 面、上向き度で 28,480 面（当初は統計表示の 5 倍の値で記録していた。次項参照）。Subdivide は形を変えないので、見た目ではなく面数で確認した。画面は `data/test/claude-session-2026-09-22/subdiv-mask/`。
- 「前回入力 4000面」と統計の 20,001 面の食い違い、Decimate が目標に届かない件は、どちらもビューポートの統計表示が原因だった（次項）。Decimate と設定欄は正しかった。

### 2026-09-22 To Volume の解像度上限を 128 へ

- ユーザー依頼により、To Volume の解像度を 16〜96 から 16〜128 へ、ボリューム系の内部格子の上限を各軸 192 点から 256 点へ引き上げた（回転・和・歪みで格子が広がる余裕は従来と同じ比）。検証・設定欄・診断文・テスト・資料を更新。96 の上限は密な配列の段階の設計上の値で、技術的な壁ではなかった。
- 実測（Release、`examples/volume-noise/` の形。To Volume → Plane Cuts → Volume Crack → Plane Cuts → Volume Noise → Dual Contouring）：96 は起動後1秒以内に表示、128 は1〜3秒。三角形数は 126,318 → 241,292（約1.9倍。統計表示の修正前は 5 倍の値で記録していた）。128 では割れ目の縁と小面のノイズがはっきりする（`data/test/claude-session-2026-09-22/res128/compare.png`）。
- 注意：Dual Contouring の 128 の出力は約24万三角形で、Subdivide の 100 万面上限には届かないが、1段割ると超える。To Volume の再入力上限 25 万にはぎりぎり収まる。下流は Decimate を前提にする。Marching Tetrahedra はさらに約3倍になる。密な配列のキャッシュはノードごとに格子を持つので、128 の鎖はメモリも約2.4倍（1格子 約10MB）。
- テストは 96 の最大設定（内部空洞の位相）を維持し、128 の閉包と格子サイズ、129 の拒否を追加。Release で全件成功。

### 2026-09-22 Shape Mask の向きを補間した頂点法線にする

- ユーザー指摘「UVがつながっていないところでマスクがぱっきり切れる」の画像を確認。切れ目は三角形の辺に沿っていて、原因は UV の境界ではなく、上向き度とオクルージョンの向きに面の法線をそのまま使っていたこと（隣の面と向きが違う辺で値が段になる）。
- 共有頂点で面積重み付き平均した頂点法線を画素ごとに補間して使うようにした。オクルージョンは半球の軸を補間した向きにし、面の平面より下へ向くレイは自己交差になるので数えない。レイの原点のオフセットは面の法線方向のまま。
- Release でテスト全件成功。サンプルの上向き度のプレビューで、三角形の形の斑が消えて面をまたいでなめらかにつながることを確認（`screens/shape-mask/direction-before-after.png`）。
- あわせて、前項の「無い .rockmat を外す」修正で `ProjectWorkspace` のテスト1件（「ID が見つからなければ失敗する」）が失敗していたのを、新しい振る舞い（項目を外して成功）に合わせた。前回はテストを再ビルドせずに実行していたため見落とした。

### 2026-09-22 アセットの Del キー削除と幽霊アセットの自動削除

- ユーザー依頼「アセットを Del キーで削除。実際にファイルが無くて幽霊のように残っているアセットも自動で削除」への対応。
- **Del キー**：アセット欄で選んだファイルを削除（退避）の対象にする。既存の「ファイルを削除…」と同じ検査 → 確認 → 実行の流れに、`m_assetDeleteQueue` で複数選択を1件ずつ流す。確認ダイアログに残り件数を出し、キャンセルで残りも止める。フォルダは対象外（右クリックメニューと同じ）。テクスチャ欄には以前から Del があった。
- **フォルダの自動更新**：`ProcessAssetWork` で1秒ごとに表示中のフォルダの `last_write_time` を見て、変わっていれば一覧を作り直す。エクスプローラで消したファイルが一覧に残らない。削除の検査で対象が既に無ければ、ダイアログを出さず一覧だけ直す。
- **リンク切れテクスチャ**：シーンを開いた直後に `PruneMissingTextures(false)` で、ファイルが無くどこからも使われていないテクスチャを自動で外す（ログに件数）。使われているものは割り当てが消えたことが分かるようリンク切れとして残し、テクスチャ欄の右クリックメニュー「リンク切れをすべて削除」で参照を外してまとめて消せる。削除の本体を `RemoveTextureNow` に切り出した。
- **開けないシーンの修正**：`data/Scenes/rock_crack.rockscene` が開けなくなった原因は、旧版（2026-09-22 より前）が自動保存した `Materials/Baked 94_*.rockmat` をシーンが参照したまま、ファイルが削除されていたこと。`ProjectWorkspace` は .rockmat が1つでも読めないとシーン全体を失敗にしていた。無いマテリアルは警告を出して表から外し、参照は「なし」へ落とすようにした。修正後は同シーンが開け、リンク切れの Bakes/ の画像 28 件も自動で外れた。
- Release でビルドと全テスト成功。Release 実アプリで、幽霊テクスチャ（`Textures/ghost.png`、未参照）を足したシーンを開くとログに「1 件外しました」と出て、保存したシーンの `textures` から消えることを確認。起動中に `dummy.png` を外部から削除し、一覧から自動で消えることを画面で確認（`data/test/claude-session-2026-09-22/screens/asset-delete/`）。Del キーの実操作と「リンク切れをすべて削除」の実操作は未確認。Debug ビルドは未実施。

### 2026-09-22 DecimateのUV保持

- UVを削除していた処理を変更。継ぎ目と島境界の頂点は固定し、島内部の縮約は位置とUVを同じ辺上の比率で補間する。UV反転と退化を避け、島IDとアトラス寸法を保持。目標以下の入力はUVを含めてそのまま返す。不正UVは診断する。
- Decimateで素材レイヤーとマスク画像を保持。複数枝の識別用履歴を引き継ぎ、変更前のベイクへの参照を無効化する。設定欄と仕様を更新。
- Releaseビルドと全テスト成功。実際の削減、UV島の面積・向き・寸法、平面での位置とUVの対応、Shape Maskで塗り分けた2素材の保持を検証。起動中のexeを避けて `build/uv-decimate/` へビルド。今回の変更の実画面での確認は未実施。
- 制限：継ぎ目の多いメッシュでは削減量が少なくなる。曲面・非線形UVでの歪みや、遠く離れたUV面どうしの重なりの検出・補正は保証しない。


### 2026-09-22 スクリーンスペースAO

- ライティングへ有効チェック、半径（0.001〜10m）、強さ（0〜3）を追加。既定はオフ。シーン保存・読込・新規リセットに対応。
- 深度から位置・法線を復元し、32点の近傍サンプルでGPU計算。HDRカラーへ乗算してから被写界深度・トーンマップへ渡す。シェーディングとクレイだけに適用し、背景・チャンネル表示・ベイクには掛けない。オフなら計算パスを省略。
- 初版は直接光と環境光を分離せず、シーンカラー全体を減衰する表示上の近似。画面外・裏面の遮蔽物、時間的蓄積、専用の平滑化パスは対象外。遠方の探索半径は128pxで制限する。
- Releaseビルドと全CTest成功。起動中のexeは置き換えず `build/ssao/rock_editor.exe` で検証。GPUシェーダのコンパイル、オン・オフの画面差、背景が同一であること、設定保存・再読込を確認。画像は `build/displace-validation/ssao-*.png`。実マウスによるチェックボックス操作は未確認。


### 2026-09-22 Shape Mask

- ユーザー依頼「マスクノードを作成。まずはオクルージョン」への対応。[設計メモ](../reference/rock-shaping-nodes.md) の 6 として **Shape Mask** を追加。入力は Mesh（UV付き）、出力は Mask で、Apply Material の Mask へつなぐ。種類はオクルージョン（溝・割れ目・入隅が白）/ 上向き度 / 高さ。設定はマスク解像度（128〜4096）、距離・サンプル数（オクルージョンのみ）、下限/上限、反転。保存名 `shapeMask`（種類は名前で保存）、右クリックメニュー（材質）、設定欄と進捗バー、保存/読込、Undo/Redo（既存スナップショット）に接続。仕様は [Shape Mask](../reference/shape-mask.md)、サンプルは `examples/shape-mask/`。
- **経緯**：最初は入力の無い Occlusion Mask とし、最終メッシュの頂点ごとに求めた値を頂点シェーダ経由で渡す形で作った。ユーザー指摘を受けて3点を改めた。(1) Mesh（UV付き）を明示的な入力にする。(2) オクルージョン専用ではなく、形状からマスクを作るノードの中で種類を選ぶ。(3) 選択時はマスクをテクスチャとして貼った状態でプレビューする。頂点マスクのためのシェーダ・頂点バッファ・`HeightSample` の変更は取り除いた（コミット前の作業内での差し替え）。
- **仕組み**：マスクは入力メッシュのUVに対応する1チャンネルの画像（`geometry::MaskImage`）。評価スレッドの CPU で、画素の表面の点ごとに値を求め（オクルージョンは形状AOの参照実装と同じ BVH とレイ。行ごとに並列）、島の無い画素を最も近い島の値で埋める。Apply Material が `GeneratedRock::maskImages` で下流へ運び、アプリが一時テクスチャにして既存の「画像マスク・UV」として渡す。**シェーダの変更は無く**、プレビュー・ベイク・Displace（CPU で画像をUVで読む）が同じマスクを使う。Field 型は入れていない。
- **選択時の表示**：Shape Mask を Mesh 系のプレビュー対象に加え、UV Unwrap のチェッカーと同じ仕組みで、選ぶとそのノードをプレビューし、外すと元へ戻す。評価結果は入力メッシュに `previewMask` を付けたもので、黒の全面＋マスクで白の2段の素材として描く。
- **キャッシュと整合**：Shape Mask をボリューム系と同じ内容キーのキャッシュへ追加（反転はキーに含めず、切り替えで再計算しない）。Apply Material より下流のキャッシュのキーに、つないだ Shape Mask のキーを含める（Displace は反転も）。ベイクの指紋には画像の画素を含める。UVの無いメッシュ・アトラス寸法の違うメッシュへの適用、Shape Mask 適用後の UV Unwrap は診断する。全面置換でマスク画像も外れる。一時テクスチャは画像ごとに1枚で、使われなくなったら `SyncMeshGraph` が捨て、プロジェクトの切り替えで一覧を捨てる。
- 単体テスト49項目を追加（`tests/ShapeMaskTests.cpp`）。壁の足元が 0.5、距離による減衰と打ち切り、再現性、下限/上限、反転が画像を変えないこと、解像度、島の外の埋め、上向き度と高さの値、画像の線形補間、不正な設定9種・UV無し・取消・進捗、ノード定義と型制約、未接続とUV無しの診断、選択時プレビュー用の結果、Apply Material への受け渡しと1回だけの計算、反転 / 種類 / 上流の変更、下流キャッシュの無効化、再UV展開・UV無しへの適用の診断、全面置換、Displace（高さのマスクで上だけ動く、反転がキーに入る、再利用）、Undo/Redo を確認。Release / Debug とも全2,755項目が成功（Debug はテストのみビルド。Debug のアプリ本体は未ビルド）。
- Release 実アプリ（`build/statusbar/rock_editor.exe`。通常の exe は起動中でロックされていた）でサンプルを開き、割れ目に錆色、上面に埃色が載ること、オクルージョンと上向き度の Shape Mask を `--select-node` で選ぶとプレビューが「Shape Mask」へ切り替わり白黒のマスクが貼られること、設定欄が種類に応じて変わることを確認。Material Bake を足した検証シーンをコマンドラインからベイク・出力し、「ベイク済み（UVで表示中）」の表示がベイク前と一致すること、`--save-project` で2つの `shapeMask` の設定と接続が残り、一時テクスチャが保存されないことを確認。画面は `data/test/claude-session-2026-09-22/screens/shape-mask/`。読み込みから表示まで約26秒（3万三角形の UV Unwrap が大半）。実マウスでの選択・選択解除・スライダー操作・メニュー・Undo/Redo の手動確認は未実施。
- 制限：UVが必要（先に Decimate → UV Unwrap）。オクルージョンは CPU 計算で、4096² や多サンプルは遅い。遮蔽物は入力メッシュだけ。曲率・切断面の種類、マスクどうしの合成、マスク画像の書き出しは無い。テクスチャ一覧に「Shape Mask（一時）」が出る。サンプルで Decimate の出力が 150,001 三角形に見えたのは統計表示の 5 倍の値で、実際は目標どおり 30,000 だった。

### 2026-09-22 詳細メッシュのキャッシュ

- Displaceを永続キャッシュへ追加し、ハイトの実値・投影・マスクをキーへ含めた。Displaceを経由するためキーが作れなかった下流Decimateも再利用できる。素材ハイトの準備キーをグラフ全体の改訂から参照入力と画像読込世代へ変更。
- 実計算回数を使う回帰テストで、同一入力・無関係な編集・同内容の別スナップショットの再利用、下流だけの変更、ハイト変更時の再計算、ハイト不足時に古い結果を返さないことを確認。

### 2026-09-22 Subdivide / Displace

- Apply Materialの後段へSubdivideとDisplaceを追加。Subdivideは形とコーナーUVを保持した共有辺の4分割（0〜6段）、面数予測と合計100万面上限を持つ。DisplaceはGPUで生成したリニアハイトを使い、CPUワーカーで共有頂点を変位する。変位量と基準ハイト、UV/Triplanar投影、画像/定数マスク、Heightチャンネル、ノイズに対応。
- UVの継ぎ目は角度重み付きでハイトを平均し、共有頂点を一度だけ動かす。表示のスムーズ設定には依存しない。材質とUVを引き継ぎ、古いベイクと直方体の高速経路を無効化。素材の有効/無効を細分化キャッシュへ反映し、複数枝も保持する。保存・再読込・Undoと取消に対応。
- Release/Debugビルド、全CTest（約9秒/180秒）が成功。最後の入力検証とキャッシュ修正後はRelease全CTestとDebugの `rock_editor_tests.exe --displace-only` が成功。閉包・体積・UV・実距離・中立値・継ぎ目・マスク・チャンネル・上限・取消・Undo・材質変更/無効化・複数枝を検証。
- Release実アプリで画像ハイトによる輪郭変化、変位量0との比較、画像マスク、UVを先に作る接続、保存/再読込を確認。変位後のUV展開→形状AOベイク→PNG4枚の出力も成功。検証画像は `build/displace-validation/`。資料は[Subdivide / Displace](../reference/displace.md)、サンプルは `examples/displace/`。
- 制限：共有頂点でない重複点の溶接、自己交差修復、Model型への直接適用は対象外。素材のGPU準備は同期。実マウスによるノードの追加・配線操作は未確認（ノード定義と接続は単体テスト、設定欄は実画面で確認）。

### 2026-09-22 Scatter Points の点を 2D で描く

- ユーザー依頼により、Scatter Points のプレビューを八面体のメッシュから、ビューポートに ImGui で重ねる 2D の点（半径 3px・縁取り付きの丸）に変更。`PreparePiecePreview` は点をメッシュにしなくなり、`RockEvaluation::points` を `Application::m_pointPreview` に控えて `DrawPointPreview` が描く。奥行きでは隠さないので、形の内側の点も見える。
- Release でコンパイルとテスト全項目が成功。起動中のアプリが exe を掴んでいたため `rock_editor.exe` のリンクは未完了で、実画面では未確認。

### 2026-09-22 Normal もエッジパディングにする

- ユーザー判断により、ベイクした Normal も島の縁の向きを全面へ伸ばす（Base Color などと同じ `FillBakeBackground`）。既定の法線色（128, 128, 255）で埋めるのは、島が1つも無い画像のときだけ。余白の幅を上流の UV Unwrap から調べる処理は不要になったので削除した。
- Release/Debug ビルドと全2,680項目が成功。法線マップを持つ `examples/uv-bake` をベイクして出力し、`Normal.png`（1024×1024）のアルファが全面 255 で、空きが島の縁の向きで埋まっていることを確認（`data/test/claude-session-2026-09-22/screens/bake-tiles/normal-padded.png`）。

### 2026-09-22 ベイク画像のエッジパディング

- ユーザー指摘「アルベドやラフネスでも島以外が黒なのはよくない。色を伸ばす感じに」への対応。`renderer::FillBakeBackground` を追加。既存の幅優先の塗り広げ（`DilateBakePixels`）を、埋める画素が無くなるまで続ける。各画素は縦横の歩数で最も近い島の色になり、全面が不透明になる。島が1つも無い画像は変えない。
- Base Color / Roughness・Metallic・AO / Height は全面を埋める。Normal は前項のとおり、余白の幅だけ伸ばした外を既定の法線色（128, 128, 255）で埋める。
- 単体テスト4項目を追加（全面が不透明になること、最も近い島の色になること、島の中を変えないこと、島の無い画像を変えないこと）。Release/Debug ビルドと全2,680項目が成功。Release 実アプリでベイクして出力し、4枚ともアルファが全面 255 で、空きが島の縁の色で埋まっていることを確認（`data/test/claude-session-2026-09-22/screens/bake-tiles/padding-before-after.png`。上段が変更前、下段が変更後）。

### 2026-09-22 Material Bake の結果を一時的なものにし、出力ボタンを追加

- ユーザー指摘「勝手に保存されるのではなく、出力ボタンを押して出力したい。ノードに表示するテクスチャは一時保存のものに」への対応。これまでベイクは PNG 4枚をルートの `Bakes/` へ書き、プロジェクトのテクスチャとして読み直し、結果の材質をシーン保存時に共有アセット（`.rockmat`）として書き出していた。
- **結果はメモリ上にだけ持つ。** `TextureLibrary::AddTransient` で画像から直接 GPU テクスチャを作り、`LibraryTexture::transient` / `MaterialAsset::transient` の印を付ける。ファイルを持たず（パスは空）、シーンの `textures` / `materials`・共有アセットの保存から除く。Material Bake ノードは、結果の材質を書けないとき `layer` と `fingerprint` も書かない（指紋だけ残すと、結果が無いのに「ベイク済み」と判定されるため）。開き直すと「再ベイクが必要です」になる。
- **「テクスチャを出力…」ボタン**を設定欄に追加。フォルダを選ぶと `BaseColor.png` / `Normal.png` / `RoughnessMetallicAO.png` / `Height.png` を書き出し、完了を通知する（通知から出力先を開ける）。出力できるのは、この起動中にベイクした結果だけ。画像は `m_bakeImages` に持ち、プロジェクトの切り替えで捨てる。
- **Normal の島以外の部分**を、既定の法線色（128, 128, 255）の不透明で埋める。以前は色だけ入れてアルファが 0 のままだったので、画像として見ると透明（タイルでは黒）だった。
- 旧版が `Bakes/` へ保存した結果（一時でない材質）を持つシーンは、従来どおり読み込み・保存できる。設定欄には、もう一度ベイクすると出力できる旨を出す。
- 開発用の引数 `--export-bake <フォルダ>` を追加（`--bake-node` のベイクが終わったら、ダイアログなしで出力する）。
- Release/Debug ビルドと全2,676項目が成功。Release 実アプリで確認：ベイク後にルートへ `Bakes/` も `Materials/` も作られないこと、`--export-bake` で4枚が出力されること、出力した `Normal.png` の島以外の画素が (128,128,255,255) であること、保存したシーンのベイクノードに `layer` / `fingerprint` が無く一時のテクスチャ・材質が書かれていないこと、開き直すと再ベイク待ちになり出力ボタンが無効になること、旧版のベイク済みシーンが読めてタイルが出ること。画面は `data/test/claude-session-2026-09-22/screens/bake-tiles/`。実マウスでのボタン操作とフォルダ選択ダイアログは未確認。
- 注意：再ベイクのたびに、前の一時テクスチャと材質は Undo のために残す。同じ起動中に何度もベイクすると、その分の VRAM を使い続ける（プロジェクトを開き直すと消える）。

### 2026-09-22 Material Bake の結果をノードの設定欄に表示

- ユーザー依頼により、Material Bake の設定欄へ「ベイク結果」の6枚のタイルを追加（Base Color / Normal / Roughness / Metallic / AO / Height）。ベイクが作る材質（`bakedLayer.material`）から各スロットのテクスチャとチャンネルを引く。Roughness・Metallic・AO は1枚の画像の R / G / B へ詰めて焼いているので、テクスチャライブラリが持つ「1チャンネルだけを灰色で描く SRV」で分けて出す。新しい GPU リソースは作らない。
- タイルは欄の幅に合わせて3列か2列。UV の島が無い部分は RGB の画像では透明、1チャンネルの表示では黒になるので、同じ黒の下地を敷いて揃える。ホバーでファイル名、クリックでテクスチャプレビューの窓を同じ画像・同じチャンネルで開く。下に寸法と保存先を出す。
- Release ビルド成功。`examples/material-layers` をコマンドラインからベイクし、ベイク直後と、保存→再読み込み後の両方で6枚が出ることを実画面で確認（`data/test/claude-session-2026-09-22/screens/bake-tiles/`）。実マウスでのクリック（プレビューの窓が開くこと）は未確認。
- グラフのノード自体にサムネイルを出すこと（Surface ノードと同じ形）はしていない。

### 2026-09-22 Decimate

- UV Unwrap が遅い問題（前項）への対応として、ユーザー判断により **Decimate** を自前実装で追加。QEM による辺の縮約で、外部ライブラリは使わない。入力・出力とも Mesh 型。目標の三角形数（既定 10,000）、形のずれの上限（最長辺に対する比、既定 0.004）、稜線の保護を持つ。保存名 `decimate`、右クリックメニュー、設定欄（実際の出力の三角形数と、目標に届かなかった旨を表示）、保存/読込、Undo/Redo（既存スナップショット）、枝のキャッシュ、別スレッドでの評価・進捗表示・キャンセルに接続。仕様は [Decimate](../reference/decimate.md)。
- 縮約のたびにリンク条件・向かいの頂点の面数・面の裏返り・細長い三角形を検査し、閉じた多様体と連結成分の数を保つ。出力は `InspectMesh` で検証してから返す。
- **実測**（Release、6.4万三角形）：Decimate は約0.1秒。UV Unwrap は、減らさないと17.0秒、2万で3.6秒、1万で1.2秒、5千で0.6秒。
- **テストと実測で3点直した。** (1) 平らな面ではどの縮約も誤差が 0 で順序が決まらず、針のような三角形ができて UV が縮退した。優先順位へ辺の長さの項を足し（形のずれの判定には含めない）、細長い三角形を作る縮約を断る検査を足した。(2) その検査が厳しすぎて、もとから細長い三角形の多い Marching Tetrahedra のメッシュが 73,312 → 71,770 しか減らなかった。どの検査で断られたかを数えて原因を特定し、「形の良かった場所に細長い三角形を作らない」へ絞った。Marching Tetrahedra も 1,000 まで減り、UV 展開も通る。(3) 実画面で目標 10,000 と 3,000 が同じ形（11,176枚）になった。形のずれの上限で止まっていたためで、設定欄にその旨を出すようにした。
- 単体テスト40項目を追加（`tests/DecimateTests.cpp`）。Release/Debug ビルドと全2,676項目が成功。Release 実アプリで、元のメッシュ・約1.1万枚・3千枚（上限なし）の形を見比べ、塊の構成と割れ目が残ることを確認（`data/test/claude-session-2026-09-22/screens/decimate/`）。実マウスでの確認は未実施。
- 制限：細部は消える。戻すには High → Low の法線ベイク（未実装）が要る。UV は引き継がないので UV Unwrap の前に置く。目標は上限であり保証ではない。

### 2026-09-22 評価中のノードの表示と、UV Unwrap の非同期化

- ユーザー指摘「UV Unwrap も計算中なのを分かりやすく」への対応。評価スレッドが、いま計算しているノード・段階・進み具合を `RockEvaluationProgress`（atomic の組）へ書き、UI が読む。表示は3か所。ステータスバーの「更新中（UV Unwrap: 島へ分割中・12秒）…」、グラフのノード見出しの橙色の「計算中」、UV Unwrap の設定欄の進捗バー。ノード名の表示は全ノード共通で、To Volume などで待っているときも分かる。
- **UV Unwrap だけのグラフは、これまで UI スレッドで展開していた**（非同期にする条件が「ピース系か To Volume を含む」だったため）。展開中はアプリが固まり、表示も出せなかった。条件へ UV Unwrap を足した。
- `UnwrapMesh` に `std::stop_token` と進捗の通知を追加。xatlas の進捗コールバックから段階を受け取り、`false` を返して打ち切る。打ち切った不完全な結果は使わない。設定のスライダーを動かしたときや終了時に、展開を最後まで待たなくなる。
- **実測して表示を直した。** 6.4万三角形（`data/test/…/debug/uv_timing.cpp`）で全体約16秒。最初の約11秒は「島へ分割」の前半で、xatlas が進捗を通知しない（0% のまま、打ち切りも効かない）。後半の「島を配置」は収まるまで最大8回詰め直し、重なりの修復で最初からやり直すので、百分率が何度も 0 へ戻る。百分率をそのまま出すと誤解を招くので、段階名と経過秒数を基本にし、百分率は島への分割の後半だけに出す。進み具合が分からない間、設定欄のバーは左右に動く表示にする。
- xatlas は連結成分ごとに並列化する。岩は1つの連結成分なので、島への分割は実質1スレッドで走る。島の成長は優先度付きキューによる逐次処理で、GPU 化には向かない。速くする現実的な手段は、展開前の三角形の削減（Decimate）か、面の向きで分ける簡易展開の追加。
- 単体テスト6項目を追加（段階と進み具合の通知、通知の有無で結果が変わらないこと、開始前と途中のキャンセル、評価後に計算中のノードが無しへ戻ること）。Release/Debug ビルドと全2,636項目が成功。Release 実アプリで、展開中の3か所の表示を確認（`data/test/claude-session-2026-09-22/screens/uv-progress/`）。実マウスでの確認は未実施。

### 2026-09-22 Volume Noise

- [設計メモ](../reference/rock-shaping-nodes.md) の4番目（Volume Displace / Noise）として **Volume Noise** を追加。距離場へ `d + 量 × ノイズ(0〜1)` を足して表面を削る。削る方向に限るので形は広がらず、外周は空のまま保たれる。種類は、なめらか（値ノイズ）、セル状（Worley F1）、小面（最も近い特徴点ごとのランダムな平面。セルの中は平らで境が段差になる）。重ねる数 1〜5。歪み（domain warp）はサンプル位置を3本の値ノイズでずらし、平面と直線的な割れ目を波打たせる。歪みがあると、表面が動く量だけ格子を広げる。量と歪みは形の最長辺に対する比。保存名 `volumeNoise`、右クリックメニュー、設定欄、保存/読込、Undo/Redo（既存スナップショット）、ボリューム枝のキャッシュに接続。仕様は [Volume Noise](../reference/volume-noise.md)、サンプルは `examples/volume-noise/`。
- 表面から「歪み × 1.75 + 量 + 2セル」より離れた格子点は結果が変わらないので計算を省く。浮いた小片の除去（Plane Cuts）と、新しくできた閉じた空洞の埋め戻し（Volume Crack）を共通の関数にして使う。
- テストの前に実画面で3種類を見比べた。面取り → 割れ目 → 稜線の欠けの後ろに置くと、歪みが割れ目と面取りの直線を波打たせ、小面のノイズが平らな面を割れ肌にする。ここまでの4ノードで、形は参考画像の Macro と Meso の層にかなり近づいた。色と Micro（材質マスク、焼き込み）は未着手。
- 単体テスト54項目を追加（`tests/VolumeNoiseTests.cpp`）。量と歪みが 0 のときの恒等、3種類それぞれの閉包・1成分・格子の不変・形を広げないこと・削る深さと体積・再現性、重ねる数、最も細かく深い設定で小片と空洞を残さないこと、歪みの体積・格子の拡張・動く量・平面が波打つこと、上限と不正設定の拒否、グラフの型制約とキャッシュを確認。Release/Debug ビルドと全2,630項目が成功。
- Release 実アプリでサンプルの3種類の表示、設定欄、`--save-project` による保存で設定と9本の接続が残ることを確認。検証は `build/statusbar/rock_editor.exe`。実マウスでのメニュー操作と Undo/Redo の手動確認は未実施。
- 制限：セル間隔より細かい凹凸は格子で潰れる（目安は細かさ ≤ 解像度 ÷ 4）。歪みは境界をセル1個ぶん程度なまらせる。盛り上げる方向のノイズと段化（Terrace）は無い。面の向きや曲率で量を変えることはできない（Field 型が要る）。

### 2026-09-22 Volume Crack

- [設計メモ](../reference/rock-shaping-nodes.md) の3番目として **Volume Crack** を追加。入力は Volume と Points（Scatter Points の出力）。点が作る Voronoi の境界面までの厳密な距離（垂直二等分面までの距離の最小）で溝を彫る。断面は表面で最も広く、指定の深さで閉じる V 字。割れ目ごとの幅のばらつき（点の対から決める固定の倍率。一部は閉じる）と、割れ目に沿ったゆらぎ（3D の値ノイズ。途切れる）を持つ。幅と深さは形の最長辺に対する比。格子は入力のまま。保存名 `volumeCrack`、右クリックメニュー、設定欄、保存/読込、Undo/Redo（既存スナップショット）、ボリューム枝のキャッシュ（Volume 側と Points 側の両方の上流をキーに含む）に接続。仕様は [Volume Crack](../reference/volume-crack.md)、サンプルは `examples/volume-crack/`。
- Plane Cuts で分かった「重なった立体のボリュームは内部に残る面の近くでも距離が小さい」問題は、ここでは内部の空洞として現れる。彫ったあとで格子の外周から外部をたどり、届かない彫り跡を入力の値へ戻す。入力にもとからある空洞は残す。浮いた塊は除かない（深さ 1 で形を分けるのは意図した使い方）。
- Scatter Points は凸な Mesh を必要とするので、凸でない形に割れ目を入れるときは、形を囲む Base Shape の Box に点を散らす。点は形の外にあってもよい。この使い方をサンプルと仕様に書いた。
- 単体テスト42項目を追加（`tests/VolumeCrackTests.cpp`）。溝の位置と深さ、V 字、対称、格子の不変、離れた点の完全一致、深さ 1 での分割、幅 0 の恒等、再現性、ばらつき・ゆらぎ、形の外の点、内部の空洞を作らないこと、不正な設定と点の拒否、グラフの型制約・診断・キャッシュを確認。Release/Debug ビルドと全2,576項目が成功。
- Release 実アプリでサンプル（面取り → 割れ目 → 稜線の欠け）を開き、割れ目が複数の面をまたいで連続し、太さに差があり、途中で消えるものがあることを確認。設定欄と、`--save-project` による保存で設定と8本の接続が残ることを確認。検証は `build/statusbar/rock_editor.exe`。実マウスでのメニュー操作と Undo/Redo の手動確認は未実施。
- 制限：セル間隔より細い割れ目は格子で潰れる（幅 0.03 は解像度96で約3セル）。割れ目は平面の組み合わせで直線的。方向性を付ける設定は無い。512点・各軸192点では重い。

### 2026-09-22 Plane Cuts

- [設計メモ](../reference/rock-shaping-nodes.md) の2番目として **Plane Cuts** を追加。平面の群で Volume を切り落とす。枚数・Seed・適用範囲（全体 / 局所）・半径・深さの最小と最大・法線の分布（等方 / 主方向1〜3系統、向き、ばらつき）・なめらかさを持つ。長さの設定は形に対する比で持ち、格子は入力のまま。保存名 `planeCuts`、右クリックメニュー、設定欄、保存/読込、Undo/Redo（既存スナップショット）、ボリューム枝のキャッシュに接続。仕様は [Plane Cuts](../reference/plane-cuts.md)、サンプルは `examples/plane-cuts/`。
- **実画面を見て設計を直した。** (1) 全体に効く半空間だけでは、枚数を増やすと凸包へ近づき、直方体の塊の凹凸が消えた。表面の点まわりの球の中だけを切る「局所」を追加した。(2) 局所の中心が形の内側に選ばれ、空洞と破片ができた。重なった立体のボリュームは内部に残る面の近くでも距離が小さいためで、表面の点を「隣に外部の点を持つ内部点」で選ぶよう直した。(3) 球の壁が丸いくぼみとして残った。切断面を斜めに持ち上げる案は上方の形まで削るため取りやめ、中心を凸な点に限り、法線を表面の外向きへ寄せ、切り取る材料が球の縁に掛かる割合で欠けを選別する方式にした（稜線と角は通し、平らな面のくぼみは除く）。浮いた小片は最大の塊だけを残して除く。
- 単体テスト58項目を追加（`tests/PlaneCutsTests.cpp`）。球冠の体積、格子の不変、形を広げないこと、影響のない点の完全一致、再現性、枚数追加時の既存平面の不変、等方の偏りのなさ、主方向の円錐と系統、不正設定の拒否、局所の中心・法線・深さ、十字形での全体と局所の差、浮いた小片の除去、グラフの型制約とキャッシュを確認。Release/Debug ビルドと全2,534項目が成功。
- Release 実アプリでサンプルを開き、切り落とす前・全体5枚・局所256枚指定の3段の形、設定欄、`--save-project` による保存で2ノードの全設定と5本の接続が残ることを確認。検証は `build/statusbar/rock_editor.exe`。実マウスでのメニュー操作と Undo/Redo の手動確認は未実施。
- 制限：局所で実際に使われた枚数は表示していない。局所の欠けは稜線と角に集まり、平らな面の中央は欠けない（Volume Displace / Noise の役割）。距離値は符号が正しい近似。

### 2026-09-22 設計メモと Volume Boolean

- ユーザー提示の参考画像（`docs/references/thumbnail_bea6868d-…​.webp`、Git 対象外）のような岩を作るための処理を整理し、[岩らしい形と色のためのノード設計メモ](../reference/rock-shaping-nodes.md) に残した。候補は Volume Boolean、Plane Cuts、Volume Crack、Volume Displace / Noise、Volume Smooth、形状からのマスク、ディテールの焼き込み。概念として Field（スカラー場）型を挙げた。着手順は Boolean → Plane Cuts → Crack。
- その1番目として **Volume Boolean** を追加。A / B の Volume 入力から和・交差・差を取り、なめらかさ（0〜10 m）でつなぎ目を丸める。結果は A の格子を引き継ぎ、A の値は補間せずに使う。和は B を含む範囲まで格子を延ばし、交差は重なる範囲へ狭める。Z スライス単位の並列処理とボリューム枝のキャッシュに対応。保存名 `volumeBoolean`、右クリックメニュー、設定欄、保存/読込、Undo/Redo（既存スナップショット）に接続。仕様と制限は [Volume Boolean](../reference/volume-boolean.md)、サンプルは `examples/volume-boolean/`。
- 単体テスト36項目を追加（`tests/VolumeBooleanTests.cpp`）。体積 8.5 / 0.5 / 7.5 m³、格子の広がり方、A の値の完全一致、再現性、なめらかさ、離れた B、上限・不正設定の診断、グラフの型制約・未接続の診断・キャッシュを確認。Release/Debug ビルドと全2,476項目が成功。
- Release 実アプリでサンプルを開き、差・なめらかな和・交差の3つの形、ノード選択時の設定欄と説明文を確認。`--save-project` による保存で `volumeBoolean` の設定と7本の接続が残ることを確認。通常の実行ファイルは起動中だったため、検証は `build/statusbar/rock_editor.exe`。実マウスでのメニュー操作と Undo/Redo の手動確認は未実施。
- 制限：結果の距離値は符号が正しい近似で、後続のオフセット・平滑化には距離の再計算が要る。細部は A のセル間隔まで。なめらかさ 0 のつなぎ目は Dual Contouring でもセル程度にギザつくことがある。

### 2026-09-22 更新中の表示をステータスバーへ移動

- ユーザー指定により、非同期評価中の「更新中…前回の結果を表示しています」をビューポート左上からステータスバー左端へ移した。「マテリアルを評価中…」と同じ並びで、評価が終われば消える。ピース設定欄の案内文は残す。
- Releaseビルド成功。`examples/material-layers` の起動直後のフレームでステータスバーに表示され、ビューポート上に出ないことを実画面で確認。起動中の通常Releaseはロックされていたため、検証用は `build/statusbar/rock_editor.exe`。通常の実行ファイルへの反映はアプリ終了後の再ビルドが必要。

### 2026-09-22 全体レビューによる修正と最適化

- **形状コアの高速化（出力はビット単位で不変）**：To Volumeの一般メッシュ経路を(Y,Z)の行単位で並列化。BVHは子の順序付けに使った箱距離を再利用し、隣の格子点の最近距離から探索の上限を決める。Random Boxesのボリューム化とVolume TransformはZスライス単位、Voronoi Fractureは片単位で並列化した。診断・三角形数の上限・体積の合計はID順にまとめ、直列処理と同じ結果・同じエラー文を返す。`InspectMesh` の辺照合は要素ごとに確保しない開番地法の表へ変更。Marching Tetrahedraの交点表は事前確保する。
- 旧コードと新コードを同じ入力で比べる検証用プログラムで、To Volume（楕円体＋ノイズ12,288三角形・解像度96、Dual Contouringの53,892三角形の再ボリューム化、原点から500 m離れた入力）、Random Boxes 32個・解像度96、Volume Transform、Marching Tetrahedra / Dual Contouring、Voronoi 24/232/512点×2 Seed（方向付き伸長を含む）の全出力のハッシュが一致。Release実測は To Volume 5,774→187 ms、再ボリューム化 10,472→359 ms、Random Boxes 90→9 ms、Volume Transform 64→9 ms、Voronoi 512点 281→19 ms。単体テストのVolumeドラッグ相当は19.0→9.8 ms/更新、Releaseの全テストは13.5→約4.5秒。
- **キャッシュ**：Apply Materialを通るとボリューム枝のキーが作れず、下流のTo Volumeを毎回作り直していた。材質・マスクは接続だけをキーへ含め、形状のキーを下流へ通す。
- **Scatter → Voronoi**：Scatterは倍精度の点で、Voronoiはfloatへ丸めた保存値で内外を調べていた。Scatterでも保存値をVoronoiと同じ許容差で確かめる。従来の判定は残したので、既存Seedの結果は変わらない（上の一致検証に含む）。
- **Volume Transform**：移動量に対してセルが小さく、floatの格子座標が隣どうしで潰れる設定を診断する（To Volumeと同じ考え方）。
- **アプリ**：評価タスクの例外を生成エラーとして表示。終了時にタスクを止めて待つ。選択ノードだけが変わったときは実行中の評価を止めず、完了後のキャッシュで新しい選択を評価する。ベイク状態の `HintText` へ書式文字列として渡していた不具合を修正。テクスチャ削除で不透明度の参照を外し、使用箇所の一覧・件数へ不透明度とMaterial Maskを追加。文書の切り替えでグラフのクリップボードを捨てる。AO結果とベイク画像の寸法を照合。設定ファイルは一時ファイル経由で保存。`projectVersion` のないシーンを版4として拒否していた既定値を1へ修正。
- **描画**：Apply Material使用時に捨てられていた基本材質の読み取り（Triplanarなら9回の異方性サンプル）を省略。マスク抜きの `discard` がある場合は従来どおり読む。`examples/material-layers` の実画面を変更前後のシェーダで撮り、ビューポートが画素単位で一致。生成メッシュの頂点配列の事前確保、UV面積0の面のNaN接線の回避、`UploadMeshScene` のシーン全体の複製1回分を削減。StructuredBufferをCOMMONで作成し、DebugのD3D12検証の警告を解消。
- **ビルド**：`/MP` を追加。`rock_sources` の `RockMesh.cpp` の二重登録を削除。
- Release/Debugビルドが警告の追加なしで成功し、単体テスト2,440項目が両構成で成功（Apply Material下流のキャッシュ4件、遠方のScatter→Voronoi 1件を追加）。`--test-gpu-ao` の実GPU比較も成功。Release実アプリで `examples/material-layers` と `examples/voronoi-pieces` の読み込み・表示を確認。実マウスでの操作確認、Debug実画面でのD3D12警告の消滅確認は未実施。
- レビューで見つけたが今回は直していない項目は「未完了」の「レビューの持ち越し」に記録。

### 形状AOのGPU化・進捗表示

- 形状AOのレイ判定をDX12 Computeへ移行。空間/UV BVHはCPUで構築し、GPUは128×64画素のタイルをフレーム間に分割して処理する。完了タイル数による進捗表示とキャンセル、入力変更時の破棄、適用前の指紋照合を追加。専用DXR機能は不要。
- 実GPUとCPU参照実装の比較で、平面・遮蔽・距離・強度・8/32/128サンプルの最大差は8bit値で1。タイル端・進捗単調増加・部分実行の破棄/再利用を開発用 `--test-gpu-ao` で検証。512のSDFメッシュのAOベイク画像は従来CPU版と画素単位で一致。Release実アプリで2048/128サンプルの完了・進捗画面、キャンセル、保存再読込を確認。Debugの2048実画面検証は長時間化したため中止し、実GPU比較テストとビルド・CTestで検証した。

- Release/DebugビルドとCTest成功（約15秒/131秒）。実GPU参照比較も両構成で成功。DebugのD3D12検証でエラーなし（共通StructuredBuffer生成によるCOPY_DEST初期状態の警告は残る）。

### Apply Material・マスク合成・形状AO

- Apply Material（Mesh / Material / Mask → Mesh）とMaterial Mask（定数・画像R、反転、UV/Triplanar）を追加。マスク省略は全面置換、接続時は最大8段の素材合成。Surfaceごとの投影を保持し、UV UnwrapとMaterial Bakeへ引き継ぐ。旧Output/BakeのMaterial入力は全面置換として互換維持。
- Material Bakeに距離・強度・サンプル数を持つ形状AOを追加。UV画素ごとのBVHレイ判定で素材AOへ乗算し、BaseColorは保持する。マスク画像・各段の投影・AO設定をベイク指紋へ含める。保存・再読込・Undoに対応。
- Release/Debugビルドと全CTest成功（約13秒/107秒）。マスク型、全面置換、素材順序、UV/Bake継承、8段上限、無効Surface、Undo/Redo、AOの遮蔽・距離・強度・再現性を検証。実アプリで黒/白/50%と画像マスクのプレビュー・ベイクを確認。黒/白/50%のBaseColor中央値はそれぞれ[202,38,38]/[38,38,202]/[150,38,150]。AOは素材の上限値を維持し、遮蔽で暗くなることを確認。保存再読込でマスク・AO・指紋と接続を維持し、AO距離変更で再ベイク待ちになることを画面確認。配布サンプルを別ルートにコピーして読み込み・ベイク成功。
- 詳細と制限は[素材の適用と形状AO](../reference/material-application.md)。サンプルは `examples/material-layers/`。

### 2026-09-21 Dual Contouringの共有面接続修正

- sample.rockscene相当（Box 2×3×2、232点・Seed 1、内部79片、解像度64/96）で、共有面の2本の輪郭が同一の辺になり4面が接続する問題を再現。輪郭ごとの面上中間点を共有して別の接続を保つよう修正した。
- 別方式へ切り替えず、Dual ContouringのQEFを維持。再現条件とセルの全255非空符号配置を回帰テストへ追加。Release/Debugのビルドと全CTest（約13秒/111秒）、実際のdata/Scenes/sample.rocksceneを設定変更せずに開いたSDFプレビュー表示を確認。通常のRelease実行ファイルへScatter上限拡張も含めて反映済み。

### 2026-09-21 Scatter Points上限拡張

- 点数の上限を128から512へ拡張。UIとScatter/Voronoiの検証に共通定数を使用し、既定24点と既存Seedの生成規則は維持する。点数を増やすほど分割処理の負荷は増える。
- 最大点数での分割・各片の閉包・体積保存と、上限超過の拒否を回帰テストへ追加。

- Releaseの全CTest成功。起動中アプリによる通常exeのロックのため、アプリ本体は `build/scatter-preview/rock_editor.exe` へ別出力してビルド成功。通常のexeへの反映はアプリ終了後に再ビルドが必要。

### 2026-09-21 Mesh入力のTo Volume

- Random BoxesをMesh出力、To VolumeをMesh入力へ統一。Base ShapeやPieces to Meshから閉じたメッシュをボリューム化できる。直方体の高速経路を維持し、一般メッシュのBVH距離計算、凹形状・穴・空洞・重複・分離ピース、非同期評価と上流キャッシュ無効化に対応。旧シーンの接続は維持する。
- 開いたメッシュ、逆向き外殻、無効設定を診断。自己交差修復は対象外。重複面を含む内部距離は近似となる。一般メッシュは25万三角形・100万頂点まで。
- 解析Boxとの全格子点比較、凹形状、トーラス、内殻、重複・接触・分離した立体、UV接続、Voronoiセルの再ボリューム化とキャッシュの回帰テストを追加。

- Release/Debugビルドと全CTestが成功。実アプリでBase ShapeとPieces to Meshからの変換表示を確認。旧Random Boxesシーンを読み込み・再保存し、接続IDが維持されることも確認した。

### 2026-09-21 Voronoi分割とピース操作（V1〜V4初版）

- [設計書](../reference/voronoi-pieces.md)を追加。Scatter Points / Voronoi Fracture / Piece Select / Piece Filter / Piece Transform / Pieces to Meshの6ノード、ピースID・選択の有効性、方向性のある分割、Box面を指定した外周除去、保存/Undo、キャッシュと非同期評価を定義した。
- 初版はBoxを基準とする凸立体で、分割→外側除去→一部抽出→個別操作→UV/ベイクを完成させる。凹形状や空洞を含む一般メッシュの対応は次段階。6ノードを実装済み。サンプルは `examples/voronoi-pieces/`。
- Base Rockの表示名をBase Shapeへ変更。ノードタイトル、追加メニュー、エラー表示を更新し、保存名 `baseRock` と設定は維持した。Releaseビルド成功。旧保存名のシーンを読み、画面にBase Shapeが表示され、再保存でも `baseRock` が維持されることを確認。

- `geometry/Pieces` は正規化した倍精度座標の凸クリップを使用。同一辺の交点共有と境界閉路によるキャップ生成を行い、floatメッシュの閉包・正体積・体積保存を確認してから公開する。2〜128点、伸長比16以下、入力2万/出力25万三角形まで。
- Points / Pieces / Selectionを独立型として追加。Piece Selectは手動ID、Box外面、重心範囲、体積、ランダムに対応。Keep/Deleteを接続済みで追加できる。Piece Transformは全体/各片の重心、IDごとの変換、W/E/Rギズモを持つ。
- 分割ノードID・生成世代・点IDで片を識別。上流変更で手動選択/個別変換が古くなればリセットを案内する。Selectionは枝と配置にも結び付け、別の入力への誤適用を防ぐ。JSONの64bit世代は文字列で保存。コピーした分割ノードへの内部参照も更新する。
- Scatter/Voronoiを入力内容でキャッシュし、Filterは参照、Transformは行列を更新。CPU評価はグラフのスナップショットを非同期処理し、取消と世代照合で古い結果を棄却する。GPUへの転送はメインスレッド。更新待ちは前回表示と明記し、選択とベイクを止める。
- Release画面で元Box、32分割、外面の除去、一部抽出、UVチェッカー、PNG4枚のベイク、保存再読込を確認。CLI入力注入で移動ギズモとEsc取消、手動クリック選択、Shiftの追加/解除、空白の解除を検証。検証用実行ファイルと画面は `build/uv-bake/`、`build/uv-bake/validation/voronoi-*.png`。
- 自動試験で複数Seed、方向付き伸長、最大128片、薄いBox、最小/最大寸法、辺/頂点上の切断、断面の面積と向き、最近点条件による所属・非重複、凹形状/重複点の拒否、選択の世代不一致、キャッシュ再利用、UV接続、個別XYZ変換、JSON往復を確認。

- 最終コードでDebug/Releaseビルドが警告なしで成功。CTestは両構成とも全件成功（Debug約40秒、Release約8秒）。通常の `build/bin/Release/rock_editor.exe` にも反映済み。サンプル一式を独立した検証ルートへコピーして、読み込みとベイクを確認した。

**初版の制約**：凹形状・空洞・自己交差の修復は未対応。Outerの6方向は軸に沿ったBox面を対象とする。Pieces to Meshは接触面を残す単純結合。ギズモの倍率は均等、XYZ別倍率は数値欄。実マウスによる全機能の通し操作は未検証。

### 2026-09-21 Joint Set / Crack / Fracture ノードを削除

- ボリューム研究の方針に合わせ、ユーザー判断で3ノードを削除。グラフ定義・評価器・保存/読込・プロパティ UI・右クリックメニュー、`src/crack/`・`src/fracture/`・亀裂/Bridge ガイド描画、Fracture の Chunk 選択・色分け・ギズモ、関連テスト6本を取り除いた。
- 列挙値 35～37 は再利用しない。旧シーンのこれらのノードは未知の種類として捨て、つながっていたリンクも読み込み時に除く。Base Rock は残す。
- Debug ビルド（アプリ・テスト）が警告なしで成功し、単体テストはすべて成功。実画面での確認はまだ行っていない。

### 2026-09-21 UV解像度を2のべき乗へ統一

- UV Unwrapの解像度を128 / 256 / 512 / 1024 / 2048 / 4096の選択式に変更。既定値は1024。
- 旧シーンの任意解像度は次の2のべき乗へ切り上げて読み込む。生成処理でも非対応値を拒否し、解像度変更後は既存の指紋比較で再ベイクを案内する。
- Releaseビルドと単体テストが成功。実画面の選択欄と、旧シーンの600が1024へ変換されて保存されることを確認。

### 2026-09-21 英数字付きUVチェッカーと通常の照明

- 参考画像に合わせた16×16マスの2048×2048 PNGを `assets/textures/uv_checker.png` に追加。続く指定でHoudini形式の配置へ変更し、明るいマスに行名A〜P、暗いマスに列番号1〜16を表示する。再生成スクリプトは `tools/generate_uv_checker.py`。CMakeで実行ファイル横へ同梱する。
- チェッカーを照明付きの拡散色として表示し、天球・影・露出・トーンマッピング・被写界深度の処理を共通化。シーンの材質やベイクへ混ぜず、チェック終了後は元の表示設定へ戻る。
- Debug/Releaseビルド、Python構文確認、Release実画面で文字と陰影を確認。同じカメラの通常表示と背景領域が画素単位で一致し、保存したグラフ・材質・プレビュー設定も一致。ISO 100→400でチェッカーにも露出が反映された。実行ファイルは `build/uv-bake/rock_editor.exe`。

### 2026-09-21 自動UV展開・UVビュー・材質ベイク

- 前段の生成メッシュ材質・Triplanar対応を `6b4ac2e` にコミット。その後、ユーザー指定の `UV Unwrap → Material Bake` を追加。xatlasで生成メッシュを1枚のUVへ展開し、選択時のチェッカーと、ビューポート横の「UVビュー」タブで確認できる。
- ベイクはSurface材質をUVへ手動転写し、色・DirectX法線・Roughness/Metallic/AO・Heightの4枚のPNGを保存して適用する。入力変更時は元材質へ戻し再ベイクを案内する。保存/読込・Undo/Redoへ接続した。
- SDF由来の微小面を展開できるよう座標を正規化し、面積のあるUV重なりを検査・修復する。両メッシュ変換方式のUV、形状不変、決定性、キャッシュ、材質優先順、Undo/Redo、余白のテストが成功。
- Debug/Releaseビルド、Release単体テスト、Debug CTest（1/1、48.34秒）が成功。実画面のベイクボタン、チェッカー選択/解除、UVタブ、512/1024出力、形状・投影・余白変更時の再ベイク待ちを確認。保存→再読み込みで指紋とビューポート画像が一致した。
- サンプルは `examples/uv-bake/`、仕様・制約は [UVとベイク](../reference/uv-bake.md)。実行ファイルは `build/uv-bake/rock_editor.exe`。起動中の通常Release版は置き換えていない。初版は同期処理・8bit PNGで、UVの手動編集・High/Low転写・メッシュ書き出しは含まない。

### 2026-09-21 生成メッシュへのSurface材質とTriplanar

- Mesh OutputにMaterial入力を追加。Surfaceの材質を生成メッシュへ渡し、UV／Triplanarを選べる。Triplanarはワールド座標基準で、サイズ・位置・回転・混合の鋭さを調整。Surface単位で保存・Undo/Redoする。
- Base Color／Normal／Roughness／Metallic／AOを接続。法線の投影方向を戻して勾配を合成する。Heightはチャンネル表示のみ。UV展開・ベイク・Height変位・ローカル座標追従は未対応。モデル材質は既存のモデル側設定を使う。
- Debug/Releaseビルド、Release単体テスト、Debug CTest（1/1、52.99秒）が成功。実画面で材質表示、背面の投影、UI切り替え、旧シーン互換、保存往復を確認。サンプルの別フォルダへのコピーも読み込み・表示できた。詳細は [仕様と検証](../reference/triplanar.md)。サンプルは `examples/triplanar/`。
- 実行ファイルは `build/triplanar/rock_editor.exe`。起動中のアプリは置き換えていない。

### 2026-09-21 SDFプレビュー方式の共通設定

- 「プレビュー設定」に共通の変換方式を追加。Volume の直接表示に適用し、明示的な Volume to Mesh の出力方式とは独立させた。アプリ設定に保存し、シーンや Undo 履歴は変更しない。
- 表示方式を外皮キャッシュの判定に含め、内部SDFは再利用する。To Volume / Volume Transform / Mesh Output への直接接続、方式の往復、明示的な変換結果への非干渉を回帰テストで確認。
- Debug/Releaseビルド、Release単体テスト、Debug CTest（1/1）が成功。隔離したアプリ設定でDual Contouring表示と、画面の既定値ボタンによるMarching Tetrahedraへの即時切り替え・設定保存を確認。表示三角形数は72,161から222,561へ変化した。
- 実行ファイルは `build/sdf-preview/rock_editor.exe`。検証画像は同フォルダの `validation/`。起動中の通常Releaseは置き換えていない。

**2026-09-21 のユーザー指定により、しばらくこのリポジトリを「岩からボリュームを研究する場所」として使う。** 見た目の完成を急がず、ボリューム（SDF の格子）上の操作を増やして試す。現状の表現は密な配列に持つ SDF で、OpenVDB は使っていない。

`Random Boxes → To Volume → Volume Transform → Volume to Mesh → Mesh Output` が通る。重なる直方体の塊を Seed で作り、実体のある3次元グリッドへ変換し、格子のまま動かして外皮を表示できる。

**参考写真のような外観は未達成。** これは形を作る出発点であり、亀裂や溝の機能数を外観の進捗とは扱わない。溝の拡張は保留。次の調整はこの塊の実画面を見て判断する。

| 区分 | 現在の状態 |
| --- | --- |
| アプリ基盤 | DX12 / ImGui、モデル表示、グラフ編集、素材・アセット・保存基盤あり |
| 岩生成 | Base Shape（Box / RoundedBox / Sphere / Ellipsoid、丸み、分割数、弱いノイズ、Seed）、Random Boxes、Scatter Points / Voronoi Fracture とピースの選別・個別変換に対応。Joint Set / Crack / Fracture は2026-09-21に削除済み |
| メッシュ接続 | RockEvaluator → RockMesh → SyncMeshGraph で表示。Merge と途中プレビューに対応。Volume to Mesh は Marching Tetrahedra / Dual Contouring を選択可能。Remesh で三角形を一様に作り直し、Decimate で減らせる |
| 評価・保存 | Revision ごとの再評価、寸法と seed の保存/復元、Undo/Redo に対応。ボリューム系の枝キャッシュに対応。他の枝のキャッシュは P6 |
| ボリューム | 密な配列の SDF。Volume Transform で移動・回転・拡大。格子の再サンプルで実装。ビューポートのギズモで操作できる。Volume Boolean で2つのボリュームの和・交差・差を取れる。Plane Cuts で平面の群による面取りと欠けを作れる。Volume Crack で点の群の境界に沿う割れ目を彫れる。Volume Noise で表面を削り、歪みで直線的な面を崩せる。Volume Terrace で層状の段を刻み、Volume Smooth で面の向きに応じてなまらせる / 角を立てられる。Volume Close で割れ目の奥など外から見えない隙間を埋められる（遮蔽 / 幅） |
| 材質 | Apply Material / Material Mask / Shape Mask（オクルージョン・上向き度・高さ）/ Mask Combine（マスクの合成）、Triplanar、UV Unwrap、Material Bake（GPU形状AO）に対応 |
| 次の作業 | 形を作る4ノード、Volume Terrace、Volume Smooth、Shape Mask、Mask Combine が入った。[設計メモ](../reference/rock-shaping-nodes.md) の順では、Shape Mask の種類の追加（曲率 / 切断面）とディテールの焼き込みが候補 |

## 完了

### 2026-09-21 Volume to Mesh に Dual Contouring を追加

- 「変換方式」で Marching Tetrahedra（既定・従来方式）と Dual Contouring を選択する。設定・保存・読込・Undo/Redoに接続。方式をキャッシュキーへ含め、切り替え時も上流SDFを再利用する。設定のない旧シーンは従来方式。
- Dual Contouring は入力SDFの格子から交点・法線を推定し、セル内に制約したQEFで頂点を求める。セル内で分離した表面には別頂点を配置し、曖昧な面の接続を両セルで揃える。最終的な閉包・向き・体積の検証は共通処理。詳細と制約は [メッシュ変換方式](../reference/volume-meshing.md)。
- Debug/Releaseビルド成功、Debug CTest（1/1、38.16秒）、Release単体テスト全項目成功。複数Seed・最大32箱/解像度96・内部空洞・斜めBox・球・球殻・決定性・不正入力・方式変更・キャッシュ・Undo/Redoを検証。
- Release実画面で両方式と設定欄を表示し、Dual Contouringの保存→再読込→再保存でグラフが一致。解像度48の同じ変換サンプルで描画統計の三角形数222,561→72,161。実マウスによるメニュー操作の通し確認は未実施。比較画像・保存ファイルは `build/dual-contouring/validation/`。
- 比較用シーンは `examples/volume-meshing/`。実行ファイルは `build/dual-contouring/rock_editor.exe`。起動中の通常Release版は置き換えていない。
- 格子で失われた細部や再サンプル誤差は残る。角が完全な直線になる保証はなく、セル程度の小さな穴・接続のトポロジーは方式間で変わる場合がある。適応格子・簡略化・一般的な自己交差検出は未対応。

### 2026-09-21 スムーズシェーディングとワイヤーフレームの削除

- 岩メッシュの表示が面法線だけだったので、「表示」メニューに**スムーズシェーディング（折れ角 40 度）**を足した。オフなら従来と同じ面法線。
- `MakeRockMeshData` に折れ角つきの頂点法線の平均を実装。頂点を使う面を CSR で持ち、その面と40度以内で向きが揃う面だけを面積で重み付けして平均する。直方体の90度の角は分かれ、面法線のまま残る。
- 平均した法線とは辺の向きが直交しなくなるため、接線をグラム・シュミットで張り直す。これを入れる前は `ValidateMeshScene` の直交条件で弾かれ、メッシュが表示されなかった。
- 表示の属性なのでシーンには保存せず、アプリ設定（`settings.json` の `display.smoothShading`）に持つ。法線は頂点に焼くので、切り替えると `SyncMeshGraph` がメッシュを作り直す。
- ワイヤーフレーム表示（分割前 / 分割後）を削除。道路エディタ由来で、テセレーションを使わない岩メッシュでは分割前後の区別に意味がない。メニュー・`DisplaySettings`・`PreviewRenderer` の分岐と `SceneMesh::showWireframe`・`PsWireframe` シェーダまで取り除いた。
- Debug ビルド、CTest（1/1、34.90秒）、単体テスト全項目が成功。`rock_editor_tests` に `src/renderer/RockMesh.cpp` を足し、法線の単位長・接線の直交・球で大半の法線が変わること・直方体の角が分かれること・描画へ渡せることを検査する。
- 実画面で、同じシーンのフラットとスムーズを描き比べて確認（`build/volume-transform-validation/shots/smooth-compare.png`）。格子由来の段差の陰影は消え、輪郭の段差は残る。
- 形は変えていないので、輪郭の階段は解像度・平滑化・抽出法の課題として残る。メニューの実マウス操作と Release の実行ファイルでの確認は未実施。

### 2026-09-21 Volume Transform の回転時の格子上限を修正

- Y軸回転で表示が消える報告を受けて調査。最大解像度96では余白追加だけでも旧上限102点を超える場合があった。保存済み `sample.rockscene` の解像度64・直方体11個の設定では、保存済みの回転にYを15度加えると107×98×104点となり、同じ上限に達する。報告時の未保存状態そのものは未取得。
- 内部格子の上限を各軸192点へ変更し、入力検証・変換・表面抽出で統一。解像度や距離値を粗くする回避は行わず、割り当て前の上限検査を維持。連続変換で上限を超えた場合は引き続き診断する。
- ノード設定欄だけでなくビューポート下部にも生成エラーを表示し、設定欄が閉じていても消えた理由が分かるようにした。
- Debug/Releaseビルド、Debug CTest（1/1、38.46秒）、Release単体テスト全項目が成功。解像度96の0/1/15/45/90/-45/180/360度・複合回転、保存済み設定とY15度追加、精度・閉包・体積・上限拒否を検証。
- 修正版Releaseは `build/volume-optimized/rock_editor.exe`。起動中の `build/bin/Release/rock_editor.exe` は旧版のまま。前回のキャッシュ最適化も修正版に含む。
- Release実画面で、保存済み設定のY回転を15度増やしたメッシュの表示と、未接続入力のエラーがビューポート下部に出ることを確認。元シーンは変更せず、検証コピーと画像を `build/rotation-fix-validation/` に保存した。

### 2026-09-21 Volume Transform の評価負荷を削減

- アプリが保持する `RockEvaluationCache` を追加。Random Boxes / To Volume / Volume Transform / Volume to Mesh の枝について、設定値と上流接続が同じなら結果を再利用する。Volume の直接プレビューも同じグリッドの外皮を再利用する。
- 上流の変更・接続変更・削除・Undoによる復元は、改版番号だけに頼らず内容で判定する。各ノードの最新結果だけを保持し、削除済み・変更済みの結果は破棄する。ボリューム以外の永続キャッシュは未対応。
- `InspectMesh` の辺照合を64bitの頂点IDペアとハッシュ表へ変更。閉包・向き・連結性・体積・不正値の検証条件を維持する。
- Debug ビルドと CTest（1/1、16.52秒）、別出力先の Release ビルドと単体テスト全項目が成功。キャッシュなしとの頂点・三角形の一致、上流SDFの再利用、回転・移動・解像度・Seed・接続変更・Undo相当の復元・上流削除・直接プレビューを確認。
- Release、解像度48、移動と回転の連続6更新でCPU評価を比較。最適化前相当23.76ms/更新、上流キャッシュのみ21.08ms、辺照合の改善後はキャッシュ込み15.38ms（約35%減）。短い実測の参考値で、UI・描画データへの変換・GPU転送を含まない。
- 起動中の通常Release版は置き換えていない。最適化版は `build/volume-optimized/rock_editor.exe`、検証ログは同フォルダの `tests-release.log`。ドラッグ中の低解像度化や遅延確定は導入しておらず、変更したボリュームの再サンプルとメッシュ生成は引き続き行う。
- 最適化版Releaseでサンプルの起動・描画を確認。画像は `build/volume-optimized/validation/preview.png`。実マウスによるドラッグの体感確認は未実施。

### 2026-09-21 Volume Transform のギズモ操作

- Volume Transform を選ぶと、Model / Transform / Chunk と同じギズモをビューポートに出す。W 移動 / E 回転 / R 倍率、Ctrl の刻み、ドラッグ中の Esc で取り消し。
- ギズモの中心は移動量の位置。ボリュームは倍率 → 回転 → 移動の順に原点まわりで動かすので、その点が回転・倍率の中心にもなる。
- 既存の `NodeTransform` / `NodeGizmoFrame` に分岐を足して実装。`NodeTransformRef` に「形を作り直す設定か」を示す `regenerate` を追加し、Chunk と同じくドラッグ中にグラフを改版して再評価する（Model / Transform は従来どおり改版しない）。
- 表示中の岩がこのノードを上流に持つときだけギズモを出す（`RockMeshUsesNode`）。ビューポートの Delete によるノード削除はモデルの系統のノードに限定した。
- Debug ビルド、CTest（1/1、20.53秒）、単体テスト全項目が成功。CLI 入力注入で移動・回転・倍率のドラッグを実行し、移動 X=1.76 / 回転 Z 方向の変化 / 倍率3.71 が設定へ入り、保存ファイルに残ることを確認。Esc 付きのドラッグでは設定が変わらないことも確認。
- ドラッグ中は毎フレーム上流から再評価するため、解像度を上げると重い。実マウスによる通し操作と Undo/Redo の手動確認、Release の実行ファイルでの確認は未実施。検証素材は `build/volume-transform-validation/shots/`。

### 2026-09-21 Volume Transform とメニューの型別整理

- Volume 入力を、倍率 → 回転 → 平行移動の順に原点まわりで動かして Volume を返す `Volume Transform` を追加。回転は右手系 Z → X → Y で Crack / Model と同じ規約。
- 格子を作り直す実装。出力の各点を逆変換して入力をトリリニア補間で読み、倍率を掛け戻して距離の単位を保つ。格子の外は外周の正値へ外に出た距離を足し、符号を外部のまま保つ。
- セル間隔を倍率に比例させ、拡大・縮小でセル数を変えない。範囲は入力格子の8隅の AABB + 2セル。回転で各軸102を超える場合は上流の解像度を下げるよう診断する。0倍率・非有限値・空の格子・内部が残らない場合も診断する。
- グラフの右クリックメニューを型別に再編。「メッシュ（ポリゴン）」「ボリューム」「モデル」「共通」「材質」の見出しで分け、変換ノードは変換後の型の側に置く（Volume to Mesh はメッシュ側、Random Boxes はボリュームの素材としてボリューム側）。
- 保存名 `volumeTransform`、プロパティ UI、保存/読込、Undo/Redo に接続。サンプルは `examples/volume-transform/`。
- Debug / Release ビルドと CTest（1/1、21.14秒）、単体テスト全項目が成功。識別変換・移動・45度回転・倍率2/0.5・連続適用の体積と外接箱、再現性、上限と不正設定の診断、型の接続制約、評価とプレビューへの伝播、Undo/Redo を確認。
- Debug の実行ファイルで、移動のみ・回転のみ・縮小のみのシーンを描画し、形を保ったまま位置・向き・大きさだけが変わることを目視確認。保存→読込→再保存でグラフが一致。検証素材は `build/volume-transform-validation/`。
- 再サンプルのため、回転では境界がセル1個ぶん程度なまる。軸ごとの倍率、平面を境に片側だけずらす操作、実マウスによる右クリックメニューの通し操作は未対応・未検証。**Release の `rock_editor.exe` は起動中のため再リンクしていない。**

### 2026-09-21 Volume to Mesh

- Volume 入力からゼロ等値面を取り出し、通常の Mesh 型を出力する独立ノードを追加。追加設定はなく、解像度は上流の To Volume で調整する。
- To Volume のグリッド生成とメッシュ抽出を分離。旧シーンの Volume 直接表示は最終プレビュー段階で外皮を抽出して維持する。
- 右クリックメニュー・プロパティ説明・保存名 `volumeToMesh`・Undo/Redo・4ノードのサンプルを接続。
- Debug / Release ビルド、CTest（1/1、19.49秒）が成功。Release で4ノードの表示・変換結果・説明欄を確認し、保存→再読込→再保存のグラフが一致。検証素材は `build/volume-to-mesh-validation/`。
- 通常の Release（`build/bin/Release/rock_editor.exe`）へ反映済み。その実行ファイルで4ノードのサンプルの表示・保存を確認。


### 2026-09-21 Random Boxes と To Volume

- 直方体集合を作る Random Boxes、内外の符号を持つ密な3次元グリッドを作る To Volume を追加。Boxes / Volume を独立したピン型にし、Mesh Output でそれぞれプレビューできる。
- Box の個数・寸法・サイズばらつき・配置の広がり・回転幅・Seed、ボリュームの解像度を編集。追加 Box は最初の Box と必ず重なる。グリッドは和集合を評価し、内部の重複面を出力しない。空洞の壁は体積の向きで外皮と区別して保持する。
- 設定の保存/読込と既存スナップショットによる Undo/Redo に接続。例は `examples/random-boxes/random-boxes.rockscene`、手順は [直方体とボリューム](../reference/box-volume.md)。
- Debug / Release ビルド、CTest（1/1、19.19秒）が成功。Seed の再現、Box の重なり、和集合の体積、閉包、最大個数/解像度、内部空洞、型の接続制約、Undo/Redo を確認。
- CLI 入力注入で設定欄を操作し、個数8→20、解像度48→65の独立した変更と再生成・保存を確認。実マウスによる手動通し操作と Debug 描画は未検証。
- Release で変換前後、別 Seed、回転0、解像度16/48、3視点を確認。保存→読込→再保存のグラフ一致を確認。素材は `build/box-volume-validation/`、実行ファイルは `build/box-volume-release/`。
- 境界は格子解像度による近似で、表示は面法線。細かな角張りが残る。極端に薄い寸法は解像度不足を診断する。任意 Mesh の変換、VDB、平滑化は未対応。岩らしい完成外観を得たという評価ではない。


### 2026-09-21 曲面の有限長部分溝（試作・保留）

- ユーザーの外観優先の指示により、この試作の追加拡張は保留。今回のコミットに試作として保存し、今後の優先作業にはしない。
- 前回の全幅部分切断を `a90f992` にコミット。今回は「Mesh 有限溝」へ拡張し、U 半幅で溝の長さを指定できるようにした。
- U 両端の最大2平面を追加し、長さの終端壁も生成。断面の平均点が全境界辺の内側なら扇形で三角形化し、該当しない断面は従来の ear clipping を使う。セル結合後の開いた辺に残る T 字接続を分割して閉じる。
- Debug / Release ビルドと CTest（1/1、10.08秒）が成功。4形状×軸方向/斜め×全幅/有限長、ノイズ付き3072三角形、決定性、体積、両端/片端/非交差の解析解、端面の向き、グラフと Undo/Redo、従来の完全分割を確認。
- Release で4形状の斜め有限溝、片端が外面へ抜ける溝、ノイズ付き軸方向の有限溝を目視確認。有限長設定の保存と、楕円体シーンの保存→読込→再保存でグラフが完全一致。
- ノイズ付き楕円体（寸法2/2.5/1.7、分割16、強度0.08、Seed17、回転12/25/8）は中間 tip 平面の数値検証で拒否。壊れたメッシュは出力せず診断する。任意設定での成功を保証する段階ではない。
- 検証素材は `build/p5-finite-validation/`（拒否例も保存）、実行ファイルは `build/p5-finite-release/`。実マウスの通し操作と Debug 描画は未検証。

**残る範囲**：不安定断面の改善、交差する複数亀裂、曲面の Bridge 計測、複数ループ/穴、一般的な自己交差修復。P5 全体は未完了。

### 2026-09-21 曲面と任意角度の全幅部分溝（P5 限定実装）

- `crack/MeshCut` と Crack の「Mesh 全幅溝」を追加。U 全幅と +V 側外面を覆うパッチから、有限深さの溝を作る。部分切断オン時だけ形状へ適用する。
- 亀裂幅の両側と終端の3平面で母岩を区分し、溝セルと内部区切り面を除き、外皮・両壁・終端を結合する。未破断部を残す閉じた1連結体と体積を検証する。
- 未加工の母岩1個・切り込み1回・4096三角形以下。ゼロ幅/深さ・非交差は入力を維持する。曲面の Bridge 断面値を矩形近似で代用せず、計測/表示は未対応と明示する。
- 旧シーンは Box 専用方式を維持。Mesh 全幅溝の設定を保存/読込と Undo/Redo へ接続。
- Debug / Release ビルドと CTest が成功（1/1、8.76秒）。4形状の軸方向/斜め切断、3072三角形のノイズ付き母岩、決定性、体積保存、残存部の完全分割、無変更条件と不正設定、Undo/Redo を確認。
- Release で4形状とノイズ付き母岩の溝の壁・終端、ゼロ幅、深すぎる設定の診断を目視確認。切断設定の保存と、保存→読込→再保存のグラフ一致を確認。
- 検証素材は `build/p5-slot-validation/`、実行ファイルは `build/p5-slot-release/`。実マウスでの通し操作と Debug 描画は未検証。

**残る範囲**：U 方向も途中で止まる有限長溝、交差する複数亀裂、曲面の Bridge 断面計測/表示、穴/複数ループの中間切断、一般的な自己交差修復。P5 全体は未完了。

### 2026-09-21 Joint Set による多片の完全分割（P5 限定実装）

- `fracture/MultiSplit` と Fracture の「Joint Set で分割」を追加。非表示でも保持する上流の節理定義を、無限平面列として適用する。旧シーン/既定は従来の単一平面モード。
- 非交差/接触で片を維持し、同一/逆法線の重複面は最初の定義を使う。各切断は閉じた1連結体と単純な断面ループ1本を条件とし、極小片・多ループ・不正閉包は全体を診断する。
- 最大32平面・128片・20万三角形。母岩比1e-7以下の片を拒否し、最終体積和を相対1e-4で確認する。
- 節理ノード ID/パッチ番号と各側の組で片の変換キーを生成。色分け、ドロップダウン/クリック選択、Locked、移動/回転、保存/Undo に対応。最終片の共有断面・面積と元の節理を保持する。
- Debug / Release ビルド、CTest が成功。直交3平面の8片/12共有断面、平行列、曲面3形状と弱いノイズ、複数 Seed、32平面の33片、128片超過、再現性、体積、Undo/Redo を確認。
- Release で曲面/Box の9片、分離配置、ばらつき、ガイド非表示、上限エラー、旧単一平面を目視確認。分離配置の保存→読込→再保存でグラフが一致。
- CLI 入力注入で9片目のクリック選択とギズモ表示、移動/回転、Esc 取消、Locked の操作抑止を確認。移動/回転は対象キー1つだけが変わり、取消/選択のみ/Locked 時は保存グラフが一致。実マウスによる通し操作と Debug 描画は未検証。
- 検証素材は `build/p5-multi-validation/`、実行ファイルは `build/p5-multi-release/`。


**残る範囲**：曲面/交差パッチの部分切断、穴/複数ループ、再帰破砕、continuity/roughness。平面集合やコピーによる節理 ID の変更でキーが変わる場合、古い片の設定は残すが適用せず、新しい片は Locked の既定値にする。自動対応付け/選択の永続化は P6。

### 2026-09-20 Joint Set の有限パッチ列（P5 後半の表示基盤）

- `crack/JointSet` と Joint Set ノードを追加。1～64本のパッチを基準法線方向に配置し、位置とローカル U/V 軸の傾きを固定ハッシュと Seed で再現する。
- 設定パネルで中心・回転・間隔・位置/角度ばらつき・オフセット・本数・Seed・半幅・深さ・Persistence・Aperture・表示を編集。保存/読込と Undo/Redo に接続。
- 複数系統の直列接続、途中プレビュー、合流時のパッチ単位の重複排除に対応。ガイド表示は母岩の実形状を変更しない。
- 向き/配置、決定性、最大本数とばらつき、有限範囲、不正設定、母岩不変、直列/合流、Undo/Redo の自動テストが成功。Debug / Release ビルドと CTest が成功。
- Release で1系統/2系統、表示切替、Seed 違い、設定省略時の既定値、不正本数の診断を目視確認。保存→読込→再保存で全設定と接続が一致。検証素材は `build/p5-joint-validation/`、実行ファイルは `build/p5-joint-release/`。実マウスによる通し操作と Debug の描画確認は未実施。

**残る範囲**：Joint Set による実切断・多片分割、continuity/roughness、曲面/交差部分亀裂。Fracture は Joint Set のパッチ列をまだ使わず、従来の独立した単一平面で分割する。

### 2026-09-20 母岩形状と弱い BaseNoise（P5 前半）

- `geometry/BaseRock` に4形状と共通設定を追加。曲面は cube surface の共有頂点を投影して生成し、Sphere は直径、Ellipsoid は各軸寸法、RoundedBox は最短半辺に対する丸みで指定する。
- 面分割数は4/8/16/32（最大12,288三角形）。ノイズなしの Box と丸み0の RoundedBox は従来の Box メッシュをそのまま使う。
- seed を使った固定ハッシュと補間によるノイズを、原点からの正の放射方向変位として加える。強度0～0.15、細かさ0.5～4。強度0では Seed によらない。
- 設定パネル、保存/読込、Undo/Redo とグラフへ接続。旧シーンは Box・ノイズ0として読み込む。不明形状/不正設定は診断する。
- 全形状・全分割数の閉包、連結性、外向き面、頂点共有数、指定寸法、球/楕円体の解析曲面と体積、最小/最大寸法、最大ノイズ・丸み、Seed の再現性と変位上限を自動検証。
- 曲面の中央/斜め平面による分割、ノイズ付き母岩の分割、体積保存、曲面への部分切断拒否、Undo/Redo を検証。Debug / Release ビルドと CTest が成功。
- Release で Sphere / Ellipsoid / RoundedBox / ノイズ / Seed 違い / 斜め分割 / 旧 Box / 不明形状の画面を確認。ノイズと分割設定の保存→読込→再保存を検証。素材は `build/p5-validation/`、実行ファイルは `build/p5-release/`。

**残る範囲**：Joint Set・多片分割・交差亀裂を含む P5 全体は未完了。P3 の部分切断は引き続きノイズなし Box 専用。曲面は三角形近似で、表示は面ごとの法線。写真の主稜線・細かな亀裂・剥離・材質はこの段階で完成とはしない。


### 2026-09-20 単一平面での完全分割と Chunk 操作（P4 限定実装）

- `fracture/PlaneSplit` と Fracture ノードを追加。共有交点で両側をクリップし、断面の境界頂点を保持した三角形化で閉包を作る。両側が閉じた1連結体、体積が正、体積和が一致することを検証する。
- 入力は未分割の岩1個。単純断面1本を対象とし、任意角度の平面、Box の頂点/辺を通る平面、P3 の切り込みを含む凹断面に対応。Crack から渡された形状を分割し、Depth を自動的に完全貫通へ変更しない。
- `(ノード ID, 負側1/正側2)` の Chunk ID、親の生成元、分割面の中心・法線・面積を保持。再帰分割・複数ループ/穴・面の重なり・極小片は診断する。
- 青/茶で色分けし、クリックまたは設定欄で片を選ぶ。Locked を解除すると数値欄と W/E の移動/回転ギズモで編集できる。Locked は現在の配置を保つフラグ。物理計算はしない。
- 片の回転は分割直後の外接箱中心が基準。大きな移動でも体積積分が安定するよう基準点を変更し、変換精度が不足するときは診断する。
- 設定・変換・Locked を JSON に保存し、生成 ID を再構築する。Undo/Redo は既存スナップショットへ統合。選択中の片番号自体は UI の一時状態で、保存しない。
- Debug / Release ビルドと CTest、160組の Box 寸法・斜め平面、最小/最大寸法、辺/頂点上、法線反転、非交差、極小片、部分亀裂からの分割、断面法線・面積・体積、Merge と Undo/Redo を検証。
- Release の CLI 入力注入で、片のクリック選択、移動ギズモ、回転ギズモ、Esc による取消、Locked 中の操作抑止を確認。実マウスでの手動通し操作とは区別する。
- Box / 部分切断 / 有限長の溝 / 斜め平面 / 非交差の画面、設定の保存・再読み込みを検証。検証素材は `build/p4-validation/`、実行ファイルは `build/p4-release/`。
- ユーザー提示の `DSC02801.JPG` を外観の参考として整理。細かな分岐亀裂・剥離・材質の調整は現 MVP の機能完成と別に検証する。

**残る制約**：外部の任意メッシュの自己交差検出・修復、穴/複数断面、再帰分割、複数片一括変換、倍率・抽出・削除、選択の保存、永続的な評価キャッシュ。分割平面自体の操作は数値欄。ドラッグ中は小さなメッシュを再評価・転送する。Debug の実画面確認と手動操作は未実施。


### 2026-09-20 Box の部分切断と Rock Bridge（P3 限定実装）

- `crack/PartialCut` に、未加工 Box 1個と軸に沿う単一パッチの実切断を追加。Crack の「部分切断 (Box)」で有効にする。旧シーンは既定で無効。
- 切り込みの境界平面で Box を厳密に分割し、側壁・終端・外面を閉じた1連結体として生成する。均等 voxel や形状の解像度近似は使わない。
- +V 側の外面から入る切り込みに限定。任意角度・内部空洞・複数切断・複数 Box・Bridge 消失・開口が両側面へ抜ける設定は診断する。幅/深さゼロ・非交差は元の Box を保つ。
- 実切込深さ、未破断部の厚さと断面積を表示。Bridge の断面は緑の透視ガイドとして表示でき、亀裂の候補ガイドとは別に切り替えられる。
- 上流からノードごとに評価し、加工前と加工後の枝を別の結果として保持。評価1回の中で共有上流を再利用する。切断設定・Bridge 表示は保存/読込と Undo/Redo に対応。
- Debug / Release ビルド、既存テストを含む CTest 成功。160組の寸法・範囲・深さ、64組の90度回転、最小/最大 Box、薄い Bridge を検証した。
- 自動検証は閉包、1連結成分、各頂点周囲の面のつながり、除去体積、全三角形が固体境界にあること、側壁・終端の存在、描画データ、非破壊な枝、Undo/Redo を確認。
- Release の画面で全幅の溝、有限長の溝、Bridge 表示、90度回転、幅ゼロ、貫通時の診断、旧P2のガイド表示を確認。保存→再読込→再保存で設定と接続が一致。
- 検証素材は `build/p3-validation/`、検証用 Release は `build/p3-release/`。既存 data/ は変更していない。

**残る制約**：任意角度・一般メッシュ・複数交差への汎用切断処理、taper、粗さ、一般メッシュの自己交差検出器は未実装。P3 の対応範囲では直交分割で面の交差を避ける。専用ギズモと実マウス操作による通し検証は引き続き未対応/未実施。


### 2026-09-20 有限亀裂パッチの表示（P2）

- `crack/CrackPatch` に中心・回転・U/V 半幅・Depth・Persistence・Aperture と座標系の生成・検証を追加。
- `Base Rock → Crack → Mesh Output` で候補矩形（青）と到達矩形（橙）を半透明で透視表示する。母岩のトポロジーは変更しない。
- 設定パネルで位置・回転・各パラメータを編集し、ガイドを非表示にできる。表示深さは `min(depth, 2 * extentV) * persistence`。これは P2 の可視化規約で、母岩表面との交差や未破断部の判定はまだ行わない。
- 設定と表示フラグの保存/読込、Undo/Redo、連続した Crack、途中プレビューに対応。入力欠落・不正パラメータは診断し、古いガイドを消す。
- Debug / Release ビルド、既存と新規の CTest が成功。パッチの回転・範囲・深さの境界、開口幅、透視用描画データ、母岩不変、表示切り替え、Undo/Redo を検証。
- Release で通常の到達範囲、Persistence 0、全範囲到達、ガイド非表示を撮影して確認。実アプリの保存→読込→再保存で全パラメータと接続が一致。
- 検証素材は `build/p2-validation/`、検証用 Release は `build/p2-release/`。既存 data/ は変更しない。
- 位置・回転の編集は数値欄。パッチ専用のビューポート選択・ギズモ、およびマウス操作による通し検証は未対応/未実施。


### 2026-09-20 Base Rock の Box（P0/P1）

- `geometry/Mesh` に共有頂点の CPU メッシュ、Box、面法線、AABB・体積・閉包・連結性の検証を追加。
- Base Rock を右クリックメニューに追加。原点中心の Box を Mesh Output / Merge / 途中プレビューから表示する。
- 寸法は各軸 0.001～1000 m。Seed は保存するが Box 形状には影響しない。寸法不正時はノード ID を含む診断を出す。
- `ProjectIo` に設定の保存/読込を追加し、既存 Undo/Redo のスナップショットに統合。
- Debug ビルド、既存と新規テストを含む CTest が成功。テストは形状・不正入力・描画変換・接続・再評価・Undo/Redo を確認。
- Release 版で 3×4×5 m、Seed 123 の Box と設定パネルを目視確認。実アプリで保存→再読み込み→再保存を行い、寸法・seed・接続の一致を確認。
- 検証資料は Git 対象外の `build/p1-validation/`（`box-ui.png`、`reloaded-ui.png`、往復保存したシーン、ログ）。既存 `data/` は変更していない。

**検証環境の補足**

- vcpkg 再実行が `C:/vcpkg/buildtrees` の書き込み権限で失敗したため、導入済み依存を利用して `cmake --preset x64 -DVCPKG_MANIFEST_INSTALL=OFF` で再構成した。リポジトリのプリセットは変更していない。
- 起動中の既存 Release アプリが通常の出力先を使用していたため、MSBuild の `OutDir` で `build/p1-release/` に検証用実行ファイルを出力した。
- Debug の GPU ベースバリデーション有効時は初回描画までの待機が長く、今回の画面確認は Release で行った。Debug の起動描画確認は未完了。


### 2026-09-20 岩をモチーフにしたアプリアイコン

- 切断面と割れ目のある岩塊を描いた透過 PNG と、16 / 24 / 32 / 48 / 64 / 128 / 256px を含む ICO を作成。
- `assets/icons/rock_editor_v2.*` に画像・生成プロンプトを保存し、`RockEditor.rc` の参照先を更新。

### 2026-09-20 rock-editor への改名と土台のクリーンアップ

road-editor（旧 terrain-graph）を岩生成エディタの土台にするための整理。

**削除したもの**（約 14,000 行）

- `src/graph/`：Road / RoadMask / RoadProfile / Path / ConnectionPrototype /
  SurfaceLayout 一式（SurfaceLayout / Editing / Evaluation / BandGeometry / PresetGraph）
- `src/io/SurfaceLayoutIo`
- `src/app/`：ApplicationPathEdit / ApplicationSurfaceLayout / ApplicationSurfacePreset /
  ApplicationBoundaryMaterial / RoadMaskUi
- `tests/`：RoadTests / SurfaceLayoutTests / ConnectionPrototypeTests
- `tools/make_connection_prototype.py`
- ノードの種類：Road / Lane Marking / Road Mask / Decal / Shoulder / Crack / Path
- 値の型：Path / RoadMask
- レイヤーマテリアル（.tglayer）と境界マテリアル（.tgboundary）の編集・アセット機能（描画用の関連型・属性には残存あり）
- 旧形式（.tgproj / .mmproj / .mmmat）の読み込み互換

**残したもの**

DX12 レンダラ（rhi / renderer）、Dear ImGui、ビューポートと軌道カメラ、
ノードグラフ（Surface / Model / Transform / Merge / Mesh Output）、
マテリアル・テクスチャ・天球・モデルのアセット基盤、
プロジェクトの保存と読み込み、アセットブラウザ、アンドゥ。

**改名**

| 対象 | 旧 | 新 |
| --- | --- | --- |
| CMake プロジェクト / ターゲット | `road_editor` | `rock_editor` |
| vcpkg マニフェスト | `road-editor` | `rock-editor` |
| リソーススクリプト | `src/app/TerrainGraph.rc` | `src/app/RockEditor.rc` |
| C++ 名前空間 | `tg::` | `rock::` |
| マクロ接頭辞 | `TG_` | `ROCK_` |
| JSON 形式識別子 | `terrain-graph.*` | `rock-editor.*` |
| 内部フォルダ | `<root>/.terrain-graph` | `<root>/.rock-editor` |
| 設定の保存先 | `%LOCALAPPDATA%/road-editor` | `%LOCALAPPDATA%/rock-editor` |
| レイアウト | `road_editor_imgui.ini` | `rock_editor_imgui.ini` |
| プロジェクトの目印 | `project.tgproj` | `project.reproj` |
| シーン | `.tgscene` | `.rockscene` |
| マテリアル | `.tgmat` | `.rockmat` |
| 天球 | `.tgsky` | `.rocksky` |
| モデル | `.tgmodel` | `.rockmodel` |

Debug ビルドとテスト（`rock_editor_tests`）が通り、アプリが起動して
ビューポート・グラフ・アセットブラウザが表示されることを確認した。

## 未完了

### レビューの持ち越し（2026-09-22）

- 材質だけの編集でも `MarkDocumentChanged` が形状の再評価・全メッシュの再構築・再転送を起こす。ベイク指紋はそのたびに参照テクスチャのファイルを読み直す。材質の変更と形状の変更を分け、テクスチャのハッシュを読み込み時に控える。
- `Mesh::Create` がメッシュ1個ごとにGPU全同期を行う（最大512片）。転送を1回のコマンドにまとめる。Piece Transformは頂点へ焼かず、描画時の行列で渡す。
- ビューポートのリサイズごとの `WaitForGpu`、形状AOのタイルごとの同期待ち、最小化中にベイクが進まない点、デバイスロスト後に通知なく止まる点。
- 再ベイクのたびに一時の材質・テクスチャが増える（2026-09-22にファイルへの自動保存はやめたので、増えるのはメモリ上だけ。開き直すと消える）。
- ギズモのドラッグをEscで取り消すと、何も変わらないUndo段が残る。
- Volume to Mesh・Volume Transformは取消トークンを見ない（UV Unwrapは2026-09-22に対応。ただし島への分割の前半は打ち切れない）。
- Marching Tetrahedraの出力（解像度96で約16万〜40万三角形）がTo Volumeの入力上限25万三角形を超え得る（2026-09-22に追加したDecimateを挟めば減らせる）。
- CTestが全スイートを1件として実行する。`--test-gpu-ao` はCTest未登録。テストとアプリで同じソースを二重にコンパイルしている（共有の静的ライブラリ化が候補）。
- Releaseの実行ファイルがソースツリーの `shaders/` の絶対パスを参照する。

- （削除済み機能の残作業だった「完全分割の穴・複数ループ」「部分切断の不安定断面・複数交差」は、Joint Set / Crack / Fracture の削除により対象外。ボリューム上の操作として設計し直すときに改めて扱う）
- Volume Crack の割れ目の源を Points 以外（Pieces の切断面、Mesh の板ポリゴン、柱状・層状などの分割の種類）へ広げる。2026-09-22 のユーザー依頼で予定に入れた。詳細は [plan](plan.md) の「現在の方向」。
- Chip、OBJ 書き出し。（Joint Set / Crack / Fracture は削除済み。Triplanar と枝単位キャッシュは対応済み）
- P1 の Box は無地表示。Surface 接続と岩用 UV/Triplanar は未対応。既存の Transform ノードはモデル専用のまま。
- グラフからの寸法変更・Undo/Redo は自動テスト、Chunk ギズモは CLI 入力注入で確認。実マウス操作による一連の編集操作は未検証。

## 注意点

- src/renderer/MeshData.h には roadUv、道路・境界材質関連属性が残る。岩用トポロジーとは分離し、残存部分の全面整理を岩生成の前提作業にはしない。

- **`data/` は改名の対象外**（依頼で「触らない」と決めたため）。
  中身は road-editor 時代のまま（`project.tgproj`、`.terrain-graph/`、
  `BoundaryMaterials/`、`LayerMaterials/`、道路テクスチャなど約 1 GB）。
  新しい rock-editor はこれらを認識しないので、ルートを開くと
  `project.reproj` を新たに作る。不要になったら手で整理する。
- 旧 `assets/icons/rock_editor.*` は道路の絵柄の資料として保持。アプリは `rock_editor_v2.ico` を使用する。
- `AGENTS.md` / `CLAUDE.md` はバージョンの基準を `package.json` と書いているが、
  このプロジェクトに `package.json` は無い。実際の基準は `CMakeLists.txt` の
  `project(rock_editor VERSION ...)`。
- 旧 road-editor の状態は Git の最初のコミット（`chore: 既存の road_editor /
  TerrainGraph 時点のスナップショット`）に残してある。
